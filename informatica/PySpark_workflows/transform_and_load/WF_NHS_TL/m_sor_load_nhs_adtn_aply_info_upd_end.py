'''
* Script - M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_NHS_ADTN_APLY_INFO_STS")
        # Reading Data From Source - read_SSA_NHS_ADTN_APLY_INFO_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_NHS_ADTN_APLY_INFO_STS")
        
        logger.info("Step: read_SSA_NHS_ADTN_APLY_INFO")
        # Reading Data From Source - read_SSA_NHS_ADTN_APLY_INFO
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_NHS_ADTN_APLY_INFO")
        
        logger.info("Step: apply_SQ_SSA_NHS_ADTN_APLY_INFO_STS")
        # Source Qualifier: apply_SQ_SSA_NHS_ADTN_APLY_INFO_STS
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'E' OR OPR_IND = 'EB'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("ADTN_APLY_INFO_KEY", "BGN_DATE", "END_DATE", "ADTN_APLY_INFO_ATTR_VAL_TEXT", "ADTN_APLY_INFO_ATTR_TYPE_TEXT", "PHASE_CODE", "ROW_VER_NUM", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_NHS_ADTN_APLY_INFO")
        # Source Qualifier: apply_SQ_SSA_NHS_ADTN_APLY_INFO
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'E'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("ADTN_APLY_INFO_KEY", "HOS_APLY_KEY", "NHS_ADTN_APLY_INFO_ATTR_NAME", "AGMT_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_NHS_ADTN_APLY_INFO_STS")
        # Write to Target: write_SOR_NHS_ADTN_APLY_INFO_STS
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "adtn_aply_info_attr_val_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "adtn_aply_info_attr_val_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "adtn_aply_info_attr_type_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "adtn_aply_info_attr_type_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "phase_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "phase_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(TimestampType()))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("ADTN_APLY_INFO_ATTR_VAL_TEXT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("ADTN_APLY_INFO_ATTR_TYPE_TEXT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PHASE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("ROW_VER_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("LAST_REC_TXN_TYPE_CODE", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"ADTN_APLY_INFO_KEY": "ADTN_APLY_INFO_KEY", "BGN_DATE": "SOR_DATE", "END_DATE": "END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['ADTN_APLY_INFO_KEY', 'BGN_DATE', 'END_DATE', 'ADTN_APLY_INFO_ATTR_VAL_TEXT', 'ADTN_APLY_INFO_ATTR_TYPE_TEXT', 'PHASE_CODE', 'ROW_VER_NUM', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_NHS_ADTN_APLY_INFO_STS", mode="append")

        logger.info("write_SOR_NHS_ADTN_APLY_INFO_STS write completed")
        logger.info("Step: write_SOR_NHS_ADTN_APLY_INFO")
        # Write to Target: write_SOR_NHS_ADTN_APLY_INFO
        df_write = df_sq_4
        # Cast columns to match target schema data types
        if "nhs_adtn_aply_info_attr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "nhs_adtn_aply_info_attr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(TimestampType()))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("HOS_APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("NHS_ADTN_APLY_INFO_ATTR_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("LAST_REC_TXN_TYPE_CODE", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"ADTN_APLY_INFO_KEY": "ADTN_APLY_INFO_KEY", "AGMT_IND": "AGMT_IND", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['ADTN_APLY_INFO_KEY', 'HOS_APLY_KEY', 'NHS_ADTN_APLY_INFO_ATTR_NAME', 'AGMT_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_NHS_ADTN_APLY_INFO", mode="append")

        logger.info("write_SOR_NHS_ADTN_APLY_INFO write completed")
        
        metrics.complete()
        logger.info("Mapping M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_SOR_LOAD_NHS_ADTN_APLY_INFO_UPD_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()