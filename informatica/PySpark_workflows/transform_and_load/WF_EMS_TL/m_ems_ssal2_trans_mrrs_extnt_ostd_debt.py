'''
* Script - M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()


    v_snsh_date = ""
    v_init_flag = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    try:
        _param_obj = objects.get("UTL_JOB_PARAM", {})
        if isinstance(_param_obj, dict):
            _param_path = lib._resolve_path(_param_obj.get('path'))
        with open(_param_path, "r") as _f:
            for _line in _f:
                _line = _line.strip()
                for _var in [ "$$v_snsh_date",  "$$v_init_flag", ]:
                    if _line.startswith(_var + "="):
                        _val = _line.split("=", 1)[1]
                        _clean = _var.replace("$", "")
                        if _clean == "v_snsh_date":
                            v_snsh_date = _val
                        if _clean == "v_init_flag":
                            v_init_flag = _val
                        logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
    except Exception:
        logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_EMS_SRP_MRRS_EXTNT_OSTD_DEBT")
        # Reading Data From Source - read_EMS_SRP_MRRS_EXTNT_OSTD_DEBT
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_EMS_SRP_MRRS_EXTNT_OSTD_DEBT = lib.read_sql(spark, _conn, table="EMS_SRP_MRRS_EXTNT_OSTD_DEBT")
        
        logger.info("Step: apply_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT")
        # Source Qualifier: apply_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT
        df_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT = df_EMS_SRP_MRRS_EXTNT_OSTD_DEBT
        # Select only SQ output ports (matches Informatica behavior) — missing ports become lit(None)
        _port_cols = ["CUST_KEY", "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY", "SYS_RPT_YEAR", "SYS_RPT_MTH", "HSE_UNIT_CODE_ADDR", "CUST_MTH_RENT_AMT", "FRST_EXTNT_OSTD_DEBT_AMT", "SCND_EXTNT_OSTD_DEBT_AMT", "THRD_EXTNT_OSTD_DEBT_AMT", "EXTNT_OSTD_DEBT_STL_AMT", "EXTNT_OSTD_DEBT_STL_DATE", "AUCT_STL_AMT", "HSE_UNIT_VOID_BGN_DATE", "EXTNT_OSTD_DEBT_RMK_TEXT", "LAST_REC_TXN_USER_ID", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "FRTH_EXTNT_OSTD_DEBT_AMT"]
        df_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT = df_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT.select([col(c) if c.lower() in [x.lower() for x in df_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT.columns] else lit(None).alias(c) for c in _port_cols])
        ctx.register_df("df_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT", df_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = df_SQ_EMS_SRP_MRRS_EXTNT_OSTD_DEBT
        df_EXPTRANS = df_EXPTRANS.withColumn("LAST_REC_TXN_DATE", expr("current_timestamp()"))
        df_EXPTRANS = df_EXPTRANS.withColumn("OPR_IND", expr("'B'"))
        df_EXPTRANS = df_EXPTRANS.withColumn("CUST_KEY_V", expr("CASE WHEN (CUST_KEY IS NULL) THEN ' ' ELSE CUST_KEY END"))
        df_EXPTRANS = df_EXPTRANS.withColumn("HSE_SRVC_APLY_KEY_V", expr("CASE WHEN (HSE_SRVC_APLY_KEY IS NULL) THEN ' ' ELSE HSE_SRVC_APLY_KEY END"))
        df_EXPTRANS = df_EXPTRANS.withColumn("HSE_UNIT_KEY_V", expr("CASE WHEN (HSE_UNIT_KEY IS NULL) THEN ' ' ELSE HSE_UNIT_KEY END"))
        df_EXPTRANS = df_EXPTRANS.withColumn("SYS_RPT_YEAR_V", expr("CASE WHEN (SYS_RPT_YEAR IS NULL) THEN 0 ELSE SYS_RPT_YEAR END"))
        df_EXPTRANS = df_EXPTRANS.withColumn("SYS_RPT_MTH_V", expr("CASE WHEN (SYS_RPT_MTH IS NULL) THEN 0 ELSE SYS_RPT_MTH END"))
        df_EXPTRANS = df_EXPTRANS.withColumn("TNCY_AGRMT_BK", expr("lpad(CUST_KEY_V,9,'0') || lpad(HSE_SRVC_APLY_KEY_V,15,'0')"))
        df_EXPTRANS = df_EXPTRANS.withColumn("EXTNT_OSTD_DEBT_BK", expr("rpad(SYS_RPT_YEAR_V,4,' ') || rpad(SYS_RPT_MTH_V,2,' ') || rpad(CUST_KEY_V,9,' ') || rpad(HSE_SRVC_APLY_KEY_V,15,' ') || rpad(HSE_UNIT_KEY_V,9,' ')"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["CUST_KEY", "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY", "SYS_RPT_YEAR", "SYS_RPT_MTH", "HSE_UNIT_CODE_ADDR", "CUST_MTH_RENT_AMT", "FRST_EXTNT_OSTD_DEBT_AMT", "SCND_EXTNT_OSTD_DEBT_AMT", "THRD_EXTNT_OSTD_DEBT_AMT", "EXTNT_OSTD_DEBT_STL_AMT", "EXTNT_OSTD_DEBT_STL_DATE", "AUCT_STL_AMT", "HSE_UNIT_VOID_BGN_DATE", "EXTNT_OSTD_DEBT_RMK_TEXT", "LAST_REC_TXN_USER_ID", "LAST_REC_TXN_TYPE_CODE", "FRTH_EXTNT_OSTD_DEBT_AMT"]:
            if _col.lower() not in [x.lower() for x in df_EXPTRANS.columns]:
                df_EXPTRANS = df_EXPTRANS.withColumn(_col, lit(None))
        # Keep all upstream columns + computed columns (no select filtering)
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_MPLT_TRANS_TIME_STAMP_EXPTRANS3")
        # Expression: apply_MPLT_TRANS_TIME_STAMP_EXPTRANS3
        df_MPLT_TRANS_TIME_STAMP_EXPTRANS3 = df_EXPTRANS
        df_MPLT_TRANS_TIME_STAMP_EXPTRANS3 = df_MPLT_TRANS_TIME_STAMP_EXPTRANS3.withColumn("LAST_REC_TXN_DATE1", expr("CASE WHEN NOT (LAST_REC_TXN_DATE IS NULL) THEN date_format(LAST_REC_TXN_DATE, 'dd-MMM-yy hh.mm.ss') || '.000000 ' || date_format(LAST_REC_TXN_DATE, 'a') ELSE null END"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        # Keep all upstream columns + computed columns (no select filtering)
        ctx.register_df("df_MPLT_TRANS_TIME_STAMP_EXPTRANS3", df_MPLT_TRANS_TIME_STAMP_EXPTRANS3)
        
        logger.info("Step: apply_MPLT_TRANS_TIME_STAMP")
        # Expression: apply_MPLT_TRANS_TIME_STAMP
        df_MPLT_TRANS_TIME_STAMP = df_MPLT_TRANS_TIME_STAMP_EXPTRANS3
        ctx.register_df("df_MPLT_TRANS_TIME_STAMP", df_MPLT_TRANS_TIME_STAMP)
        
        
        metrics.complete()
        logger.info("Mapping M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SSAL2_TRANS_MRRS_EXTNT_OSTD_DEBT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())