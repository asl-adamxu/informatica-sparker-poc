'''
* Script - M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_PRV_APLY_MARK_FLAT_STS")
        # Reading Data From Source - read_SSA_EMS_PRV_APLY_MARK_FLAT_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_PRV_APLY_MARK_FLAT_STS")
        
        logger.info("Step: read_SSA_EMS_PRV_APLY_MARK_FLAT")
        # Reading Data From Source - read_SSA_EMS_PRV_APLY_MARK_FLAT
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_EMS_PRV_APLY_MARK_FLAT")
        
        logger.info("Step: apply_SQ_SSA_EMS_PRV_APLY_MARK_FLAT_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_PRV_APLY_MARK_FLAT_STS
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'E' OR OPR_IND = 'EB'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("APLY_MARK_FLAT_KEY", "BGN_DATE", "END_DATE", "APLY_KEY", "RSRV_HIST_KEY", "RSRV_KEY", "ADTN_ROOM_IND", "MARK_FLAT_STS_CODE", "MARK_FLAT_STS_CODE_UPD_DATE", "MARK_FLAT_DATE", "MARK_FLAT_END_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE", "CNV_MARK_FLAT_PHASE_TYPE_TEXT")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_EMS_PRV_APLY_MARK_FLAT")
        # Source Qualifier: apply_SQ_SSA_EMS_PRV_APLY_MARK_FLAT
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'E'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("APLY_MARK_FLAT_KEY", "APLY_MARK_FLAT_BK", "AGMT_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_EMS_PRV_APLY_MARK_FLAT_STS")
        # Write to Target: write_SOR_EMS_PRV_APLY_MARK_FLAT_STS
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "adtn_room_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "adtn_room_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "mark_flat_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "mark_flat_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "mark_flat_sts_code_upd_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "mark_flat_sts_code_upd_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "mark_flat_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "mark_flat_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "mark_flat_end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "mark_flat_end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "cnv_mark_flat_phase_type_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cnv_mark_flat_phase_type_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RSRV_HIST_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RSRV_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("ADTN_ROOM_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("MARK_FLAT_STS_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("MARK_FLAT_STS_CODE_UPD_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("MARK_FLAT_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("MARK_FLAT_END_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("LAST_REC_TXN_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("CNV_MARK_FLAT_PHASE_TYPE_TEXT", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"APLY_MARK_FLAT_KEY": "APLY_MARK_FLAT_KEY", "BGN_DATE": "SOR_DATE", "END_DATE": "END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['APLY_MARK_FLAT_KEY', 'BGN_DATE', 'END_DATE', 'APLY_KEY', 'RSRV_HIST_KEY', 'RSRV_KEY', 'ADTN_ROOM_IND', 'MARK_FLAT_STS_CODE', 'MARK_FLAT_STS_CODE_UPD_DATE', 'MARK_FLAT_DATE', 'MARK_FLAT_END_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'CNV_MARK_FLAT_PHASE_TYPE_TEXT']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PRV_APLY_MARK_FLAT_STS", mode="append")

        logger.info("write_SOR_EMS_PRV_APLY_MARK_FLAT_STS write completed")
        logger.info("Step: write_SOR_EMS_PRV_APLY_MARK_FLAT")
        # Write to Target: write_SOR_EMS_PRV_APLY_MARK_FLAT
        df_write = df_sq_4
        # Cast columns to match target schema data types
        if "aply_mark_flat_bk" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "aply_mark_flat_bk":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("APLY_MARK_FLAT_BK", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"AGMT_IND": "AGMT_IND", "APLY_MARK_FLAT_KEY": "APLY_MARK_FLAT_KEY", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['APLY_MARK_FLAT_KEY', 'APLY_MARK_FLAT_BK', 'AGMT_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PRV_APLY_MARK_FLAT", mode="append")

        logger.info("write_SOR_EMS_PRV_APLY_MARK_FLAT write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_PRV_APLY_MARK_FLAT_FOR_UPDATE_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()