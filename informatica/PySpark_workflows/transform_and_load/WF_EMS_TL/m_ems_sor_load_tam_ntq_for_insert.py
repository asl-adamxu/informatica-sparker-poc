'''
* Script - M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_TAM_NTQ")
        # Reading Data From Source - read_SSA_EMS_TAM_NTQ
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_TAM_NTQ")
        
        logger.info("Step: read_SSA_EMS_TAM_NTQ_STS")
        # Reading Data From Source - read_SSA_EMS_TAM_NTQ_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_EMS_TAM_NTQ")
        
        logger.info("Step: apply_SQ_SSA_EMS_TAM_NTQ")
        # Source Qualifier: apply_SQ_SSA_EMS_TAM_NTQ
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'B' OR OPR_IND = 'A'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("NTQ_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "NTQ_CRE_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "AGMT_IND", "OPR_IND", "SOR_DATE", "TNCY_AGRMT_KEY")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_EMS_TAM_NTQ_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_TAM_NTQ_STS
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'B' OR OPR_IND = 'EB' OR OPR_IND = 'DA'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("NTQ_KEY", "BGN_DATE", "NTQ_LTR_DATE", "NTQ_STS_CODE", "NTQ_END_DATE", "NTQ_END_RMDR_SBMT_DATE", "LAST_NTQ_STS_UPD_DATE", "ABU_KEY", "ACT_IND", "APL_DATE", "APL_HEAR_DATE", "APL_INTT_USER_CODE", "APL_PNL_RSLT_DATE", "APL_RMK_TEXT", "APL_RSLT_CODE", "LIC_FEE_END_DATE", "NTQ_BU_DATE", "NTQ_ISS_DATE", "NTQ_ISS_RSN_TEXT", "NTQ_PRLNG_STYPT_RSN_TEXT", "NTQ_PRN_IND", "NTQ_PRN_RMK_TEXT", "NTQ_STL_RSN_CODE", "NTQ_STL_RSN_TEXT", "ORIG_NEXT_RENT_BGN_DATE", "ORIG_NEXT_RENT_CHNG_RSN_CODE", "ORIG_NEXT_RENT_CHNG_RSN_TEXT", "ORIG_NEXT_RENT_END_DATE", "ORIG_NEXT_RENT_FCTR_CODE", "ORIG_NEXT_RENT_RVW_CATG_CODE", "ORIG_NEXT_RENT_RVW_CATG_DATE", "ORIG_PYMT_MTHD_CODE", "ORIG_RENT_BGN_DATE", "ORIG_RENT_END_DATE", "ORIG_RENT_FCTR_CODE", "PYMT_MTHD_CODE", "RENT_FCTR_KEY", "NTQ_ISS_RSN_CODE", "NTQ_STS_UPD_DATE", "NTQ_WTHDRW_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "END_DATE", "OPR_IND", "SOR_DATE", "APL_MEMO_CNFRM_DATE", "APL_PNL_PPR_SBMT_DATE", "APL_DATE_UPD_DATE", "MISC_HSE_BNFT_GNRT_IND", "LAST_SBMT_USER_ID", "LAST_SBMT_DATE", "LAST_APRV_USER_ID", "LAST_APRV_DATE", "ORIG_RENT_EXMPT_IND")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_EMS_TAM_NTQ")
        # Write to Target: write_SOR_EMS_TAM_NTQ
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ntq_cre_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ntq_cre_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"AGMT_IND": "AGMT_IND", "CUST_KEY": "CUST_KEY", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "NTQ_CRE_DATE": "NTQ_CRE_DATE", "NTQ_KEY": "NTQ_KEY", "TNCY_AGRMT_KEY": "TNCY_AGRMT_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['NTQ_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'NTQ_CRE_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'AGMT_IND', 'TNCY_AGRMT_KEY']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_TAM_NTQ", mode="append")

        logger.info("write_SOR_EMS_TAM_NTQ write completed")
        logger.info("Step: apply_EXP_SOR_LOAD_DATE")
        # Expression: apply_EXP_SOR_LOAD_DATE
        df_exp_5 = df_sq_4
        df_exp_5 = df_exp_5.withColumn("OUT_END_DATE", expr("CASE WHEN OPR_IND = 'EB' THEN CASE WHEN LAST_REC_TXN_TYPE_CODE = NULL THEN to_date('99991231','yyyyMMdd') ELSE END_DATE END ELSE END_DATE END"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        # Select only mapping output ports (prevents column leakage)
        df_exp_5 = df_exp_5.select("OUT_END_DATE")
        ctx.register_df("df_exp_5", df_exp_5)
        
        logger.info("Step: join_target_SOR_EMS_TAM_NTQ_STS_0")
        # Lookup: join_target_SOR_EMS_TAM_NTQ_STS_0
        # Merge parallel DataFrames on their common columns
        _common_cols = [c for c in df_sq_4.columns if c in df_exp_5.columns]
        df_tgt_merge_6 = df_sq_4.join(
            df_exp_5,
            on=_common_cols,
            how="left"
        )
        ctx.register_df("df_tgt_merge_6", df_tgt_merge_6)
        
        logger.info("Step: write_SOR_EMS_TAM_NTQ_STS")
        # Write to Target: write_SOR_EMS_TAM_NTQ_STS
        df_write = df_tgt_merge_6
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ntq_cre_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ntq_cre_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("CUST_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_SRVC_APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("NTQ_CRE_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("AGMT_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("TNCY_AGRMT_KEY", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"ABU_KEY": "ABU_KEY", "ACT_IND": "ACT_IND", "APL_DATE": "APL_DATE", "APL_DATE_UPD_DATE": "APL_DATE_UPD_DATE", "APL_HEAR_DATE": "APL_HEAR_DATE", "APL_INTT_USER_CODE": "APL_INTT_USER_CODE", "APL_MEMO_CNFRM_DATE": "APL_MEMO_CNFRM_DATE", "APL_PNL_PPR_SBMT_DATE": "APL_PNL_PPR_SBMT_DATE", "APL_PNL_RSLT_DATE": "APL_PNL_RSLT_DATE", "APL_RMK_TEXT": "APL_RMK_TEXT", "APL_RSLT_CODE": "APL_RSLT_CODE", "BGN_DATE": "BGN_DATE", "END_DATE": "OUT_END_DATE", "LAST_APRV_DATE": "LAST_APRV_DATE", "LAST_APRV_USER_ID": "LAST_APRV_USER_ID", "LAST_NTQ_STS_UPD_DATE": "LAST_NTQ_STS_UPD_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "LAST_SBMT_DATE": "LAST_SBMT_DATE", "LAST_SBMT_USER_ID": "LAST_SBMT_USER_ID", "LIC_FEE_END_DATE": "LIC_FEE_END_DATE", "MISC_HSE_BNFT_GNRT_IND": "MISC_HSE_BNFT_GNRT_IND", "NTQ_BU_DATE": "NTQ_BU_DATE", "NTQ_END_DATE": "NTQ_END_DATE", "NTQ_END_RMDR_SBMT_DATE": "NTQ_END_RMDR_SBMT_DATE", "NTQ_ISS_DATE": "NTQ_ISS_DATE", "NTQ_ISS_RSN_CODE": "NTQ_ISS_RSN_CODE", "NTQ_ISS_RSN_TEXT": "NTQ_ISS_RSN_TEXT", "NTQ_KEY": "NTQ_KEY", "NTQ_LTR_DATE": "NTQ_LTR_DATE", "NTQ_PRLNG_STYPT_RSN_TEXT": "NTQ_PRLNG_STYPT_RSN_TEXT", "NTQ_PRN_IND": "NTQ_PRN_IND", "NTQ_PRN_RMK_TEXT": "NTQ_PRN_RMK_TEXT", "NTQ_STL_RSN_CODE": "NTQ_STL_RSN_CODE", "NTQ_STL_RSN_TEXT": "NTQ_STL_RSN_TEXT", "NTQ_STS_CODE": "NTQ_STS_CODE", "NTQ_STS_UPD_DATE": "NTQ_STS_UPD_DATE", "NTQ_WTHDRW_IND": "NTQ_WTHDRW_IND", "ORIG_NEXT_RENT_BGN_DATE": "ORIG_NEXT_RENT_BGN_DATE", "ORIG_NEXT_RENT_CHNG_RSN_CODE": "ORIG_NEXT_RENT_CHNG_RSN_CODE", "ORIG_NEXT_RENT_CHNG_RSN_TEXT": "ORIG_NEXT_RENT_CHNG_RSN_TEXT", "ORIG_NEXT_RENT_END_DATE": "ORIG_NEXT_RENT_END_DATE", "ORIG_NEXT_RENT_FCTR_CODE": "ORIG_NEXT_RENT_FCTR_CODE", "ORIG_NEXT_RENT_RVW_CATG_CODE": "ORIG_NEXT_RENT_RVW_CATG_CODE", "ORIG_NEXT_RENT_RVW_CATG_DATE": "ORIG_NEXT_RENT_RVW_CATG_DATE", "ORIG_PYMT_MTHD_CODE": "ORIG_PYMT_MTHD_CODE", "ORIG_RENT_BGN_DATE": "ORIG_RENT_BGN_DATE", "ORIG_RENT_END_DATE": "ORIG_RENT_END_DATE", "ORIG_RENT_EXMPT_IND": "ORIG_RENT_EXMPT_IND", "ORIG_RENT_FCTR_CODE": "ORIG_RENT_FCTR_CODE", "PYMT_MTHD_CODE": "PYMT_MTHD_CODE", "RENT_FCTR_KEY": "RENT_FCTR_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['NTQ_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'NTQ_CRE_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'AGMT_IND', 'TNCY_AGRMT_KEY']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_TAM_NTQ", mode="append")

        logger.info("write_SOR_EMS_TAM_NTQ_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SOR_LOAD_TAM_NTQ_FOR_INSERT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()