'''
* Script - M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_HSM_TPS_UNIT_STS")
        # Reading Data From Source - read_SSA_EMS_HSM_TPS_UNIT_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_HSM_TPS_UNIT_STS")
        
        logger.info("Step: apply_SQ_SSA_EMS_HSM_TPS_UNIT_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_HSM_TPS_UNIT_STS
        df_sq_2 = df_src_1
        df_sq_2 = df_sq_2.filter(expr("OPR_IND = 'E' OR OPR_IND = 'EB'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("UNIT_KEY", "BGN_DATE", "END_DATE", "OWNR_CODE", "UNIT_SALE_ASGN_DATE", "UNIT_BYBK_DATE", "UNIT_TNTV_BYBK_DATE", "UNIT_FRST_ASGN_DATE", "UNIT_SALE_CNT", "UNIT_SLBL_IND", "UNIT_PCHS_IND", "UNIT_STCK_TYPE_CODE", "UNIT_SALE_TYPE_CODE", "UNIT_MGT_SHR_IND", "UNIT_PLNTR_AVAIL_CODE", "UNIT_STS_CODE", "UNIT_STS_UPD_DATE", "UNIT_PREM_PAY_DATE", "UNIT_PREM_PAY_UPD_DATE", "UNIT_MGT_SHR_AMT", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: write_SOR_EMS_HSM_TPS_UNIT_STS")
        # Write to Target: write_SOR_EMS_HSM_TPS_UNIT_STS
        df_write = df_sq_2
        # Cast columns to match target schema data types
        if "bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "ownr_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ownr_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_sale_asgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_sale_asgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "unit_bybk_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_bybk_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "unit_tntv_bybk_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_tntv_bybk_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "unit_frst_asgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_frst_asgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "unit_slbl_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_slbl_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_pchs_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_pchs_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_stck_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_stck_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_sale_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_sale_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_mgt_shr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_mgt_shr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_plntr_avail_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_plntr_avail_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_sts_upd_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_sts_upd_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "unit_prem_pay_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_prem_pay_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "unit_prem_pay_upd_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_prem_pay_upd_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("OWNR_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_SALE_ASGN_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_BYBK_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_TNTV_BYBK_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_FRST_ASGN_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_SALE_CNT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_SLBL_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_PCHS_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_STCK_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_SALE_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_MGT_SHR_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_PLNTR_AVAIL_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_STS_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_STS_UPD_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_PREM_PAY_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_PREM_PAY_UPD_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_MGT_SHR_AMT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("LAST_REC_TXN_TYPE_CODE", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"BGN_DATE": "SOR_DATE", "END_DATE": "END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "UNIT_KEY": "UNIT_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['UNIT_KEY', 'BGN_DATE', 'END_DATE', 'OWNR_CODE', 'UNIT_SALE_ASGN_DATE', 'UNIT_BYBK_DATE', 'UNIT_TNTV_BYBK_DATE', 'UNIT_FRST_ASGN_DATE', 'UNIT_SALE_CNT', 'UNIT_SLBL_IND', 'UNIT_PCHS_IND', 'UNIT_STCK_TYPE_CODE', 'UNIT_SALE_TYPE_CODE', 'UNIT_MGT_SHR_IND', 'UNIT_PLNTR_AVAIL_CODE', 'UNIT_STS_CODE', 'UNIT_STS_UPD_DATE', 'UNIT_PREM_PAY_DATE', 'UNIT_PREM_PAY_UPD_DATE', 'UNIT_MGT_SHR_AMT', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_HSM_TPS_UNIT_STS", mode="append")

        logger.info("write_SOR_EMS_HSM_TPS_UNIT_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_HSM_TPS_UNIT_FOR_UPDATE_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()