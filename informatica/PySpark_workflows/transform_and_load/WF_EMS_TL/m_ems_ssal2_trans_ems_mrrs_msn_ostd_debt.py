'''
* Script - M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_EMS_MRRS_MSN_PRFT_OSTD_DEBT")
        # Reading Data From Source - read_EMS_MRRS_MSN_PRFT_OSTD_DEBT
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="EMS_MRRS_MSN_PRFT_OSTD_DEBT")
        
        logger.info("Step: apply_SQ_EMS_MRRS_MSN_PRFT_OSTD_DEBT")
        # Source Qualifier: apply_SQ_EMS_MRRS_MSN_PRFT_OSTD_DEBT
        df_sq_2 = df_src_1
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("CUST_KEY", "HSE_SRVC_APLY_KEY", "SYS_RPT_YEAR", "SYS_RPT_MTH", "HSE_UNIT_KEY", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "CUST_MTH_MSN_PRFT_AMT", "FRST_EXCUST_OSTD_DEBT_AMT", "SCND_EXCUST_OSTD_DEBT_AMT", "THRD_EXCUST_OSTD_DEBT_AMT", "EXCUST_OSTD_DEBT_STL_AMT", "EXCUST_OSTD_DEBT_STL_DATE", "AUCT_STL_AMT", "HSE_UNIT_VOID_BGN_DATE", "EXCUST_OSTD_DEBT_RMK_TEXT", "LAST_REC_TXN_USER_ID")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_exp_3 = df_sq_2
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_DATE", expr("current_timestamp()"))
        df_exp_3 = df_exp_3.withColumn("OPR_IND", expr("'B'"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["CUST_KEY", "HSE_SRVC_APLY_KEY", "SYS_RPT_YEAR", "SYS_RPT_MTH", "HSE_UNIT_KEY", "LAST_REC_TXN_TYPE_CODE", "CUST_MTH_MSN_PRFT_AMT", "FRST_EXCUST_OSTD_DEBT_AMT", "SCND_EXCUST_OSTD_DEBT_AMT", "THRD_EXCUST_OSTD_DEBT_AMT", "EXCUST_OSTD_DEBT_STL_AMT", "EXCUST_OSTD_DEBT_STL_DATE", "AUCT_STL_AMT", "HSE_UNIT_VOID_BGN_DATE", "EXCUST_OSTD_DEBT_RMK_TEXT", "LAST_REC_TXN_USER_ID"]:
            if _col not in df_exp_3.columns:
                df_exp_3 = df_exp_3.withColumn(_col, lit(None))
        # Select only mapping output ports (prevents column leakage)
        df_exp_3 = df_exp_3.select("CUST_KEY", "HSE_SRVC_APLY_KEY", "SYS_RPT_YEAR", "SYS_RPT_MTH", "HSE_UNIT_KEY", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "CUST_MTH_MSN_PRFT_AMT", "FRST_EXCUST_OSTD_DEBT_AMT", "SCND_EXCUST_OSTD_DEBT_AMT", "THRD_EXCUST_OSTD_DEBT_AMT", "EXCUST_OSTD_DEBT_STL_AMT", "EXCUST_OSTD_DEBT_STL_DATE", "AUCT_STL_AMT", "HSE_UNIT_VOID_BGN_DATE", "EXCUST_OSTD_DEBT_RMK_TEXT", "LAST_REC_TXN_USER_ID", "OPR_IND")
        ctx.register_df("df_exp_3", df_exp_3)
        
        logger.info("Step: apply_mplt_EXPTRANS3")
        # Expression: apply_mplt_EXPTRANS3
        df_mplt_expr_4 = df_exp_3
        df_mplt_expr_4 = df_mplt_expr_4.withColumn("LAST_REC_TXN_DATE1", expr("CASE WHEN NOT (LAST_REC_TXN_DATE IS NULL) THEN cast(LAST_REC_TXN_DATE as string) || '.000000 ' || cast(LAST_REC_TXN_DATE as string) ELSE null END"))
        ctx.register_df("df_mplt_expr_4", df_mplt_expr_4)
        
        logger.info("Step: apply_MPLT_TRANS_TIME_STAMP")
        # Expression: apply_MPLT_TRANS_TIME_STAMP
        df_mplt_5 = df_mplt_expr_4
        ctx.register_df("df_mplt_5", df_mplt_5)
        
        logger.info("Step: join_target_SSA_EMS_MRRS_MSN_OSTD_DEBT_0")
        # Lookup: join_target_SSA_EMS_MRRS_MSN_OSTD_DEBT_0
        # Merge parallel DataFrames on their common columns
        _common_cols = [c for c in df_mplt_5.columns if c in df_exp_3.columns]
        df_tgt_merge_6 = df_mplt_5.join(
            df_exp_3,
            on=_common_cols,
            how="left"
        )
        ctx.register_df("df_tgt_merge_6", df_tgt_merge_6)
        
        logger.info("Step: write_SSA_EMS_MRRS_MSN_OSTD_DEBT")
        # Write to Target: write_SSA_EMS_MRRS_MSN_OSTD_DEBT
        df_write = df_tgt_merge_6
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "excust_ostd_debt_stl_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excust_ostd_debt_stl_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "hse_unit_void_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_void_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "excust_ostd_debt_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excust_ostd_debt_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "opr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "opr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"AUCT_STL_AMT": "AUCT_STL_AMT", "CUST_KEY": "CUST_KEY", "CUST_MTH_MSN_PRFT_AMT": "CUST_MTH_MSN_PRFT_AMT", "EXCUST_OSTD_DEBT_RMK_TEXT": "EXCUST_OSTD_DEBT_RMK_TEXT", "EXCUST_OSTD_DEBT_STL_AMT": "EXCUST_OSTD_DEBT_STL_AMT", "EXCUST_OSTD_DEBT_STL_DATE": "EXCUST_OSTD_DEBT_STL_DATE", "FRST_EXCUST_OSTD_DEBT_AMT": "FRST_EXCUST_OSTD_DEBT_AMT", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY": "HSE_UNIT_KEY", "HSE_UNIT_VOID_BGN_DATE": "HSE_UNIT_VOID_BGN_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE1", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "OPR_IND": "OPR_IND", "SCND_EXCUST_OSTD_DEBT_AMT": "SCND_EXCUST_OSTD_DEBT_AMT", "SYS_RPT_MTH": "SYS_RPT_MTH", "SYS_RPT_YEAR": "SYS_RPT_YEAR", "THRD_EXCUST_OSTD_DEBT_AMT": "THRD_EXCUST_OSTD_DEBT_AMT"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['CUST_KEY', 'HSE_SRVC_APLY_KEY', 'SYS_RPT_YEAR', 'SYS_RPT_MTH', 'HSE_UNIT_KEY', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'CUST_MTH_MSN_PRFT_AMT', 'FRST_EXCUST_OSTD_DEBT_AMT', 'SCND_EXCUST_OSTD_DEBT_AMT', 'THRD_EXCUST_OSTD_DEBT_AMT', 'EXCUST_OSTD_DEBT_STL_AMT', 'EXCUST_OSTD_DEBT_STL_DATE', 'AUCT_STL_AMT', 'HSE_UNIT_VOID_BGN_DATE', 'EXCUST_OSTD_DEBT_RMK_TEXT', 'OPR_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SSA_EMS_MRRS_MSN_OSTD_DEBT", mode="append")

        logger.info("write_SSA_EMS_MRRS_MSN_OSTD_DEBT write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SSAL2_TRANS_EMS_MRRS_MSN_OSTD_DEBT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()