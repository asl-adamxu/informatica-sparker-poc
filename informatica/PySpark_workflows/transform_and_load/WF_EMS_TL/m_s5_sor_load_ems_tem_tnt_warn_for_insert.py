'''
* Script - M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_TEM_TNT_WARN")
        # Reading Data From Source - read_SSA_EMS_TEM_TNT_WARN
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_SSA_EMS_TEM_TNT_WARN = lib.read_sql(spark, _conn, table="SSA_EMS_TEM_TNT_WARN")
        
        logger.info("Step: read_SSA_EMS_TEM_TNT_WARN_STS")
        # Reading Data From Source - read_SSA_EMS_TEM_TNT_WARN_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_SSA_EMS_TEM_TNT_WARN_STS = lib.read_sql(spark, _conn, table="SSA_EMS_TEM_TNT_WARN_STS")
        
        logger.info("Step: apply_SQ_SSA_EMS_TEM_TNT_WARN")
        # Source Qualifier: apply_SQ_SSA_EMS_TEM_TNT_WARN
        df_SQ_SSA_EMS_TEM_TNT_WARN = df_SSA_EMS_TEM_TNT_WARN
        df_SQ_SSA_EMS_TEM_TNT_WARN = df_SQ_SSA_EMS_TEM_TNT_WARN.filter(expr("OPR_IND = 'B' OR OPR_IND = 'A'"))
        # Select only SQ output ports (matches Informatica behavior) — missing ports become lit(None)
        _port_cols = ["TNT_WARN_KEY", "TNT_WARN_BK", "WARN_SEQ_NUM", "CUST_KEY", "HSE_SRVC_APLY_KEY", "AGMT_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE"]
        df_SQ_SSA_EMS_TEM_TNT_WARN = df_SQ_SSA_EMS_TEM_TNT_WARN.select([col(c) if c.lower() in [x.lower() for x in df_SQ_SSA_EMS_TEM_TNT_WARN.columns] else lit(None).alias(c) for c in _port_cols])
        ctx.register_df("df_SQ_SSA_EMS_TEM_TNT_WARN", df_SQ_SSA_EMS_TEM_TNT_WARN)
        
        logger.info("Step: apply_SQ_SSA_EMS_TEM_TNT_WARN_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_TEM_TNT_WARN_STS
        df_SQ_SSA_EMS_TEM_TNT_WARN_STS = df_SSA_EMS_TEM_TNT_WARN_STS
        df_SQ_SSA_EMS_TEM_TNT_WARN_STS = df_SQ_SSA_EMS_TEM_TNT_WARN_STS.filter(expr("OPR_IND = 'B' OR OPR_IND = 'EB' OR OPR_IND = 'DA'"))
        # Select only SQ output ports (matches Informatica behavior) — missing ports become lit(None)
        _port_cols = ["TNT_WARN_KEY", "BGN_DATE", "END_DATE", "WARN_TYPE_CODE", "WARN_LTR_TYPE_CODE", "OFNC_ITEM_CODE", "OFNC_DATE", "SCORE_NUM", "WARN_LTR_STS_CODE", "WARN_LTR_PRCS_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE"]
        df_SQ_SSA_EMS_TEM_TNT_WARN_STS = df_SQ_SSA_EMS_TEM_TNT_WARN_STS.select([col(c) if c.lower() in [x.lower() for x in df_SQ_SSA_EMS_TEM_TNT_WARN_STS.columns] else lit(None).alias(c) for c in _port_cols])
        ctx.register_df("df_SQ_SSA_EMS_TEM_TNT_WARN_STS", df_SQ_SSA_EMS_TEM_TNT_WARN_STS)
        
        logger.info("Step: write_SOR_EMS_TEM_TNT_WARN")
        # Write to Target: write_SOR_EMS_TEM_TNT_WARN
        df_write = df_SQ_SSA_EMS_TEM_TNT_WARN
        # Map source columns to target columns using connector field map (handles name
        # mismatches) — done BEFORE the _update_flag split so UPDATE/DELETE use target
        # column names in batch_update/batch_delete.
        _field_map = {"AGMT_IND": "AGMT_IND", "CUST_KEY": "CUST_KEY", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "TNT_WARN_BK": "TNT_WARN_BK", "TNT_WARN_KEY": "TNT_WARN_KEY", "WARN_SEQ_NUM": "WARN_SEQ_NUM"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col.lower() not in [x.lower() for x in df_write.columns] and _src_col.lower() in [x.lower() for x in df_write.columns]:
                # Drop any column that would conflict case-insensitively with the target name 
                for _c in list(df_write.columns):
                    if _c.lower() == _tgt_col.lower() and _c != _src_col:
                        df_write = df_write.drop(_c)
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['TNT_WARN_KEY', 'TNT_WARN_BK', 'WARN_SEQ_NUM', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'AGMT_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col.lower() in [x.lower() for x in df_write.columns]])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_TEM_TNT_WARN", mode="append")

        logger.info("write_SOR_EMS_TEM_TNT_WARN write completed")
        logger.info("Step: apply_EXP_SOR_LOAD_DATE")
        # Expression: apply_EXP_SOR_LOAD_DATE
        df_EXP_SOR_LOAD_DATE = df_SQ_SSA_EMS_TEM_TNT_WARN_STS
        df_EXP_SOR_LOAD_DATE = df_EXP_SOR_LOAD_DATE.withColumn("OUT_END_DATE", expr("CASE WHEN OPR_IND = 'EB' THEN CASE WHEN LAST_REC_TXN_TYPE_CODE IS NULL THEN to_date('99991231','yyyyMMdd') ELSE END_DATE END ELSE END_DATE END"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        # Keep all upstream columns + computed columns (no select filtering)
        ctx.register_df("df_EXP_SOR_LOAD_DATE", df_EXP_SOR_LOAD_DATE)
        
        logger.info("Step: write_SOR_EMS_TEM_TNT_WARN_STS")
        # Write to Target: write_SOR_EMS_TEM_TNT_WARN_STS
        df_write = df_EXP_SOR_LOAD_DATE
        # Map source columns to target columns using connector field map (handles name
        # mismatches) — done BEFORE the _update_flag split so UPDATE/DELETE use target
        # column names in batch_update/batch_delete.
        _field_map = {"BGN_DATE": "BGN_DATE", "END_DATE": "OUT_END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "OFNC_DATE": "OFNC_DATE", "OFNC_ITEM_CODE": "OFNC_ITEM_CODE", "SCORE_NUM": "SCORE_NUM", "TNT_WARN_KEY": "TNT_WARN_KEY", "WARN_LTR_PRCS_DATE": "WARN_LTR_PRCS_DATE", "WARN_LTR_STS_CODE": "WARN_LTR_STS_CODE", "WARN_LTR_TYPE_CODE": "WARN_LTR_TYPE_CODE", "WARN_TYPE_CODE": "WARN_TYPE_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col.lower() not in [x.lower() for x in df_write.columns] and _src_col.lower() in [x.lower() for x in df_write.columns]:
                # Drop any column that would conflict case-insensitively with the target name 
                for _c in list(df_write.columns):
                    if _c.lower() == _tgt_col.lower() and _c != _src_col:
                        df_write = df_write.drop(_c)
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['TNT_WARN_KEY', 'BGN_DATE', 'END_DATE', 'WARN_TYPE_CODE', 'WARN_LTR_TYPE_CODE', 'OFNC_ITEM_CODE', 'OFNC_DATE', 'SCORE_NUM', 'WARN_LTR_STS_CODE', 'WARN_LTR_PRCS_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col.lower() in [x.lower() for x in df_write.columns]])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_TEM_TNT_WARN_STS", mode="append")

        logger.info("write_SOR_EMS_TEM_TNT_WARN_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_TEM_TNT_WARN_FOR_INSERT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())