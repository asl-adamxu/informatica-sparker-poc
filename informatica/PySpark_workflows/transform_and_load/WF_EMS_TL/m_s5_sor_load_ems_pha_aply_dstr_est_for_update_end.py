'''
* Script - M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_PHA_APLY_DSTR_EST")
        # Reading Data From Source - read_SSA_EMS_PHA_APLY_DSTR_EST
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_PHA_APLY_DSTR_EST")
        
        logger.info("Step: read_SSA_EMS_PHA_APLY_DSTR_EST_STS")
        # Reading Data From Source - read_SSA_EMS_PHA_APLY_DSTR_EST_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_EMS_PHA_APLY_DSTR_EST_STS")
        
        logger.info("Step: apply_SQ_SSA_EMS_PHA_APLY_DSTR_EST")
        # Source Qualifier: apply_SQ_SSA_EMS_PHA_APLY_DSTR_EST
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'E'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("APLY_SLCT_DSTR_EST_CHC_KEY", "APLY_SLCT_DSTR_EST_CHC_BK", "APLY_KEY", "AGMT_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_EMS_PHA_APLY_DSTR_EST_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_PHA_APLY_DSTR_EST_STS
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'E' OR OPR_IND = 'EB'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("APLY_SLCT_DSTR_EST_CHC_KEY", "BGN_DATE", "END_DATE", "DSTR_CHC_KEY_1", "DSTR_CHC_KEY_2", "DSTR_CHC_KEY_3", "EST_CHC_KEY_1", "EST_CHC_KEY_2", "EST_CHC_KEY_3", "EST_CHC_KEY_4", "EST_CHC_KEY_5", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "FLAT_TYPE_CHC_CODE_1", "FLAT_TYPE_CHC_CODE_2", "FLAT_TYPE_CHC_CODE_3", "IFA_CHC_AREA_1", "IFA_CHC_AREA_2", "IFA_CHC_AREA_3", "OPR_IND", "SOR_DATE", "HSE_DSTR_REF_TYPE_CODE_1", "HSE_DSTR_REF_TYPE_CODE_2", "HSE_DSTR_REF_TYPE_CODE_3", "HSE_EST_REF_TYPE_CODE_1", "HSE_EST_REF_TYPE_CODE_2", "HSE_EST_REF_TYPE_CODE_3", "HSE_EST_REF_TYPE_CODE_4", "HSE_EST_REF_TYPE_CODE_5", "PTCL_PPLR_HSE_EST_CHC_KEY_1", "PTCL_PPLR_HSE_EST_CHC_KEY_2", "PTCL_PPLR_EST_REF_TYPE_CODE_1", "PTCL_PPLR_EST_REF_TYPE_CODE_2")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_EMS_PHA_APLY_DSTR_EST")
        # Write to Target: write_SOR_EMS_PHA_APLY_DSTR_EST
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "aply_slct_dstr_est_chc_bk" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "aply_slct_dstr_est_chc_bk":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("APLY_SLCT_DSTR_EST_CHC_BK", lit(None).cast(StringType()))
        df_write = df_write.withColumn("APLY_KEY", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"AGMT_IND": "AGMT_IND", "APLY_SLCT_DSTR_EST_CHC_KEY": "APLY_SLCT_DSTR_EST_CHC_KEY", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['APLY_SLCT_DSTR_EST_CHC_KEY', 'APLY_SLCT_DSTR_EST_CHC_BK', 'APLY_KEY', 'AGMT_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PHA_APLY_DSTR_EST", mode="append")

        logger.info("write_SOR_EMS_PHA_APLY_DSTR_EST write completed")
        logger.info("Step: write_SOR_EMS_PHA_APLY_DSTR_EST_STS")
        # Write to Target: write_SOR_EMS_PHA_APLY_DSTR_EST_STS
        df_write = df_sq_4
        # Cast columns to match target schema data types
        if "bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "dstr_chc_key_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dstr_chc_key_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "dstr_chc_key_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dstr_chc_key_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "dstr_chc_key_3" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dstr_chc_key_3":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "est_chc_key_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "est_chc_key_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "est_chc_key_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "est_chc_key_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "est_chc_key_3" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "est_chc_key_3":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "est_chc_key_4" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "est_chc_key_4":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "est_chc_key_5" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "est_chc_key_5":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "flat_type_chc_code_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flat_type_chc_code_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "flat_type_chc_code_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flat_type_chc_code_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "flat_type_chc_code_3" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flat_type_chc_code_3":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_dstr_ref_type_code_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_dstr_ref_type_code_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_dstr_ref_type_code_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_dstr_ref_type_code_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_dstr_ref_type_code_3" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_dstr_ref_type_code_3":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_est_ref_type_code_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_est_ref_type_code_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_est_ref_type_code_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_est_ref_type_code_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_est_ref_type_code_3" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_est_ref_type_code_3":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_est_ref_type_code_4" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_est_ref_type_code_4":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_est_ref_type_code_5" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_est_ref_type_code_5":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ptcl_pplr_hse_est_chc_key_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ptcl_pplr_hse_est_chc_key_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ptcl_pplr_hse_est_chc_key_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ptcl_pplr_hse_est_chc_key_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ptcl_pplr_est_ref_type_code_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ptcl_pplr_est_ref_type_code_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ptcl_pplr_est_ref_type_code_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ptcl_pplr_est_ref_type_code_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("DSTR_CHC_KEY_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("DSTR_CHC_KEY_2", lit(None).cast(StringType()))
        df_write = df_write.withColumn("DSTR_CHC_KEY_3", lit(None).cast(StringType()))
        df_write = df_write.withColumn("EST_CHC_KEY_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("EST_CHC_KEY_2", lit(None).cast(StringType()))
        df_write = df_write.withColumn("EST_CHC_KEY_3", lit(None).cast(StringType()))
        df_write = df_write.withColumn("EST_CHC_KEY_4", lit(None).cast(StringType()))
        df_write = df_write.withColumn("EST_CHC_KEY_5", lit(None).cast(StringType()))
        df_write = df_write.withColumn("LAST_REC_TXN_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("FLAT_TYPE_CHC_CODE_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("FLAT_TYPE_CHC_CODE_2", lit(None).cast(StringType()))
        df_write = df_write.withColumn("FLAT_TYPE_CHC_CODE_3", lit(None).cast(StringType()))
        df_write = df_write.withColumn("IFA_CHC_AREA_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("IFA_CHC_AREA_2", lit(None).cast(StringType()))
        df_write = df_write.withColumn("IFA_CHC_AREA_3", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_DSTR_REF_TYPE_CODE_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_DSTR_REF_TYPE_CODE_2", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_DSTR_REF_TYPE_CODE_3", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_EST_REF_TYPE_CODE_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_EST_REF_TYPE_CODE_2", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_EST_REF_TYPE_CODE_3", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_EST_REF_TYPE_CODE_4", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_EST_REF_TYPE_CODE_5", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PTCL_PPLR_HSE_EST_CHC_KEY_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PTCL_PPLR_HSE_EST_CHC_KEY_2", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PTCL_PPLR_EST_REF_TYPE_CODE_1", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PTCL_PPLR_EST_REF_TYPE_CODE_2", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"APLY_SLCT_DSTR_EST_CHC_KEY": "APLY_SLCT_DSTR_EST_CHC_KEY", "BGN_DATE": "SOR_DATE", "END_DATE": "END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['APLY_SLCT_DSTR_EST_CHC_KEY', 'BGN_DATE', 'END_DATE', 'DSTR_CHC_KEY_1', 'DSTR_CHC_KEY_2', 'DSTR_CHC_KEY_3', 'EST_CHC_KEY_1', 'EST_CHC_KEY_2', 'EST_CHC_KEY_3', 'EST_CHC_KEY_4', 'EST_CHC_KEY_5', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'FLAT_TYPE_CHC_CODE_1', 'FLAT_TYPE_CHC_CODE_2', 'FLAT_TYPE_CHC_CODE_3', 'IFA_CHC_AREA_1', 'IFA_CHC_AREA_2', 'IFA_CHC_AREA_3', 'HSE_DSTR_REF_TYPE_CODE_1', 'HSE_DSTR_REF_TYPE_CODE_2', 'HSE_DSTR_REF_TYPE_CODE_3', 'HSE_EST_REF_TYPE_CODE_1', 'HSE_EST_REF_TYPE_CODE_2', 'HSE_EST_REF_TYPE_CODE_3', 'HSE_EST_REF_TYPE_CODE_4', 'HSE_EST_REF_TYPE_CODE_5', 'PTCL_PPLR_HSE_EST_CHC_KEY_1', 'PTCL_PPLR_HSE_EST_CHC_KEY_2', 'PTCL_PPLR_EST_REF_TYPE_CODE_1', 'PTCL_PPLR_EST_REF_TYPE_CODE_2']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PHA_APLY_DSTR_EST_STS", mode="append")

        logger.info("write_SOR_EMS_PHA_APLY_DSTR_EST_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_PHA_APLY_DSTR_EST_FOR_UPDATE_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()