'''
* Script - M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_RAL_RAES_APLY")
        # Reading Data From Source - read_SSA_EMS_RAL_RAES_APLY
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_RAL_RAES_APLY")
        
        logger.info("Step: read_SSA_EMS_RAL_RAES_APLY_STS")
        # Reading Data From Source - read_SSA_EMS_RAL_RAES_APLY_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_EMS_RAL_RAES_APLY")
        
        logger.info("Step: apply_SQ_SSA_EMS_RAL_RAES_APLY")
        # Source Qualifier: apply_SQ_SSA_EMS_RAL_RAES_APLY
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'E'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("RAES_APLY_KEY", "RAES_APLY_BK", "HSE_SRVC_APLY_NUM", "AGMT_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_EMS_RAL_RAES_APLY_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_RAL_RAES_APLY_STS
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'E' OR OPR_IND = 'EB'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("RAES_APLY_KEY", "BGN_DATE", "END_DATE", "RAES_APLY_STG_CODE", "RAES_APLY_STS_UPD_DATE", "RAES_APLY_STG_UPD_DATE", "RAES_APLY_CRE_DATE", "RAES_APLY_UPD_DATE", "RAES_APLY_ROPT_IND", "RAES_APLY_LIFT_STOP_IND", "RAES_APLY_BLDG_AGE", "RAES_APLY_LVNG_AREA", "RAES_APLY_LDLR_NAME", "RAES_APLY_INTVWR_CODE", "RAES_APLY_INTVW_DATE", "RAES_APLY_SRVC_TEAM_NAME", "RAES_APLY_RMK_TEXT", "APLY_RSN_CODE", "RAES_APLY_CNCL_CODE", "RAES_APLY_CNTC_NAME", "RAES_APLY_CNTC_CHI_NAME", "RAES_APLY_CNTC_PHONE_NUM", "RAES_APLY_CRSP_FLAT_NUM", "RAES_APLY_CRSP_FLR_NUM", "RAES_APLY_CRSP_BLK_NUM", "RAES_APLY_CRSP_BLDG_NAME", "RAES_APLY_CRSP_STRT_NAME", "RAES_APLY_CRSP_DSTR_CODE", "RAES_APLY_CRSP_DSTR_NAME", "RAES_APLY_CRSP_CODE_ADDR", "RAES_APLY_CRSP_WORK_PHONE_NUM", "RAES_ACMD_TYPE_CODE", "QTA_CATG_TYPE_CODE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "RAES_APLY_STS_CODE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_EMS_RAL_RAES_APLY")
        # Write to Target: write_SOR_EMS_RAL_RAES_APLY
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "raes_aply_bk" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_bk":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("RAES_APLY_BK", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_SRVC_APLY_NUM", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"AGMT_IND": "AGMT_IND", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "RAES_APLY_KEY": "RAES_APLY_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['RAES_APLY_KEY', 'RAES_APLY_BK', 'HSE_SRVC_APLY_NUM', 'AGMT_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_RAL_RAES_APLY", mode="append")

        logger.info("write_SOR_EMS_RAL_RAES_APLY write completed")
        logger.info("Step: write_SOR_EMS_RAL_RAES_APLY_STS")
        # Write to Target: write_SOR_EMS_RAL_RAES_APLY_STS
        df_write = df_sq_4
        # Cast columns to match target schema data types
        if "bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "raes_aply_stg_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_stg_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_sts_upd_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_sts_upd_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "raes_aply_stg_upd_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_stg_upd_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "raes_aply_cre_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_cre_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "raes_aply_upd_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_upd_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "raes_aply_ropt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_ropt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_lift_stop_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_lift_stop_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_ldlr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_ldlr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_intvwr_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_intvwr_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_intvw_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_intvw_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "raes_aply_srvc_team_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_srvc_team_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "aply_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "aply_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_cncl_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_cncl_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_cntc_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_cntc_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_cntc_chi_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_cntc_chi_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_cntc_phone_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_cntc_phone_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_flat_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_flat_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_flr_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_flr_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_blk_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_blk_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_bldg_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_bldg_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_strt_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_strt_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_dstr_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_dstr_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_dstr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_dstr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_code_addr" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_code_addr":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_aply_crsp_work_phone_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_crsp_work_phone_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "raes_acmd_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_acmd_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "qta_catg_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "qta_catg_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "raes_aply_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "raes_aply_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("RAES_APLY_STG_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_STS_UPD_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_STG_UPD_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRE_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_UPD_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_ROPT_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_LIFT_STOP_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_BLDG_AGE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_LVNG_AREA", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_LDLR_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_INTVWR_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_INTVW_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_SRVC_TEAM_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_RMK_TEXT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("APLY_RSN_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CNCL_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CNTC_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CNTC_CHI_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CNTC_PHONE_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_FLAT_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_FLR_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_BLK_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_BLDG_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_STRT_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_DSTR_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_DSTR_NAME", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_CODE_ADDR", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_CRSP_WORK_PHONE_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_ACMD_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("QTA_CATG_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("LAST_REC_TXN_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RAES_APLY_STS_CODE", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"BGN_DATE": "SOR_DATE", "END_DATE": "END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "RAES_APLY_KEY": "RAES_APLY_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['RAES_APLY_KEY', 'BGN_DATE', 'END_DATE', 'RAES_APLY_STG_CODE', 'RAES_APLY_STS_UPD_DATE', 'RAES_APLY_STG_UPD_DATE', 'RAES_APLY_CRE_DATE', 'RAES_APLY_UPD_DATE', 'RAES_APLY_ROPT_IND', 'RAES_APLY_LIFT_STOP_IND', 'RAES_APLY_BLDG_AGE', 'RAES_APLY_LVNG_AREA', 'RAES_APLY_LDLR_NAME', 'RAES_APLY_INTVWR_CODE', 'RAES_APLY_INTVW_DATE', 'RAES_APLY_SRVC_TEAM_NAME', 'RAES_APLY_RMK_TEXT', 'APLY_RSN_CODE', 'RAES_APLY_CNCL_CODE', 'RAES_APLY_CNTC_NAME', 'RAES_APLY_CNTC_CHI_NAME', 'RAES_APLY_CNTC_PHONE_NUM', 'RAES_APLY_CRSP_FLAT_NUM', 'RAES_APLY_CRSP_FLR_NUM', 'RAES_APLY_CRSP_BLK_NUM', 'RAES_APLY_CRSP_BLDG_NAME', 'RAES_APLY_CRSP_STRT_NAME', 'RAES_APLY_CRSP_DSTR_CODE', 'RAES_APLY_CRSP_DSTR_NAME', 'RAES_APLY_CRSP_CODE_ADDR', 'RAES_APLY_CRSP_WORK_PHONE_NUM', 'RAES_ACMD_TYPE_CODE', 'QTA_CATG_TYPE_CODE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'RAES_APLY_STS_CODE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_RAL_RAES_APLY_STS", mode="append")

        logger.info("write_SOR_EMS_RAL_RAES_APLY_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_RAL_RAES_APLY_FOR_UPDATE_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()