'''
* Script - M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_RWV_RENT_WVE_REC")
        # Reading Data From Source - read_SSA_EMS_RWV_RENT_WVE_REC
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_RWV_RENT_WVE_REC")
        
        logger.info("Step: read_SSA_EMS_RWV_RENT_WVE_REC_STS")
        # Reading Data From Source - read_SSA_EMS_RWV_RENT_WVE_REC_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_EMS_RWV_RENT_WVE_REC")
        
        logger.info("Step: apply_SQ_SSA_EMS_RWV_RENT_WVE_REC")
        # Source Qualifier: apply_SQ_SSA_EMS_RWV_RENT_WVE_REC
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'B' OR OPR_IND = 'A'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("RENT_WVE_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "TNCY_AGRMT_KEY", "RENT_WVE_EXRC_KEY", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID", "AGMT_IND", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_EMS_RWV_RENT_WVE_REC_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_RWV_RENT_WVE_REC_STS
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'B' OR OPR_IND = 'EB' OR OPR_IND = 'DA'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("RENT_WVE_KEY", "RENT_WVE_TYPE_CODE", "RENT_WVE_START_DATE", "RENT_WVE_END_DATE", "ACTL_RENT_WVE_BGN_DATE", "RENT_WVE_CRE_USER_ID", "RENT_WVE_CRE_DATE", "RENT_WVE_BGN_TYPE_CODE", "RENT_WVE_EXRC_STS_CODE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID", "RENT_WVE_AMT", "RENT_WVE_EXRC_DATE", "RENT_WVE_ITEM_RMK_TEXT", "RENT_WVE_CNCL_USER_ID", "RENT_WVE_CNCL_BSNS_DATE", "BGN_DATE", "END_DATE", "OPR_IND", "SOR_DATE", "CR_JRNL_CRE_DATE", "CR_JRNL_CRE_IND", "DR_JRNL_CRE_DATE", "DR_JRNL_CRE_IND")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_EMS_RWV_RENT_WVE_REC")
        # Write to Target: write_SOR_EMS_RWV_RENT_WVE_REC
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_wve_exrc_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_wve_exrc_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"AGMT_IND": "AGMT_IND", "CUST_KEY": "CUST_KEY", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "RENT_WVE_EXRC_KEY": "RENT_WVE_EXRC_KEY", "RENT_WVE_KEY": "RENT_WVE_KEY", "TNCY_AGRMT_KEY": "TNCY_AGRMT_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['RENT_WVE_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'TNCY_AGRMT_KEY', 'RENT_WVE_EXRC_KEY', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'AGMT_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_RWV_RENT_WVE_REC", mode="append")

        logger.info("write_SOR_EMS_RWV_RENT_WVE_REC write completed")
        logger.info("Step: apply_EXP_SOR_LOAD_DATE")
        # Expression: apply_EXP_SOR_LOAD_DATE
        df_exp_5 = df_sq_4
        df_exp_5 = df_exp_5.withColumn("OUT_END_DATE", expr("CASE WHEN OPR_IND = 'EB' THEN CASE WHEN LAST_REC_TXN_TYPE_CODE = NULL THEN to_date('99991231','yyyyMMdd') ELSE END_DATE END ELSE END_DATE END"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        # Select only mapping output ports (prevents column leakage)
        df_exp_5 = df_exp_5.select("OUT_END_DATE")
        ctx.register_df("df_exp_5", df_exp_5)
        
        logger.info("Step: join_target_SOR_EMS_RWV_RENT_WVE_REC_STS_0")
        # Lookup: join_target_SOR_EMS_RWV_RENT_WVE_REC_STS_0
        # Merge parallel DataFrames on their common columns
        _common_cols = [c for c in df_sq_4.columns if c in df_exp_5.columns]
        df_tgt_merge_6 = df_sq_4.join(
            df_exp_5,
            on=_common_cols,
            how="left"
        )
        ctx.register_df("df_tgt_merge_6", df_tgt_merge_6)
        
        logger.info("Step: write_SOR_EMS_RWV_RENT_WVE_REC_STS")
        # Write to Target: write_SOR_EMS_RWV_RENT_WVE_REC_STS
        df_write = df_tgt_merge_6
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_wve_exrc_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_wve_exrc_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("CUST_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_SRVC_APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("TNCY_AGRMT_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("RENT_WVE_EXRC_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("AGMT_IND", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"ACTL_RENT_WVE_BGN_DATE": "ACTL_RENT_WVE_BGN_DATE", "BGN_DATE": "BGN_DATE", "CR_JRNL_CRE_DATE": "CR_JRNL_CRE_DATE", "CR_JRNL_CRE_IND": "CR_JRNL_CRE_IND", "DR_JRNL_CRE_DATE": "DR_JRNL_CRE_DATE", "DR_JRNL_CRE_IND": "DR_JRNL_CRE_IND", "END_DATE": "OUT_END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "RENT_WVE_AMT": "RENT_WVE_AMT", "RENT_WVE_BGN_DATE": "RENT_WVE_START_DATE", "RENT_WVE_BGN_TYPE_CODE": "RENT_WVE_BGN_TYPE_CODE", "RENT_WVE_CNCL_BSNS_DATE": "RENT_WVE_CNCL_BSNS_DATE", "RENT_WVE_CNCL_USER_ID": "RENT_WVE_CNCL_USER_ID", "RENT_WVE_CRE_DATE": "RENT_WVE_CRE_DATE", "RENT_WVE_CRE_USER_ID": "RENT_WVE_CRE_USER_ID", "RENT_WVE_END_DATE": "RENT_WVE_END_DATE", "RENT_WVE_EXCT_DATE": "RENT_WVE_EXRC_DATE", "RENT_WVE_EXRC_STS_CODE": "RENT_WVE_EXRC_STS_CODE", "RENT_WVE_ITEM_RMK_TEXT": "RENT_WVE_ITEM_RMK_TEXT", "RENT_WVE_KEY": "RENT_WVE_KEY", "RENT_WVE_TYPE_CODE": "RENT_WVE_TYPE_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['RENT_WVE_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'TNCY_AGRMT_KEY', 'RENT_WVE_EXRC_KEY', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'AGMT_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_RWV_RENT_WVE_REC", mode="append")

        logger.info("write_SOR_EMS_RWV_RENT_WVE_REC_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SOR_LOAD_RWV_RENT_WVE_FOR_INSERT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()