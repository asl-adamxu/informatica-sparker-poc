'''
* Script - M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_PAL_PRH_INTK_SCHD")
        # Reading Data From Source - read_SSA_EMS_PAL_PRH_INTK_SCHD
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_PAL_PRH_INTK_SCHD")
        
        logger.info("Step: read_SSA_EMS_PAL_PRH_INTK_SCHD_STS")
        # Reading Data From Source - read_SSA_EMS_PAL_PRH_INTK_SCHD_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_EMS_PAL_PRH_INTK_SCHD")
        
        logger.info("Step: apply_SQ_SSA_EMS_PAL_PRH_INTK_SCHD")
        # Source Qualifier: apply_SQ_SSA_EMS_PAL_PRH_INTK_SCHD
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'E'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("PRH_INTK_SCHD_KEY", "CUST_APLY_KEY", "INTL_INTK_DATE", "ALCT_UNIT_KEY", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "AGMT_IND", "OPR_IND", "SOR_DATE", "CUST_KEY", "HSE_SRVC_APLY_KEY")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_EMS_PAL_PRH_INTK_SCHD_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_PAL_PRH_INTK_SCHD_STS
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'E' OR OPR_IND = 'EB'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("PRH_INTK_SCHD_KEY", "BGN_DATE", "CUST_INTK_DATE", "TNT_SPCL_NEED_IND", "DPST_AMT", "FRST_RENT_AMT", "INTK_SCHD_INPT_USER_ID", "INTK_SCHD_INPT_DATE", "INTK_SCHD_TYPE_CODE", "INTK_SCHD_STS_CODE", "ALCT_HSE_UNIT_CODE_ADDR", "INTK_RVN_TXN_NUM", "LAST_INTK_SCHD_UPD_DATE", "INTK_SCHD_CATG_CODE", "TNT_RENT_CODE", "INTK_HSE_EST_KEY", "TNCY_AGRMT_CMNC_DATE", "INTL_INTK_TIME", "ORIG_CUST_KEY", "ORIG_HSE_SRVC_APLY_KEY", "PND_TNT_RENT_CODE", "PND_TNT_RENT_CODE_BGN_DATE", "DPST_RCPT_NUM", "DPST_RCPT_TYPE_CODE", "EST_KEY", "IH_INTK_DATE", "INTK_CLCT_TXN_NUM", "INTK_RENT_AMT", "INTK_SCHD_RMK_TEXT", "INTK_TYPE_CODE", "NEXT_RENT_BGN_DATE", "NEXT_RENT_FCTR_KEY", "OFR_DATE", "OFR_LTR_INTK_DATE", "OFR_LTR_ISS_DATE", "OFR_SEQ_NUM", "PREV_CUST_APLY_KEY", "PRH_APLY_NUM", "RENT_BGN_DATE", "RENT_FCTR_KEY", "RENT_FREE_INTL_BGN_DATE", "RENT_FREE_PHASE_CNT", "RENT_FREE_PRD_CODE", "RENT_FREE_TYPE_CODE", "RENT_FREE_TFR_DBR_END_DATE", "RENT_RVW_CATG_BGN_DATE", "RENT_RVW_CATG_KEY", "RVS_NEXT_RENT_BGN_DATE", "RVS_NEXT_RENT_FCTR_KEY", "RVS_RENT_BGN_DATE", "RVS_RENT_FCTR_KEY", "RVS_RENT_RVW_CATG_BGN_DATE", "RVS_RENT_RVW_CATG_KEY", "FWD_FROM_PREV_RENT_IND", "INTL_DOC_DATE", "RVS_DOC_DATE", "RVS_FWD_FROM_PREV_RENT_IND", "RVS_INTK_DATE", "END_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_EMS_PAL_PRH_INTK_SCHD")
        # Write to Target: write_SOR_EMS_PAL_PRH_INTK_SCHD
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "intl_intk_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "intl_intk_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("CUST_APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("CUST_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_SRVC_APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("INTL_INTK_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("ALCT_UNIT_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("AGMT_IND", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "PRH_INTK_SCHD_KEY": "PRH_INTK_SCHD_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['PRH_INTK_SCHD_KEY', 'CUST_APLY_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'INTL_INTK_DATE', 'ALCT_UNIT_KEY', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'AGMT_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PAL_PRH_INTK_SCHD", mode="append")

        logger.info("write_SOR_EMS_PAL_PRH_INTK_SCHD write completed")
        logger.info("Step: write_SOR_EMS_PAL_PRH_INTK_SCHD_STS")
        # Write to Target: write_SOR_EMS_PAL_PRH_INTK_SCHD_STS
        df_write = df_sq_4
        # Cast columns to match target schema data types
        if "intl_intk_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "intl_intk_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("CUST_APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("CUST_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("HSE_SRVC_APLY_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("INTL_INTK_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("ALCT_UNIT_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("AGMT_IND", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"BGN_DATE": "SOR_DATE", "END_DATE": "END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "PRH_INTK_SCHD_KEY": "PRH_INTK_SCHD_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['PRH_INTK_SCHD_KEY', 'CUST_APLY_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'INTL_INTK_DATE', 'ALCT_UNIT_KEY', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'AGMT_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PAL_PRH_INTK_SCHD", mode="append")

        logger.info("write_SOR_EMS_PAL_PRH_INTK_SCHD_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SOR_LOAD_PAL_PRH_INTK_SCHD_FOR_UPDATE_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()