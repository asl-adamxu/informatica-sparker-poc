'''
* Script - M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_HSM_UNIT")
        # Reading Data From Source - read_SSA_HSM_UNIT
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_SSA_HSM_UNIT = lib.read_sql(spark, _conn, table="SSA_HSM_UNIT")
        
        logger.info("Step: apply_SQ_SSA_HSM_UNIT")
        # Source Qualifier: apply_SQ_SSA_HSM_UNIT
        df_SQ_SSA_HSM_UNIT = df_SSA_HSM_UNIT
        df_SQ_SSA_HSM_UNIT = lib.sq_output(
            input_df=df_SQ_SSA_HSM_UNIT,
            port_cols={
                'UNIT_ADDR_CODE': 'string',
                'UNIT_CATG_CODE': 'string',
                'AGMT_IND': 'string',
                'UNIT_TYPE_CODE': 'string',
                'LAST_REC_TXN_TYPE_CODE': 'string',
                'UNIT_NUM': 'string',
                'LAST_REC_TXN_DATE': 'date/time',
                'UNIT_CNV_REF_IND': 'string',
                'UNIT_SHRD_IND': 'string',
                'UNIT_IFA_AREA': 'decimal',
                'UNIT_LVNG_AREA': 'double',
                'UNIT_TAKE_OVER_DATE': 'date/time',
                'UNIT_GFA_AREA': 'decimal',
                'UNIT_SFA_AREA': 'decimal',
                'UNIT_BWA_AREA': 'decimal',
                'BLK_KEY': 'decimal',
                'UNIT_KEY': 'decimal',
                'UNIT_FLR_NUM': 'double',
                'OPR_IND': 'string',
                'SOR_DATE': 'date/time',
                'EMMS_UNIT_KEY': 'string',
                'BLK_BK_1': 'string',
            },
            filter_condition="OPR_IND = 'B' OR OPR_IND = 'A'",
        )
        ctx.register_df("df_SQ_SSA_HSM_UNIT", df_SQ_SSA_HSM_UNIT)
        
        logger.info("Step: write_SOR_HSM_UNIT")
        # Write to Target: write_SOR_HSM_UNIT
        lib.write_target(
            spark=spark,
            df=df_SQ_SSA_HSM_UNIT,
            conn=conn_target,
            table='SOR_HSM_UNIT',
            mode='append',
            source_columns=[
                'UNIT_ADDR_CODE',
                'UNIT_CATG_CODE',
                'UNIT_TYPE_CODE',
                'UNIT_NUM',
                'UNIT_CNV_REF_IND',
                'UNIT_SHRD_IND',
                'UNIT_IFA_AREA',
                'UNIT_LVNG_AREA',
                'UNIT_TAKE_OVER_DATE',
                'UNIT_GFA_AREA',
                'UNIT_SFA_AREA',
                'UNIT_BWA_AREA',
                'BLK_KEY',
                'UNIT_KEY',
                'UNIT_FLR_NUM',
                'AGMT_IND',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'EMMS_UNIT_KEY',
            ],
            target_columns=[
                'UNIT_ADDR_CODE',
                'UNIT_CATG_CODE',
                'UNIT_TYPE_CODE',
                'UNIT_NUM',
                'UNIT_CNV_REF_IND',
                'UNIT_SHRD_IND',
                'UNIT_IFA_AREA',
                'UNIT_LVNG_AREA',
                'UNIT_TAKE_OVER_DATE',
                'UNIT_GFA_AREA',
                'UNIT_SFA_AREA',
                'UNIT_BWA_AREA',
                'BLK_KEY',
                'UNIT_KEY',
                'UNIT_FLR_NUM',
                'AGMT_IND',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'EMMS_UNIT_KEY',
            ],
            config=config,
        )

        logger.info("write_SOR_HSM_UNIT write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_HSM_UNIT_FOR_INSERT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())