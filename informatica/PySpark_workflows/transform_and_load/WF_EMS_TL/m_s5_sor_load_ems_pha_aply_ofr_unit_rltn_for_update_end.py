'''
* Script - M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN")
        # Reading Data From Source - read_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN = lib.read_sql(spark, _conn, table="SSA_EMS_PHA_APLY_OFR")
        
        logger.info("Step: apply_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN")
        # Source Qualifier: apply_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN
        df_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN = df_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN
        df_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN = lib.sq_output(
            input_df=df_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN,
            port_cols={
                'OFR_KEY': 'decimal',
                'UNIT_KEY': 'decimal',
                'ADTN_ROOM_IND': 'string',
                'BGN_DATE': 'date/time',
                'END_DATE': 'date/time',
                'LAST_REC_TXN_TYPE_CODE': 'string',
                'LAST_REC_TXN_DATE': 'date/time',
                'OPR_IND': 'string',
                'SOR_DATE': 'date/time',
            },
            filter_condition="OPR_IND = 'E' OR OPR_IND = 'EB'",
        )
        ctx.register_df("df_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN", df_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN)
        
        logger.info("Step: write_SOR_EMS_PHA_APLY_OFR_UNIT_RLTN")
        # Write to Target: write_SOR_EMS_PHA_APLY_OFR_UNIT_RLTN
        lib.write_target(
            spark=spark,
            df=df_SQ_SSA_EMS_PHA_APLY_OFR_UNIT_RLTN,
            conn=conn_target,
            table='SOR_EMS_PHA_APLY_OFR_UNIT_RLTN',
            mode='append',
            source_columns=[
                'OFR_KEY',
                'UNIT_KEY',
                'ADTN_ROOM_IND',
                'SOR_DATE',
                'END_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
            ],
            target_columns=[
                'OFR_KEY',
                'UNIT_KEY',
                'ADTN_ROOM_IND',
                'BGN_DATE',
                'END_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
            ],
            config=config,
        )

        logger.info("write_SOR_EMS_PHA_APLY_OFR_UNIT_RLTN write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_PHA_APLY_OFR_UNIT_RLTN_FOR_UPDATE_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())