'''
* Script - M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_RWV_LONG_VCNCY_REC")
        # Reading Data From Source - read_SSA_EMS_RWV_LONG_VCNCY_REC
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_SSA_EMS_RWV_LONG_VCNCY_REC = lib.read_sql(spark, _conn, table="SSA_EMS_RWV_LONG_VCNCY_REC")
        
        logger.info("Step: apply_SQ_SSA_EMS_RWV_LONG_VCNCY_REC")
        # Source Qualifier: apply_SQ_SSA_EMS_RWV_LONG_VCNCY_REC
        df_SQ_SSA_EMS_RWV_LONG_VCNCY_REC = df_SSA_EMS_RWV_LONG_VCNCY_REC
        df_SQ_SSA_EMS_RWV_LONG_VCNCY_REC = lib.sq_output(
            input_df=df_SQ_SSA_EMS_RWV_LONG_VCNCY_REC,
            port_cols={
                'LONG_VCNCY_RENT_FREE_KEY': 'decimal',
                'LONG_VCNCY_RENT_FREE_BK': 'string',
                'RENT_FREE_EXRC_KEY': 'string',
                'CUST_KEY': 'string',
                'HSE_SRVC_APLY_KEY': 'string',
                'TNCY_AGRMT_KEY': 'decimal',
                'UNIT_KEY': 'string',
                'LAST_REC_TXN_TYPE_CODE': 'string',
                'LAST_REC_TXN_DATE': 'date/time',
                'LAST_REC_TXN_USER_ID': 'string',
                'AGMT_IND': 'string',
                'OPR_IND': 'string',
                'SOR_DATE': 'date/time',
            },
            filter_condition="OPR_IND = 'EB' OR OPR_IND = 'DA'",
        )
        ctx.register_df("df_SQ_SSA_EMS_RWV_LONG_VCNCY_REC", df_SQ_SSA_EMS_RWV_LONG_VCNCY_REC)
        
        logger.info("Step: write_SOR_EMS_RWV_LONG_VCNCY_REC")
        # Write to Target: write_SOR_EMS_RWV_LONG_VCNCY_REC
        lib.write_target(
            spark=spark,
            df=df_SQ_SSA_EMS_RWV_LONG_VCNCY_REC,
            conn=conn_target,
            table='SOR_EMS_RWV_LONG_VCNCY_REC',
            mode='append',
            source_columns=[
                'LONG_VCNCY_RENT_FREE_KEY',
                'LONG_VCNCY_RENT_FREE_BK',
                'RENT_FREE_EXRC_KEY',
                'CUST_KEY',
                'HSE_SRVC_APLY_KEY',
                'TNCY_AGRMT_KEY',
                'UNIT_KEY',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'AGMT_IND',
            ],
            target_columns=[
                'LONG_VCNCY_RENT_FREE_KEY',
                'LONG_VCNCY_RENT_FREE_BK',
                'RENT_FREE_EXRC_KEY',
                'CUST_KEY',
                'HSE_SRVC_APLY_KEY',
                'TNCY_AGRMT_KEY',
                'HSE_UNIT_KEY',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'AGMT_IND',
            ],
            config=config,
        )

        logger.info("write_SOR_EMS_RWV_LONG_VCNCY_REC write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SOR_LOAD_RWV_LONG_VCNCY_FOR_UPDATE")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())