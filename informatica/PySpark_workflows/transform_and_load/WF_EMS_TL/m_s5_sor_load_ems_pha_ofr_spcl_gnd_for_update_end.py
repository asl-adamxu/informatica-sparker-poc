'''
* Script - M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SSA")
    conn_target = lib.get_db_config(config, "SSA")

    
    try:
        logger.info("Step: read_SSA_EMS_PHA_OFR_SPCL_GND")
        # Reading Data From Source - read_SSA_EMS_PHA_OFR_SPCL_GND
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_1 = lib.read_sql(spark, _conn, table="SSA_EMS_PHA_OFR_SPCL_GND")
        
        logger.info("Step: read_SSA_EMS_PHA_OFR_SPCL_GND_STS")
        # Reading Data From Source - read_SSA_EMS_PHA_OFR_SPCL_GND_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SSA")
        df_src_2 = lib.read_sql(spark, _conn, table="SSA_EMS_PHA_OFR_SPCL_GND_STS")
        
        logger.info("Step: apply_SQ_SSA_EMS_PHA_OFR_SPCL_GND")
        # Source Qualifier: apply_SQ_SSA_EMS_PHA_OFR_SPCL_GND
        df_sq_3 = df_src_1
        df_sq_3 = df_sq_3.filter(expr("OPR_IND = 'E'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_3 = df_sq_3.select("OFR_SPCL_GND_KEY", "OFR_SPCL_GND_BK", "BTCH_ALCT_KEY", "OFR_DATE", "AGMT_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_3", df_sq_3)
        
        logger.info("Step: apply_SQ_SSA_EMS_PHA_OFR_SPCL_GND_STS")
        # Source Qualifier: apply_SQ_SSA_EMS_PHA_OFR_SPCL_GND_STS
        df_sq_4 = df_src_2
        df_sq_4 = df_sq_4.filter(expr("OPR_IND = 'E' OR OPR_IND = 'EB'"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_4 = df_sq_4.select("OFR_SPCL_GND_KEY", "BGN_DATE", "END_DATE", "OFR_SPCL_GND_RSN_CODE", "OFR_SPCL_GND_RSN_TEXT", "DSTR_CODE_LIST", "SBDSTR_CODE_LIST", "EXCLD_EST_CODE", "DSGN_EST_CODE", "BLK_CODE", "UNIT_TYPE_CODE", "PRH_UNIT_HSC_TYPE_CODE", "UNIT_FROM_FLR_NUM", "UNIT_TO_FLR_NUM", "UNIT_FROM_IFA_AREA", "UNIT_TO_IFA_AREA", "PRH_UNIT_FROM_RFSL_NUM", "PRH_UNIT_TO_RFSL_NUM", "UNIT_VOID_PRD_DAY_CNT", "PRH_UNIT_TAKE_OVER_IND", "UNIT_CSL_VCNCY_IND", "UNIT_LIFT_STOP_IND", "UNIT_TLT_TYPE_CODE", "INCLD_ALL_ENV_CODE_LIST", "INCLD_ANY_ENV_CODE_LIST", "EXCLD_ENV_CODE_LIST", "PAST_THREE_MTH_NO_RFSL_IND", "VLT_ALCT_STD_IND", "ALCT_NEW_FLAT_TYPE_IND", "ALCT_ALL_FLAT_TYPE_IND", "APLY_ALCT_ORD_NUM", "FLAT_SLCT_ORD_NUM", "SAME_EST_AS_PREV_OFR_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "OPR_IND", "SOR_DATE")
        ctx.register_df("df_sq_4", df_sq_4)
        
        logger.info("Step: write_SOR_EMS_PHA_OFR_SPCL_GND")
        # Write to Target: write_SOR_EMS_PHA_OFR_SPCL_GND
        df_write = df_sq_3
        # Cast columns to match target schema data types
        if "ofr_spcl_gnd_bk" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_bk":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofr_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "agmt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "agmt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "ofr_spcl_gnd_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("OFR_SPCL_GND_BK", lit(None).cast(StringType()))
        df_write = df_write.withColumn("BTCH_ALCT_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("OFR_DATE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("OFR_SPCL_GND_TYPE_CODE", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"AGMT_IND": "AGMT_IND", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "OFR_SPCL_GND_KEY": "OFR_SPCL_GND_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['OFR_SPCL_GND_KEY', 'OFR_SPCL_GND_BK', 'BTCH_ALCT_KEY', 'OFR_DATE', 'AGMT_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'OFR_SPCL_GND_TYPE_CODE']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PHA_OFR_SPCL_GND", mode="append")

        logger.info("write_SOR_EMS_PHA_OFR_SPCL_GND write completed")
        logger.info("Step: write_SOR_EMS_PHA_OFR_SPCL_GND_STS")
        # Write to Target: write_SOR_EMS_PHA_OFR_SPCL_GND_STS
        df_write = df_sq_4
        # Cast columns to match target schema data types
        if "bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "ofr_spcl_gnd_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofr_spcl_gnd_rsn_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_rsn_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "dstr_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dstr_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "sbdstr_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "sbdstr_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "excld_est_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excld_est_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "dsgn_est_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dsgn_est_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "blk_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "blk_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "prh_unit_hsc_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "prh_unit_hsc_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_from_flr_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_from_flr_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_to_flr_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_to_flr_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "prh_unit_take_over_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "prh_unit_take_over_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_csl_vcncy_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_csl_vcncy_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_lift_stop_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_lift_stop_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "unit_tlt_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "unit_tlt_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incld_all_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incld_all_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incld_any_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incld_any_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "excld_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excld_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "past_three_mth_no_rfsl_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "past_three_mth_no_rfsl_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "vlt_alct_std_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "vlt_alct_std_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "alct_new_flat_type_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "alct_new_flat_type_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "alct_all_flat_type_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "alct_all_flat_type_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "aply_alct_ord_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "aply_alct_ord_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "flat_slct_ord_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flat_slct_ord_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "same_est_as_prev_ofr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "same_est_as_prev_ofr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "vlt_loc_rstr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "vlt_loc_rstr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("OFR_SPCL_GND_RSN_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("OFR_SPCL_GND_RSN_TEXT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("DSTR_CODE_LIST", lit(None).cast(StringType()))
        df_write = df_write.withColumn("SBDSTR_CODE_LIST", lit(None).cast(StringType()))
        df_write = df_write.withColumn("EXCLD_EST_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("DSGN_EST_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("BLK_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PRH_UNIT_HSC_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_FROM_FLR_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_TO_FLR_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_FROM_IFA_AREA", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_TO_IFA_AREA", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PRH_UNIT_FROM_RFSL_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PRH_UNIT_TO_RFSL_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_VOID_PRD_DAY_CNT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PRH_UNIT_TAKE_OVER_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_CSL_VCNCY_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_LIFT_STOP_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("UNIT_TLT_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("INCLD_ALL_ENV_CODE_LIST", lit(None).cast(StringType()))
        df_write = df_write.withColumn("INCLD_ANY_ENV_CODE_LIST", lit(None).cast(StringType()))
        df_write = df_write.withColumn("EXCLD_ENV_CODE_LIST", lit(None).cast(StringType()))
        df_write = df_write.withColumn("PAST_THREE_MTH_NO_RFSL_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("VLT_ALCT_STD_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("ALCT_NEW_FLAT_TYPE_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("ALCT_ALL_FLAT_TYPE_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("APLY_ALCT_ORD_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("FLAT_SLCT_ORD_NUM", lit(None).cast(StringType()))
        df_write = df_write.withColumn("SAME_EST_AS_PREV_OFR_IND", lit(None).cast(StringType()))
        df_write = df_write.withColumn("LAST_REC_TXN_TYPE_CODE", lit(None).cast(StringType()))
        df_write = df_write.withColumn("VLT_LOC_RSTR_IND", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"BGN_DATE": "SOR_DATE", "END_DATE": "END_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "OFR_SPCL_GND_KEY": "OFR_SPCL_GND_KEY"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['OFR_SPCL_GND_KEY', 'BGN_DATE', 'END_DATE', 'OFR_SPCL_GND_RSN_CODE', 'OFR_SPCL_GND_RSN_TEXT', 'DSTR_CODE_LIST', 'SBDSTR_CODE_LIST', 'EXCLD_EST_CODE', 'DSGN_EST_CODE', 'BLK_CODE', 'UNIT_TYPE_CODE', 'PRH_UNIT_HSC_TYPE_CODE', 'UNIT_FROM_FLR_NUM', 'UNIT_TO_FLR_NUM', 'UNIT_FROM_IFA_AREA', 'UNIT_TO_IFA_AREA', 'PRH_UNIT_FROM_RFSL_NUM', 'PRH_UNIT_TO_RFSL_NUM', 'UNIT_VOID_PRD_DAY_CNT', 'PRH_UNIT_TAKE_OVER_IND', 'UNIT_CSL_VCNCY_IND', 'UNIT_LIFT_STOP_IND', 'UNIT_TLT_TYPE_CODE', 'INCLD_ALL_ENV_CODE_LIST', 'INCLD_ANY_ENV_CODE_LIST', 'EXCLD_ENV_CODE_LIST', 'PAST_THREE_MTH_NO_RFSL_IND', 'VLT_ALCT_STD_IND', 'ALCT_NEW_FLAT_TYPE_IND', 'ALCT_ALL_FLAT_TYPE_IND', 'APLY_ALCT_ORD_NUM', 'FLAT_SLCT_ORD_NUM', 'SAME_EST_AS_PREV_OFR_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'VLT_LOC_RSTR_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "SOR_EMS_PHA_OFR_SPCL_GND_STS", mode="append")

        logger.info("write_SOR_EMS_PHA_OFR_SPCL_GND_STS write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SOR_LOAD_EMS_PHA_OFR_SPCL_GND_FOR_UPDATE_END")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()