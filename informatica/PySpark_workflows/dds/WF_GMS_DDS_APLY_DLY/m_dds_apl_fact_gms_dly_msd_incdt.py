'''
* Script - M_DDS_APL_FACT_GMS_DLY_MSD_INCDT.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_DDS_APL_FACT_GMS_DLY_MSD_INCDT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DDS_APL_FACT_GMS_DLY_MSD_INCDT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DDS_APL_FACT_GMS_DLY_MSD_INCDT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "DPA")

    v_snsh_date = ""
    # Load mapping variables from the UTL_JOB_PARAM file (shared helper)
    _vars = lib.load_mapping_variables(config, [ "$$v_snsh_date", ], logger)
    v_snsh_date = _vars.get("v_snsh_date", v_snsh_date)
    
    try:
        logger.info("Step: read_DPA_FACT_GMS_DLY_MSD_INCDT")
        # Reading Data From Source - read_DPA_FACT_GMS_DLY_MSD_INCDT
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_DPA_FACT_GMS_DLY_MSD_INCDT = lib.read_sql(spark, _conn, table="DPA_FACT_GMS_DLY_MSD_INCDT")
        
        logger.info("Step: apply_SQ_DPA_FACT_GMS_DLY_MSD_INCDT")
        # Source Qualifier: apply_SQ_DPA_FACT_GMS_DLY_MSD_INCDT
        df_SQ_DPA_FACT_GMS_DLY_MSD_INCDT = df_DPA_FACT_GMS_DLY_MSD_INCDT
        df_SQ_DPA_FACT_GMS_DLY_MSD_INCDT = lib.sq_output(
            input_df=df_SQ_DPA_FACT_GMS_DLY_MSD_INCDT,
            port_cols={
                'MSD_INCDT_DATE_DMNS_KEY': 'decimal',
                'MSD_CRE_DATE_DMNS_KEY': 'decimal',
                'EST_SCD_KEY': 'decimal',
                'OFCR_TYPE_DMNS_KEY': 'decimal',
                'HSHLD_SIZE_DMNS_KEY': 'decimal',
                'MSD_CODE_SCD_KEY': 'decimal',
                'OFNDR_GNDR_DMNS_KEY': 'decimal',
                'OFNDR_AGE_GRP_DMNS_KEY': 'decimal',
                'OFNC_SCORE_GRP_DMNS_KEY': 'decimal',
                'AFT_CMLT_WRT_WARN_CASE_CNT': 'decimal',
                'CMLT_PNT_ALLT_CASE_CNT': 'decimal',
                'CMLT_MSD_TOT_CASE_CNT': 'decimal',
                'REC_RLS_IND': 'string',
                'LAST_REC_TXN_DATE': 'date/time',
                'LAST_REC_TXN_TYPE_CODE': 'string',
            },
        )
        ctx.register_df("df_SQ_DPA_FACT_GMS_DLY_MSD_INCDT", df_SQ_DPA_FACT_GMS_DLY_MSD_INCDT)
        
        logger.info("Step: apply_UPDTRANS")
        # Update Strategy: apply_UPDTRANS
        # Strategy: DD_INSERT
        df_UPDTRANS = lib.update_strategy(
            input_df=df_SQ_DPA_FACT_GMS_DLY_MSD_INCDT,
        )
        ctx.register_df("df_UPDTRANS", df_UPDTRANS)
        
        logger.info("Step: write_DDS_FACT_GMS_DLY_MSD_INCDT")
        # Write to Target: write_DDS_FACT_GMS_DLY_MSD_INCDT
        lib.write_target(
            spark=spark,
            df=df_UPDTRANS,
            conn=conn_target,
            table='DDS_FACT_GMS_DLY_MSD_INCDT',
            mode='append',
            source_columns=[
                'MSD_INCDT_DATE_DMNS_KEY',
                'MSD_CRE_DATE_DMNS_KEY',
                'EST_SCD_KEY',
                'OFCR_TYPE_DMNS_KEY',
                'HSHLD_SIZE_DMNS_KEY',
                'MSD_CODE_SCD_KEY',
                'OFNDR_GNDR_DMNS_KEY',
                'OFNDR_AGE_GRP_DMNS_KEY',
                'OFNC_SCORE_GRP_DMNS_KEY',
                'AFT_CMLT_WRT_WARN_CASE_CNT',
                'CMLT_PNT_ALLT_CASE_CNT',
                'CMLT_MSD_TOT_CASE_CNT',
                'REC_RLS_IND',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_TYPE_CODE',
            ],
            target_columns=[
                'MSD_INCDT_DATE_DMNS_KEY',
                'MSD_CRE_DATE_DMNS_KEY',
                'EST_SCD_KEY',
                'OFCR_TYPE_DMNS_KEY',
                'HSHLD_SIZE_DMNS_KEY',
                'MSD_CODE_SCD_KEY',
                'OFNDR_GNDR_DMNS_KEY',
                'OFNDR_AGE_GRP_DMNS_KEY',
                'OFNC_SCORE_GRP_DMNS_KEY',
                'AFT_CMLT_WRT_WARN_CASE_CNT',
                'CMLT_PNT_ALLT_CASE_CNT',
                'CMLT_MSD_TOT_CASE_CNT',
                'REC_RLS_IND',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_TYPE_CODE',
            ],
            config=config,
        )

        logger.info("write_DDS_FACT_GMS_DLY_MSD_INCDT write completed")
        
        metrics.complete()
        logger.info("Mapping M_DDS_APL_FACT_GMS_DLY_MSD_INCDT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DDS_APL_FACT_GMS_DLY_MSD_INCDT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DDS_APL_FACT_GMS_DLY_MSD_INCDT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DDS_APL_FACT_GMS_DLY_MSD_INCDT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())