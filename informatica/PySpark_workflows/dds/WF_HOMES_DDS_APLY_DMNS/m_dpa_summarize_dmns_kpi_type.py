'''
* Script - M_DPA_SUMMARIZE_DMNS_KPI_TYPE.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_DPA_SUMMARIZE_DMNS_KPI_TYPE mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DPA_SUMMARIZE_DMNS_KPI_TYPE")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_KPI_TYPE", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()


    v_snsh_date = ""
    # Load mapping variables from the UTL_JOB_PARAM file (shared helper)
    _vars = lib.load_mapping_variables(config, [ "$$v_snsh_date", ], logger)
    v_snsh_date = _vars.get("v_snsh_date", v_snsh_date)
    
    try:
        logger.info("Step: read_SOR_HOM_SMT_KPI_INPT_MSR")
        # Reading Data From Source - read_SOR_HOM_SMT_KPI_INPT_MSR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SOR")
        df_SOR_HOM_SMT_KPI_INPT_MSR = lib.read_sql(spark, _conn, table="SOR_HOM_SMT_KPI_INPT_MSR")
        
        logger.info("Step: read_SOR_HOM_SMT_KPI_INPT_MSR_STS")
        # Reading Data From Source - read_SOR_HOM_SMT_KPI_INPT_MSR_STS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SOR")
        df_SOR_HOM_SMT_KPI_INPT_MSR_STS = lib.read_sql(spark, _conn, table="SOR_HOM_SMT_KPI_INPT_MSR_STS")
        
        logger.info("Step: apply_SQ_SOR_HOM_SMT_KPI_TYPE_CODE")
        # Source Qualifier: apply_SQ_SOR_HOM_SMT_KPI_TYPE_CODE
        # SQL Pushdown - executes Informatica SQ SQL on source database
        _conn = lib.get_db_config(config, "SOR")
        # Parameterize schema from connection config
        _schema = _conn.get("schema", "") or "psor"
        query = f"""select distinct c.kpi_type_code, cc.kpi_type_desp,
       t.kpi_type_tac, tt.kpi_type_trgt_actl_desp
  from SOR_HOM_SMT_KPI_INPT_MSR m, SOR_HOM_SMT_KPI_INPT_MSR_STS mm, 
       SOR_HOM_SMT_KPI_TYPE_CODE c, SOR_HOM_SMT_KPI_TYPE_CODE_STS cc,
       SOR_HOM_SMT_KPI_TYPE_TAC t, SOR_HOM_SMT_KPI_TYPE_TAC_STS tt
 where m.last_rec_txn_type_code is null
   and mm.last_rec_txn_type_code is null
   and c.last_rec_txn_type_code is null
   and cc.last_rec_txn_type_code is null
   and t.last_rec_txn_type_code is null
   and tt.last_rec_txn_type_code is null
   and m.KPI_INPT_MSR_KEY = mm.KPI_INPT_MSR_KEY
   and c.kpi_type_key = cc.kpi_type_key
   and t.KPI_TYPE_TRGT_ACTL_KEY = tt.KPI_TYPE_TRGT_ACTL_KEY	
   and m.kpi_type_key = c.kpi_type_key
   and m.KPI_TYPE_TRGT_ACTL_KEY = t.KPI_TYPE_TRGT_ACTL_KEY
   and TO_DATE ('$$v_snsh_date', 'YYYYMMDD') BETWEEN mm.bgn_date AND mm.end_date
   and TO_DATE ('$$v_snsh_date', 'YYYYMMDD') BETWEEN cc.bgn_date AND cc.end_date
   and TO_DATE ('$$v_snsh_date', 'YYYYMMDD') BETWEEN tt.bgn_date AND tt.end_date
   order by c.kpi_type_code, t.kpi_type_tac"""
        query = query.replace("$$v_snsh_date", v_snsh_date)
        df_SQ_SOR_HOM_SMT_KPI_TYPE_CODE = lib.read_sql(spark, _conn, query=query)
        df_SQ_SOR_HOM_SMT_KPI_TYPE_CODE = lib.sq_output(
            input_df=df_SQ_SOR_HOM_SMT_KPI_TYPE_CODE,
            port_cols={
                'KPI_TYPE_CODE': 'string',
                'KPI_TYPE_DESP': 'string',
                'KPI_TYPE_TAC': 'string',
                'KPI_TYPE_TRGT_ACTL_DESP': 'string',
            },
        )
        ctx.register_df("df_SQ_SOR_HOM_SMT_KPI_TYPE_CODE", df_SQ_SOR_HOM_SMT_KPI_TYPE_CODE)
        
        logger.info("Step: read_LKP_DDS_DMNS_KPI_TYPE")
        # Reading Data From Source - read_LKP_DDS_DMNS_KPI_TYPE
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_LKP_DDS_DMNS_KPI_TYPE = lib.read_sql(spark, _conn, table="DDS_DMNS_KPI_TYPE")
        
        logger.info("Step: apply_LKP_DDS_DMNS_KPI_TYPE")
        # Lookup: apply_LKP_DDS_DMNS_KPI_TYPE
        # Use First Value / Use Any Value: dedup by join keys
        df_LKP_DDS_DMNS_KPI_TYPE = df_LKP_DDS_DMNS_KPI_TYPE.dropDuplicates(subset=["KPI_TYPE_CODE", "KPI_TYPE_TRGT_ACTL_CODE"])
        # Rename upstream columns to match lookup input port names before join
        _lkp_input = df_SQ_SOR_HOM_SMT_KPI_TYPE_CODE
        _lkp_input = _lkp_input.withColumn("IN_KPI_TYPE_CODE", col("KPI_TYPE_CODE"))
        _lkp_input = _lkp_input.withColumn("IN_KPI_TYPE_TRGT_ACTL_CODE", col("KPI_TYPE_TAC"))
        # Join condition: IN_KPI_TYPE_CODE=KPI_TYPE_CODE AND IN_KPI_TYPE_TRGT_ACTL_CODE=KPI_TYPE_TRGT_ACTL_CODE
        # Alias-based join: _main.<source_col> == _lkp.<lookup_col>
        df_lkp_merge_SQ_SOR_HOM_SMT_KPI_TYPE_CODE = _lkp_input.alias("_main").join(
            broadcast(df_LKP_DDS_DMNS_KPI_TYPE).alias("_lkp"),
            (col("_main.IN_KPI_TYPE_CODE") == col("_lkp.KPI_TYPE_CODE")) &
            (col("_main.IN_KPI_TYPE_TRGT_ACTL_CODE") == col("_lkp.KPI_TYPE_TRGT_ACTL_CODE")),
            "left"
        ).select(
            *[_lkp_input[c] for c in _lkp_input.columns],
            *[df_LKP_DDS_DMNS_KPI_TYPE[c] for c in df_LKP_DDS_DMNS_KPI_TYPE.columns if c.lower() not in [x.lower() for x in _lkp_input.columns]]
        )
        ctx.register_df("df_lkp_merge_SQ_SOR_HOM_SMT_KPI_TYPE_CODE", df_lkp_merge_SQ_SOR_HOM_SMT_KPI_TYPE_CODE)        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = lib.expression(
            input_df=df_lkp_merge_SQ_SOR_HOM_SMT_KPI_TYPE_CODE,
            computed_columns=[
                {'name': 'IN_KPI_TYPE_DESP', 'expr': 'KPI_TYPE_DESP'},
                {'name': 'CHANGE_FLAG', 'expr': 'CASE WHEN (DMNS_KPI_TYPE_KEY IS NULL) OR CASE WHEN KPI_TYPE_DESP = KPI_TYPE_DESP THEN false ELSE true END OR CASE WHEN KPI_TYPE_TRGT_ACTL_DESP = KPI_TYPE_TRGT_ACTL_DESP THEN false ELSE true END THEN 1 ELSE 0 END'},
                {'name': 'IN_KPI_TYPE_TRGT_ACTL_DESP', 'expr': 'KPI_TYPE_TRGT_ACTL_DESP'}
            ],
        )
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_FIL_CHANGE")
        # Filter: apply_FIL_CHANGE
        df_FIL_CHANGE = lib.filter(
            input_df=df_EXPTRANS,
            condition='CHANGE_FLAG = 1 AND ( NOT (DMNS_KPI_TYPE_KEY IS NULL))',
        )
        ctx.register_df("df_FIL_CHANGE", df_FIL_CHANGE)

        
        metrics.complete()
        logger.info("Mapping M_DPA_SUMMARIZE_DMNS_KPI_TYPE completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DPA_SUMMARIZE_DMNS_KPI_TYPE FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_KPI_TYPE", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DPA_SUMMARIZE_DMNS_KPI_TYPE")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())