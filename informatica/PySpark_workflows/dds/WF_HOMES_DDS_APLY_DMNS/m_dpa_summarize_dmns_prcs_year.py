'''
* Script - M_DPA_SUMMARIZE_DMNS_PRCS_YEAR.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_DPA_SUMMARIZE_DMNS_PRCS_YEAR mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DPA_SUMMARIZE_DMNS_PRCS_YEAR")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_PRCS_YEAR", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()


    v_snsh_date = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    try:
        _param_obj = objects.get("UTL_JOB_PARAM", {})
        if isinstance(_param_obj, dict):
            _param_path = lib._resolve_path(_param_obj.get('path'))
        with open(_param_path, "r") as _f:
            for _line in _f:
                _line = _line.strip()
                for _var in [ "$$v_snsh_date", ]:
                    if _line.startswith(_var + "="):
                        _val = _line.split("=", 1)[1]
                        _clean = _var.replace("$", "")
                        if _clean == "v_snsh_date":
                            v_snsh_date = _val
                        logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
    except Exception:
        logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_SOR_HOM_BUD_PARM")
        # Reading Data From Source - read_SOR_HOM_BUD_PARM
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "SOR")
        df_SOR_HOM_BUD_PARM = lib.read_sql(spark, _conn, table="SOR_HOM_BUD_PARM")
        
        logger.info("Step: apply_SQ_SOR_HOM_BUD_PARM")
        # Source Qualifier: apply_SQ_SOR_HOM_BUD_PARM
        # SQL Pushdown - executes Informatica SQ SQL on source database
        _conn = lib.get_db_config(config, "SOR")
        # Parameterize schema from connection config
        _schema = _conn.get("schema", "") or "psor"
        query = f"""select a.prcs_year_num, a.prcs_year_text, dense_rank()
  over (
		 order by (a.prcs_year_num)
       ) year_disp_seq_num
  from (
		select y.cur_year + a.add_year prcs_year_num,
		       to_char(y.cur_year + a.add_year)||' - '||
		       to_char(y.cur_year + a.add_year + 1) prcs_year_text
		  from (
				select to_number(to_char(sysdate, 'YYYY')) cur_year
				  from dual
		       ) y,(
				select 0 add_year
				  from dual
				 union all
				select 1 add_year
				  from dual
				 union all
				select 2 add_year
				  from dual
				 union all
				select 3 add_year
				  from dual
				 union all
				select 4 add_year
				  from dual
				 union all
				select 5 add_year
				  from dual
				 union all
				select 6 add_year
				  from dual
				 union all
				select 7 add_year
				  from dual
				 union all
				select 8 add_year
				  from dual
				 union all
				select 9 add_year
				  from dual
				 union all
				select 10 add_year
				  from dual
				 union all
				select 11 add_year
				  from dual
				 union all
				select 12 add_year
				  from dual
				 union all
				select 13 add_year
				  from dual
				 union all
				select 14 add_year
				  from dual
				 union all
				select 15 add_year
				  from dual
				 union all
				select 16 add_year
				  from dual
				 union all
				select 17 add_year
				  from dual
				 union all
				select 18 add_year
				  from dual
				 union all
				select 19 add_year
				  from dual
				 union all
				select 20 add_year
				  from dual
				 union all
				select 21 add_year
				  from dual
				 union all
				select 22 add_year
				  from dual
				 union all
				select 23 add_year
				  from dual
				 union all
				select 24 add_year
				  from dual
				 union all
				select 25 add_year
				  from dual
				 union all
				select -1 add_year
				  from dual
				 union all
				select -2 add_year
				  from dual
				 union all
				select -3 add_year
				  from dual
				 union all
				select -4 add_year
				  from dual
				 union all
				select -5 add_year
				  from dual
				 union all
				select -6 add_year
				  from dual
				 union all
				select -7 add_year
				  from dual
				 union all
				select -8 add_year
				  from dual
				 union all
				select -9 add_year
				  from dual
				 union all
				select -10 add_year
				  from dual
				 union all
				select -11 add_year
				  from dual
				 union all
				select -12 add_year
				  from dual
				 union all
				select -13 add_year
				  from dual
				 union all
				select -14 add_year
				  from dual
				 union all
				select -15 add_year
				  from dual
				 union all
				select -16 add_year
				  from dual
				 union all
				select -17 add_year
				  from dual
				 union all
				select -18 add_year
				  from dual
				 union all
				select -19 add_year
				  from dual
				 union all
				select -20 add_year
				  from dual
				 union all
				select -21 add_year
				  from dual
				 union all
				select -22 add_year
				  from dual
				 union all
				select -23 add_year
				  from dual
				 union all
				select -24 add_year
				  from dual
				 union all
				select -25 add_year
				  from dual
		       ) a
		 union 
		select y.prcs_year+1000 prcs_year, 'Post '||to_char(y.prcs_year+9)||' - '||
		       to_char(y.prcs_year+10) prcs_year_text
		  from (
				select to_number(pp.parm_text) prcs_year
				  from SOR_HOM_BUD_PARM p, SOR_HOM_BUD_PARM_STS pp
				 where pp.PARM_NAME      = 'PRCS_YEAR'
				 and TO_DATE ('$$v_snsh_date', 'YYYYMMDD') BETWEEN pp.bgn_date AND pp.end_date
				 union 
				select distinct first_value(c.prcs_year)
				  over (
						 order by c.prcs_year desc
				       ) prcs_year
				  from SOR_HOM_BUD_COPY c
				 where c.last_rec_txn_type_code is null
				   and c.bdgt_exrc_code = '0'
				   and c.copy_code      = 'C'
				   and c.copy_ver_num   = -1
		       ) y
       ) a"""
        query = query.replace("$$v_snsh_date", v_snsh_date)
        df_SQ_SOR_HOM_BUD_PARM = lib.read_sql(spark, _conn, query=query)
        df_SQ_SOR_HOM_BUD_PARM = lib.sq_output(
            input_df=df_SQ_SOR_HOM_BUD_PARM,
            port_cols={
                'PRCS_YEAR': 'decimal',
                'PARM_TEXT': 'string',
            },
        )
        ctx.register_df("df_SQ_SOR_HOM_BUD_PARM", df_SQ_SOR_HOM_BUD_PARM)
        
        logger.info("Step: read_LKP_DDS_DMNS_PRCS_YEAR")
        # Reading Data From Source - read_LKP_DDS_DMNS_PRCS_YEAR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_LKP_DDS_DMNS_PRCS_YEAR = lib.read_sql(spark, _conn, table="DDS_DMNS_PRCS_YEAR")
        
        logger.info("Step: apply_LKP_DDS_DMNS_PRCS_YEAR")
        # Lookup: apply_LKP_DDS_DMNS_PRCS_YEAR
        # Use First Value / Use Any Value: dedup by join keys
        df_LKP_DDS_DMNS_PRCS_YEAR = df_LKP_DDS_DMNS_PRCS_YEAR.dropDuplicates(subset=["PRCS_YEAR_NUM"])
        # Rename upstream columns to match lookup input port names before join
        _lkp_input = df_SQ_SOR_HOM_BUD_PARM
        _lkp_input = _lkp_input.withColumn("IN_PRCS_YEAR_NUM", col("PRCS_YEAR"))
        # Join condition: IN_PRCS_YEAR_NUM=PRCS_YEAR_NUM
        # Alias-based join: _main.<source_col> == _lkp.<lookup_col>
        df_lkp_merge_SQ_SOR_HOM_BUD_PARM = _lkp_input.alias("_main").join(
            broadcast(df_LKP_DDS_DMNS_PRCS_YEAR).alias("_lkp"),
            (col("_main.IN_PRCS_YEAR_NUM") == col("_lkp.PRCS_YEAR_NUM")),
            "left"
        ).select(
            *[_lkp_input[c] for c in _lkp_input.columns],
            *[df_LKP_DDS_DMNS_PRCS_YEAR[c] for c in df_LKP_DDS_DMNS_PRCS_YEAR.columns if c.lower() not in [x.lower() for x in _lkp_input.columns]]
        )
        ctx.register_df("df_lkp_merge_SQ_SOR_HOM_BUD_PARM", df_lkp_merge_SQ_SOR_HOM_BUD_PARM)        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = lib.expression(
            input_df=df_lkp_merge_SQ_SOR_HOM_BUD_PARM,
            computed_columns=[
                {'name': 'IN_PARM_TEXT', 'expr': 'PARM_TEXT'},
                {'name': 'CHANGE_FLAG', 'expr': 'CASE WHEN (DMNS_PRCS_YEAR_KEY IS NULL) OR CASE WHEN PRCS_YEAR_NUM = IN_PRCS_YEAR_NUM THEN false ELSE true END OR CASE WHEN PRCS_YEAR_TEXT = PARM_TEXT THEN false ELSE true END THEN 1 ELSE 0 END'}
            ],
        )
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_FIL_CHANGE")
        # Filter: apply_FIL_CHANGE
        df_FIL_CHANGE = lib.filter(
            input_df=df_EXPTRANS,
            condition='CHANGE_FLAG = 1 AND ( NOT (DMNS_PRCS_YEAR_KEY IS NULL))',
        )
        ctx.register_df("df_FIL_CHANGE", df_FIL_CHANGE)

        
        metrics.complete()
        logger.info("Mapping M_DPA_SUMMARIZE_DMNS_PRCS_YEAR completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DPA_SUMMARIZE_DMNS_PRCS_YEAR FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_PRCS_YEAR", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DPA_SUMMARIZE_DMNS_PRCS_YEAR")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())