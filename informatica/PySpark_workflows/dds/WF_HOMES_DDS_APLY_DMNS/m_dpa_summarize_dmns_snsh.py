'''
* Script - M_DPA_SUMMARIZE_DMNS_SNSH.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_DPA_SUMMARIZE_DMNS_SNSH mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DPA_SUMMARIZE_DMNS_SNSH")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_SNSH", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()


    v_snsh_date = ""
    # Load mapping variables from the UTL_JOB_PARAM file (shared helper)
    _vars = lib.load_mapping_variables(config, [ "$$v_snsh_date", ], logger)
    v_snsh_date = _vars.get("v_snsh_date", v_snsh_date)
    
    try:
        logger.info("Step: apply_SQ_SOR_HOM_PRG_SNSH_STS")
        # Source Qualifier: apply_SQ_SOR_HOM_PRG_SNSH_STS
        # SQL Pushdown - executes Informatica SQ SQL on source database
        _conn = lib.get_db_config(config, "SOR")
        # Parameterize schema from connection config
        _schema = _conn.get("schema", "") or "psor"
        query = f"""select ss.snsh_year, ss.snsh_mth,
       to_char(
	       to_number(
		       to_char(
			       add_months(
				       ss.snsh_as_at_date, -3
			       ), 'YYYY'
	       )
       ))||'/'||
       substr(
	       to_char(
		       to_number(
			       to_char(
				       add_months(
					       ss.snsh_as_at_date, -3
				       ), 'YYYY'
			       )
		       )+1
	       ), 3
       ) disp_fin_year_text, to_char(ss.snsh_as_at_date, 'YYYY ') ||
       decode(
	       to_char(ss.snsh_as_at_date, 'MM'), '01', 'JAN', '02', 'FEB',
	       '03', 'MAR', '04', 'APR', '05', 'MAY', '06', 'JUN', '07', 'JUL', '08',
	       'AUG', '09', 'SEP', '10', 'OCT', '11', 'NOV', '12', 'DEC'
       ) disp_snsh_text
  from SOR_HOM_PRG_SNSH_sts ss
 where
    ss.last_rec_txn_type_code is null
   and ss.snsh_type_code = 'FINAL'
   and ss.snsh_qtr_ind   = 'N'
   and TO_DATE ('$$v_snsh_date', 'YYYYMMDD') BETWEEN ss.bgn_date AND ss.end_date
order by ss.snsh_year, ss.snsh_mth"""
        query = query.replace("$$v_snsh_date", v_snsh_date)
        df_SQ_SOR_HOM_PRG_SNSH_STS = lib.read_sql(spark, _conn, query=query)
        df_SQ_SOR_HOM_PRG_SNSH_STS = lib.sq_output(
            input_df=df_SQ_SOR_HOM_PRG_SNSH_STS,
            port_cols={
                'SNSH_YEAR': 'decimal',
                'SNSH_MTH': 'decimal',
                'DISP_FIN_YEAR_TEXT': 'string',
                'DISP_SNSH_TEXT': 'string',
            },
        )
        ctx.register_df("df_SQ_SOR_HOM_PRG_SNSH_STS", df_SQ_SOR_HOM_PRG_SNSH_STS)
        
        logger.info("Step: read_LKP_DDS_DMNS_SNSH")
        # Reading Data From Source - read_LKP_DDS_DMNS_SNSH
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_LKP_DDS_DMNS_SNSH = lib.read_sql(spark, _conn, table="DDS_DMNS_SNSH")
        
        logger.info("Step: apply_LKP_DDS_DMNS_SNSH")
        # Lookup: apply_LKP_DDS_DMNS_SNSH
        # Use First Value / Use Any Value: dedup by join keys
        df_LKP_DDS_DMNS_SNSH = df_LKP_DDS_DMNS_SNSH.dropDuplicates(subset=["SNSH_YEAR", "SNSH_MTH"])
        # Rename upstream columns to match lookup input port names before join
        _lkp_input = df_SQ_SOR_HOM_PRG_SNSH_STS
        _lkp_input = _lkp_input.withColumn("IN_SNSH_YEAR", col("SNSH_YEAR"))
        _lkp_input = _lkp_input.withColumn("IN_SNSH_MTH", col("SNSH_MTH"))
        # Join condition: IN_SNSH_YEAR=SNSH_YEAR AND IN_SNSH_MTH=SNSH_MTH
        # Alias-based join: _main.<source_col> == _lkp.<lookup_col>
        df_lkp_merge_SQ_SOR_HOM_PRG_SNSH_STS = _lkp_input.alias("_main").join(
            broadcast(df_LKP_DDS_DMNS_SNSH).alias("_lkp"),
            (col("_main.IN_SNSH_YEAR") == col("_lkp.SNSH_YEAR")) &
            (col("_main.IN_SNSH_MTH") == col("_lkp.SNSH_MTH")),
            "left"
        ).select(
            *[_lkp_input[c] for c in _lkp_input.columns],
            *[df_LKP_DDS_DMNS_SNSH[c] for c in df_LKP_DDS_DMNS_SNSH.columns if c.lower() not in [x.lower() for x in _lkp_input.columns]]
        )
        ctx.register_df("df_lkp_merge_SQ_SOR_HOM_PRG_SNSH_STS", df_lkp_merge_SQ_SOR_HOM_PRG_SNSH_STS)        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = lib.expression(
            input_df=df_lkp_merge_SQ_SOR_HOM_PRG_SNSH_STS,
            computed_columns=[
                {'name': 'IN_DISP_FIN_YEAR_TEXT', 'expr': 'DISP_FIN_YEAR_TEXT'},
                {'name': 'IN_DISP_SNSH_TEXT', 'expr': 'DISP_SNSH_TEXT'},
                {'name': 'CHANGE_FLAG', 'expr': 'CASE WHEN (DMNS_SNSH_KEY IS NULL) OR CASE WHEN DISP_FIN_YEAR_TEXT = DISP_FIN_YEAR_TEXT THEN false ELSE true END OR CASE WHEN DISP_SNSH_TEXT = DISP_SNSH_TEXT THEN false ELSE true END THEN 1 ELSE 0 END'}
            ],
        )
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_FIL_CHANGE")
        # Filter: apply_FIL_CHANGE
        df_FIL_CHANGE = lib.filter(
            input_df=df_EXPTRANS,
            condition='CHANGE_FLAG = 1 AND ( NOT (DMNS_SNSH_KEY IS NULL))',
        )
        ctx.register_df("df_FIL_CHANGE", df_FIL_CHANGE)

        
        metrics.complete()
        logger.info("Mapping M_DPA_SUMMARIZE_DMNS_SNSH completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DPA_SUMMARIZE_DMNS_SNSH FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_SNSH", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DPA_SUMMARIZE_DMNS_SNSH")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())