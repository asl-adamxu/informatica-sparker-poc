'''
* Script - M_DPA_SUMMARIZE_DMNS_SNSH.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_DPA_SUMMARIZE_DMNS_SNSH mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DPA_SUMMARIZE_DMNS_SNSH")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_SNSH", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SOR")
    conn_target = lib.get_db_config(config, "DPA")

    v_snsh_date = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    try:
        _param_obj = objects.get("UTL_JOB_PARAM", {})
        if isinstance(_param_obj, dict):
            _param_path = lib._resolve_path(_param_obj.get('path'))
        with open(_param_path, "r") as _f:
            for _line in _f:
                _line = _line.strip()
                for _var in [ "$$v_snsh_date", ]:
                    if _line.startswith(_var + "="):
                        _val = _line.split("=", 1)[1]
                        _clean = _var.replace("$", "")
                        if _clean == "v_snsh_date":
                            v_snsh_date = _val
                        logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
    except Exception:
        logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: apply_SEQ_DMNS_SNSH_KEY")
        # Sequence Generator: apply_SEQ_DMNS_SNSH_KEY
        df_SEQ_DMNS_SNSH_KEY = df_input.withColumn(
            "NEXTVAL", 
            monotonically_increasing_id() + 0
        )
        ctx.register_df("df_SEQ_DMNS_SNSH_KEY", df_SEQ_DMNS_SNSH_KEY)
        
        logger.info("Step: apply_SQ_SOR_HOM_PRG_SNSH_STS")
        # Source Qualifier: apply_SQ_SOR_HOM_PRG_SNSH_STS
        # SQL Pushdown - executes Informatica SQ SQL on source database
        _conn = lib.get_db_config(config, "SOR")
        # Parameterize schema from connection config
        _schema = _conn.get("schema", "") or "psor"
        query = f"""select ss.snsh_year, ss.snsh_mth,
       to_char(
	       to_number(
		       to_char(
			       add_months(
				       ss.snsh_as_at_date, -3
			       ), 'YYYY'
	       )
       ))||'/'||
       substr(
	       to_char(
		       to_number(
			       to_char(
				       add_months(
					       ss.snsh_as_at_date, -3
				       ), 'YYYY'
			       )
		       )+1
	       ), 3
       ) disp_fin_year_text, to_char(ss.snsh_as_at_date, 'YYYY ') ||
       decode(
	       to_char(ss.snsh_as_at_date, 'MM'), '01', 'JAN', '02', 'FEB',
	       '03', 'MAR', '04', 'APR', '05', 'MAY', '06', 'JUN', '07', 'JUL', '08',
	       'AUG', '09', 'SEP', '10', 'OCT', '11', 'NOV', '12', 'DEC'
       ) disp_snsh_text
  from SOR_HOM_PRG_SNSH_sts ss
 where
    ss.last_rec_txn_type_code is null
   and ss.snsh_type_code = 'FINAL'
   and ss.snsh_qtr_ind   = 'N'
   and TO_DATE ($$v_snsh_date, 'YYYYMMDD') BETWEEN ss.bgn_date AND ss.end_date
order by ss.snsh_year, ss.snsh_mth"""
        query = query.replace("$$v_snsh_date", v_snsh_date)
        df_SQ_SOR_HOM_PRG_SNSH_STS = lib.read_sql(spark, _conn, query=query)
        # Rename SQL result columns to SQ output ports by position (handles unaliased expressions)
        _sql_cols = df_SQ_SOR_HOM_PRG_SNSH_STS.columns
        _port_cols = ["SNSH_YEAR", "SNSH_MTH", "DISP_FIN_YEAR_TEXT", "DISP_SNSH_TEXT"]
        for _i in range(len(_sql_cols) if len(_sql_cols) < len(_port_cols) else len(_port_cols)):
            if _sql_cols[_i].lower() != _port_cols[_i].lower():
                df_SQ_SOR_HOM_PRG_SNSH_STS = df_SQ_SOR_HOM_PRG_SNSH_STS.withColumnRenamed(_sql_cols[_i], _port_cols[_i])
        # Select only SQ output ports (matches Informatica behavior)
        df_SQ_SOR_HOM_PRG_SNSH_STS = df_SQ_SOR_HOM_PRG_SNSH_STS.select("SNSH_YEAR", "SNSH_MTH", "DISP_FIN_YEAR_TEXT", "DISP_SNSH_TEXT")
        
        ctx.register_df("df_SQ_SOR_HOM_PRG_SNSH_STS", df_SQ_SOR_HOM_PRG_SNSH_STS)
        
        logger.info("Step: read_LKP_DDS_DMNS_SNSH")
        # Reading Data From Source - read_LKP_DDS_DMNS_SNSH
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_LKP_DDS_DMNS_SNSH = lib.read_sql(spark, _conn, table="DDS_DMNS_SNSH")
        
        logger.info("Step: apply_LKP_DDS_DMNS_SNSH")
        # Lookup: apply_LKP_DDS_DMNS_SNSH
        # Use First Value / Use Any Value: dedup by join keys
        df_LKP_DDS_DMNS_SNSH = df_LKP_DDS_DMNS_SNSH.dropDuplicates(subset=["SNSH_YEAR", "SNSH_MTH"])
        # Join condition: SNSH_YEAR=SNSH_YEAR AND SNSH_MTH=SNSH_MTH
        # Rename right-side join keys ONLY when they share the same name as the
        # left-side key (e.g. TNCY_AGRMT_BK=TNCY_AGRMT_BK → _lkp_TNCY_AGRMT_BK).
        # Keys with different names on each side are kept as-is.
        _lkp_right = df_LKP_DDS_DMNS_SNSH
        _lkp_right = _lkp_right.withColumnRenamed("SNSH_YEAR", "_lkp_SNSH_YEAR")
        _lkp_right = _lkp_right.withColumnRenamed("SNSH_MTH", "_lkp_SNSH_MTH")
        # Drop lookup columns that would conflict with input columns (e.g. both
        # sides having EST_KEY but only one is a join key → ambiguity after join).
        __lkp_keep = [c for c in _lkp_right.columns if c.startswith("_lkp_") or c not in df_SQ_SOR_HOM_PRG_SNSH_STS.columns]
        if len(__lkp_keep) < len(_lkp_right.columns):
            _lkp_right = _lkp_right.select(*__lkp_keep)
        df_lkp_merge_1 = df_SQ_SOR_HOM_PRG_SNSH_STS.join(
            broadcast(_lkp_right),
            (df_SQ_SOR_HOM_PRG_SNSH_STS["SNSH_YEAR"] == _lkp_right["_lkp_SNSH_YEAR"]) &
            (df_SQ_SOR_HOM_PRG_SNSH_STS["SNSH_MTH"] == _lkp_right["_lkp_SNSH_MTH"]),
            "left"
        ).drop("_lkp_SNSH_YEAR").drop("_lkp_SNSH_MTH")

        ctx.register_df("df_lkp_merge_1", df_lkp_merge_1)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = df_lkp_merge_1
        df_EXPTRANS = df_EXPTRANS.withColumn("IN_DISP_SNSH_TEXT", expr("DISP_SNSH_TEXT"))
        df_EXPTRANS = df_EXPTRANS.withColumn("CHANGE_FLAG", expr("CASE WHEN (DMNS_SNSH_KEY IS NULL) OR CASE WHEN DISP_FIN_YEAR_TEXT = DISP_FIN_YEAR_TEXT THEN false ELSE true END OR CASE WHEN DISP_SNSH_TEXT = DISP_SNSH_TEXT THEN false ELSE true END THEN 1 ELSE 0 END"))
        df_EXPTRANS = df_EXPTRANS.withColumn("IN_DISP_FIN_YEAR_TEXT", expr("DISP_FIN_YEAR_TEXT"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["DMNS_SNSH_KEY", "DISP_SNSH_TEXT", "DISP_FIN_YEAR_TEXT", "SNSH_YEAR", "SNSH_MTH", "SNSH_DISP_SEQ_NUM", "IN_SNSH_YEAR", "IN_SNSH_MTH"]:
            if _col not in df_EXPTRANS.columns:
                df_EXPTRANS = df_EXPTRANS.withColumn(_col, lit(None))
        # Keep all upstream columns + computed columns (no select filtering)
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_FIL_CHANGE")
        # Filter: apply_FIL_CHANGE
        __fil_input = df_EXPTRANS
        df_FIL_CHANGE = __fil_input.filter(expr("CHANGE_FLAG = 1 AND ( NOT (DMNS_SNSH_KEY IS NULL))"))
        ctx.register_df("df_FIL_CHANGE", df_FIL_CHANGE)
        
        logger.info("Step: apply_FIL_NEW")
        # Filter: apply_FIL_NEW
        __fil_input = df_EXPTRANS
        df_FIL_NEW = __fil_input.filter(expr("CHANGE_FLAG = 1 AND (DMNS_SNSH_KEY IS NULL)"))
        ctx.register_df("df_FIL_NEW", df_FIL_NEW)
        
        logger.info("Step: apply_Union_Transformation")
        # Union: apply_Union_Transformation
        # Select + rename upstream columns per input, then union
        df_Union_Transformation_change = df_FIL_CHANGE.select(
            col("DMNS_SNSH_KEY").alias("DMNS_SNSH_KEY"),
            col("IN_SNSH_YEAR").alias("IN_SNSH_YEAR"),
            col("IN_SNSH_MTH").alias("IN_SNSH_MTH"),
            col("IN_DISP_FIN_YEAR_TEXT").alias("IN_DISP_FIN_YEAR_TEXT"),
            col("IN_DISP_SNSH_TEXT").alias("IN_DISP_SNSH_TEXT")        )
        df_Union_Transformation_new = df_FIL_NEW.select(
            col("NEXTVAL").alias("DMNS_SNSH_KEY"),
            col("IN_SNSH_YEAR").alias("IN_SNSH_YEAR"),
            col("IN_SNSH_MTH").alias("IN_SNSH_MTH"),
            col("IN_DISP_FIN_YEAR_TEXT").alias("IN_DISP_FIN_YEAR_TEXT"),
            col("IN_DISP_SNSH_TEXT").alias("IN_DISP_SNSH_TEXT")        )
        df_Union_Transformation = df_Union_Transformation_change
        df_Union_Transformation = df_Union_Transformation.unionByName(df_Union_Transformation_new, allowMissingColumns=True)
        # Select only union output columns
        df_Union_Transformation = df_Union_Transformation.select("DMNS_SNSH_KEY", "IN_SNSH_YEAR", "IN_SNSH_MTH", "IN_DISP_FIN_YEAR_TEXT", "IN_DISP_SNSH_TEXT")
        ctx.register_df("df_Union_Transformation", df_Union_Transformation)
        
        logger.info("Step: write_DPA_DMNS_SNSH")
        # Write to Target: write_DPA_DMNS_SNSH
        df_write = df_Union_Transformation
        # Cast columns to match target schema data types
        if "disp_fin_year_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "disp_fin_year_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "disp_snsh_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "disp_snsh_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("SNSH_DISP_SEQ_NUM", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"DISP_FIN_YEAR_TEXT": "IN_DISP_FIN_YEAR_TEXT", "DISP_SNSH_TEXT": "IN_DISP_SNSH_TEXT", "DMNS_SNSH_KEY": "DMNS_SNSH_KEY", "SNSH_MTH": "IN_SNSH_MTH", "SNSH_YEAR": "IN_SNSH_YEAR"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['DMNS_SNSH_KEY', 'SNSH_YEAR', 'SNSH_MTH', 'DISP_FIN_YEAR_TEXT', 'DISP_SNSH_TEXT', 'SNSH_DISP_SEQ_NUM']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "DPA_DMNS_SNSH", mode="append")

        logger.info("write_DPA_DMNS_SNSH write completed")
        
        metrics.complete()
        logger.info("Mapping M_DPA_SUMMARIZE_DMNS_SNSH completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DPA_SUMMARIZE_DMNS_SNSH FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_SNSH", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DPA_SUMMARIZE_DMNS_SNSH")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()