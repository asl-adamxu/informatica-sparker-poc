'''
* Script - M_DPA_SUMMARIZE_DMNS_CNTR_TYPE.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_DPA_SUMMARIZE_DMNS_CNTR_TYPE mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DPA_SUMMARIZE_DMNS_CNTR_TYPE")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_CNTR_TYPE", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()


    v_snsh_date = ""
    # Load mapping variables from the UTL_JOB_PARAM file (shared helper)
    _vars = lib.load_mapping_variables(config, [ "$$v_snsh_date", ], logger)
    v_snsh_date = _vars.get("v_snsh_date", v_snsh_date)
    
    try:
        logger.info("Step: apply_SQ_SOR_HOM_CON_CNTR_TYPE")
        # Source Qualifier: apply_SQ_SOR_HOM_CON_CNTR_TYPE
        # SQL Pushdown - executes Informatica SQ SQL on source database
        _conn = lib.get_db_config(config, "SOR")
        # Parameterize schema from connection config
        _schema = _conn.get("schema", "") or "psor"
        query = f"""select cl.cntr_class_code, c.cntr_type_code, cs.cntr_type_desp, dense_rank()
  over (
		 order by c.cntr_class_key, c.cntr_type_code, cs.cntr_type_desp
       ) cntr_type_disp_seq_num
  from SOR_HOM_CON_CNTR_TYPE c, SOR_HOM_CON_CNTR_TYPE_STS cs, SOR_HOM_CON_CNTR_CLASS cl
 where c.CNTR_TYPE_KEY = cs.CNTR_TYPE_KEY and
 cl.CNTR_CLASS_KEY = c.CNTR_CLASS_KEY and
 c.LAST_REC_TXN_TYPE_CODE is null and
 cs.LAST_REC_TXN_TYPE_CODE is null and
 cl.LAST_REC_TXN_TYPE_CODE is null and
 TO_DATE ('$$v_snsh_date', 'YYYYMMDD') BETWEEN cs.bgn_date AND cs.end_date"""
        query = query.replace("$$v_snsh_date", v_snsh_date)
        df_SQ_SOR_HOM_CON_CNTR_TYPE = lib.read_sql(spark, _conn, query=query)
        df_SQ_SOR_HOM_CON_CNTR_TYPE = lib.sq_output(
            input_df=df_SQ_SOR_HOM_CON_CNTR_TYPE,
            port_cols={
                'CNTR_CLASS_CODE': 'string',
                'CNTR_TYPE_CODE': 'string',
                'CNTR_TYPE_DESP': 'string',
            },
        )
        ctx.register_df("df_SQ_SOR_HOM_CON_CNTR_TYPE", df_SQ_SOR_HOM_CON_CNTR_TYPE)
        
        logger.info("Step: read_LKP_DDS_DMNS_CNTR_TYPE")
        # Reading Data From Source - read_LKP_DDS_DMNS_CNTR_TYPE
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_LKP_DDS_DMNS_CNTR_TYPE = lib.read_sql(spark, _conn, table="DDS_DMNS_CNTR_TYPE")
        
        logger.info("Step: apply_LKP_DDS_DMNS_CNTR_TYPE")
        # Lookup: apply_LKP_DDS_DMNS_CNTR_TYPE
        # Use First Value / Use Any Value: dedup by join keys
        df_LKP_DDS_DMNS_CNTR_TYPE = df_LKP_DDS_DMNS_CNTR_TYPE.dropDuplicates(subset=["CNTR_CLASS_CODE", "CNTR_TYPE_CODE"])
        # Rename upstream columns to match lookup input port names before join
        _lkp_input = df_SQ_SOR_HOM_CON_CNTR_TYPE
        _lkp_input = _lkp_input.withColumn("IN_CNTR_CLASS_CODE", col("CNTR_CLASS_CODE"))
        _lkp_input = _lkp_input.withColumn("IN_CNTR_TYPE_CODE", col("CNTR_TYPE_CODE"))
        # Join condition: IN_CNTR_CLASS_CODE=CNTR_CLASS_CODE AND IN_CNTR_TYPE_CODE=CNTR_TYPE_CODE
        # Alias-based join: _main.<source_col> == _lkp.<lookup_col>
        df_lkp_merge_SQ_SOR_HOM_CON_CNTR_TYPE = _lkp_input.alias("_main").join(
            broadcast(df_LKP_DDS_DMNS_CNTR_TYPE).alias("_lkp"),
            (col("_main.IN_CNTR_CLASS_CODE") == col("_lkp.CNTR_CLASS_CODE")) &
            (col("_main.IN_CNTR_TYPE_CODE") == col("_lkp.CNTR_TYPE_CODE")),
            "left"
        ).select(
            *[_lkp_input[c] for c in _lkp_input.columns],
            *[df_LKP_DDS_DMNS_CNTR_TYPE[c] for c in df_LKP_DDS_DMNS_CNTR_TYPE.columns if c.lower() not in [x.lower() for x in _lkp_input.columns]]
        )
        ctx.register_df("df_lkp_merge_SQ_SOR_HOM_CON_CNTR_TYPE", df_lkp_merge_SQ_SOR_HOM_CON_CNTR_TYPE)        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = lib.expression(
            input_df=df_lkp_merge_SQ_SOR_HOM_CON_CNTR_TYPE,
            computed_columns=[
                {'name': 'IN_CNTR_TYPE_DESP', 'expr': 'CNTR_TYPE_DESP'},
                {'name': 'CHANGE_FLAG', 'expr': 'CASE WHEN (DMNS_CNTR_TYPE_KEY IS NULL) OR CASE WHEN CNTR_TYPE_DESP = CNTR_TYPE_DESP THEN false ELSE true END THEN 1 ELSE 0 END'}
            ],
        )
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_FIL_CHANGE")
        # Filter: apply_FIL_CHANGE
        df_FIL_CHANGE = lib.filter(
            input_df=df_EXPTRANS,
            condition='CHANGE_FLAG = 1 AND ( NOT (DMNS_CNTR_TYPE_KEY IS NULL))',
        )
        ctx.register_df("df_FIL_CHANGE", df_FIL_CHANGE)

        
        metrics.complete()
        logger.info("Mapping M_DPA_SUMMARIZE_DMNS_CNTR_TYPE completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DPA_SUMMARIZE_DMNS_CNTR_TYPE FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_CNTR_TYPE", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DPA_SUMMARIZE_DMNS_CNTR_TYPE")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())