'''
* Script - M_DDS_APLY_DMNS_SNSH_CNTR.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_DDS_APLY_DMNS_SNSH_CNTR mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DDS_APLY_DMNS_SNSH_CNTR")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DDS_APLY_DMNS_SNSH_CNTR", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "DPA")
    conn_target = lib.get_db_config(config, "DPA")

    
    try:
        logger.info("Step: read_DPA_DMNS_SNSH_CNTR")
        # Reading Data From Source - read_DPA_DMNS_SNSH_CNTR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_src_1 = lib.read_sql(spark, _conn, table="DPA_DMNS_SNSH")
        
        logger.info("Step: apply_SQ_DPA_DMNS_SNSH_CNTR")
        # Source Qualifier: apply_SQ_DPA_DMNS_SNSH_CNTR
        df_sq_2 = df_src_1
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("DMNS_SNSH_CNTR_KEY", "CNTR_KEY", "SNSH_KEY", "CNTR_NUM", "CNTR_TTL", "TNDR_NUM", "SNSH_CNTR_DISP_SEQ_NUM")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: write_DDS_DMNS_SNSH_CNTR")
        # Write to Target: write_DDS_DMNS_SNSH_CNTR
        df_write = df_sq_2
        # Cast columns to match target schema data types
        if "disp_fin_year_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "disp_fin_year_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "disp_snsh_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "disp_snsh_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Add NULL for unmapped target columns (schema parity) - excluding identity columns
        df_write = df_write.withColumn("DMNS_SNSH_KEY", lit(None).cast(StringType()))
        df_write = df_write.withColumn("SNSH_YEAR", lit(None).cast(StringType()))
        df_write = df_write.withColumn("SNSH_MTH", lit(None).cast(StringType()))
        df_write = df_write.withColumn("DISP_FIN_YEAR_TEXT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("DISP_SNSH_TEXT", lit(None).cast(StringType()))
        df_write = df_write.withColumn("SNSH_DISP_SEQ_NUM", lit(None).cast(StringType()))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CNTR_KEY": "CNTR_KEY", "CNTR_NUM": "CNTR_NUM", "CNTR_TTL": "CNTR_TTL", "DMNS_SNSH_CNTR_KEY": "DMNS_SNSH_CNTR_KEY", "SNSH_CNTR_DISP_SEQ_NUM": "SNSH_CNTR_DISP_SEQ_NUM", "SNSH_KEY": "SNSH_KEY", "TNDR_NUM": "TNDR_NUM"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['DMNS_SNSH_KEY', 'SNSH_YEAR', 'SNSH_MTH', 'DISP_FIN_YEAR_TEXT', 'DISP_SNSH_TEXT', 'SNSH_DISP_SEQ_NUM']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "DDS_DMNS_SNSH", mode="append")

        logger.info("write_DDS_DMNS_SNSH_CNTR write completed")
        
        metrics.complete()
        logger.info("Mapping M_DDS_APLY_DMNS_SNSH_CNTR completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DDS_APLY_DMNS_SNSH_CNTR FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DDS_APLY_DMNS_SNSH_CNTR", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DDS_APLY_DMNS_SNSH_CNTR")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()