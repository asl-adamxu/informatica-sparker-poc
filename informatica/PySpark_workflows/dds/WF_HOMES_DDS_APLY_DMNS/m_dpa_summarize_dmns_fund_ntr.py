'''
* Script - M_DPA_SUMMARIZE_DMNS_FUND_NTR.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_DPA_SUMMARIZE_DMNS_FUND_NTR mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DPA_SUMMARIZE_DMNS_FUND_NTR")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_FUND_NTR", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "SOR")
    conn_target = lib.get_db_config(config, "DPA")

    v_snsh_date = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    try:
        _param_obj = objects.get("UTL_JOB_PARAM", {})
        if isinstance(_param_obj, dict):
            _param_path = lib._resolve_path(_param_obj.get('path'))
        with open(_param_path, "r") as _f:
            for _line in _f:
                _line = _line.strip()
                for _var in [ "$$v_snsh_date", ]:
                    if _line.startswith(_var + "="):
                        _val = _line.split("=", 1)[1]
                        _clean = _var.replace("$", "")
                        if _clean == "v_snsh_date":
                            v_snsh_date = _val
                        logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
    except Exception:
        logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: apply_SEQ_DMNS_FUND_NTR_KEY")
        # Sequence Generator: apply_SEQ_DMNS_FUND_NTR_KEY
        df_SEQ_DMNS_FUND_NTR_KEY = df_input.withColumn(
            "NEXTVAL", 
            monotonically_increasing_id() + 0
        )
        ctx.register_df("df_SEQ_DMNS_FUND_NTR_KEY", df_SEQ_DMNS_FUND_NTR_KEY)
        
        logger.info("Step: apply_SQ_SOR_HOM_REF_FUND_NTR")
        # Source Qualifier: apply_SQ_SOR_HOM_REF_FUND_NTR
        # SQL Pushdown - executes Informatica SQ SQL on source database
        _conn = lib.get_db_config(config, "SOR")
        # Parameterize schema from connection config
        _schema = _conn.get("schema", "") or "psor"
        query = f"""select
f.FUND_NTR_CODE
,fs.FUND_NTR_DESP
,fs.DISP_SEQ_NUM 
from sor_hom_ref_fund_ntr f, sor_hom_ref_fund_ntr_sts fs
where f.FUND_NTR_KEY = fs.FUND_NTR_KEY 
and f.LAST_REC_TXN_TYPE_CODE is null
and fs.LAST_REC_TXN_TYPE_CODE is null
and TO_DATE ($$v_snsh_date, 'YYYYMMDD') BETWEEN fs.bgn_date AND fs.end_date"""
        query = query.replace("$$v_snsh_date", v_snsh_date)
        df_SQ_SOR_HOM_REF_FUND_NTR = lib.read_sql(spark, _conn, query=query)
        # Rename SQL result columns to SQ output ports by position (handles unaliased expressions)
        _sql_cols = df_SQ_SOR_HOM_REF_FUND_NTR.columns
        _port_cols = ["FUND_NTR_CODE", "FUND_NTR_DESP", "DISP_SEQ_NUM"]
        for _i in range(len(_sql_cols) if len(_sql_cols) < len(_port_cols) else len(_port_cols)):
            if _sql_cols[_i].lower() != _port_cols[_i].lower():
                df_SQ_SOR_HOM_REF_FUND_NTR = df_SQ_SOR_HOM_REF_FUND_NTR.withColumnRenamed(_sql_cols[_i], _port_cols[_i])
        # Select only SQ output ports (matches Informatica behavior)
        df_SQ_SOR_HOM_REF_FUND_NTR = df_SQ_SOR_HOM_REF_FUND_NTR.select("FUND_NTR_CODE", "FUND_NTR_DESP", "DISP_SEQ_NUM")
        
        ctx.register_df("df_SQ_SOR_HOM_REF_FUND_NTR", df_SQ_SOR_HOM_REF_FUND_NTR)
        
        logger.info("Step: read_LKP_DDS_DMNS_FUND_NTR")
        # Reading Data From Source - read_LKP_DDS_DMNS_FUND_NTR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_LKP_DDS_DMNS_FUND_NTR = lib.read_sql(spark, _conn, table="DDS_DMNS_FUND_NTR")
        
        logger.info("Step: apply_LKP_DDS_DMNS_FUND_NTR")
        # Lookup: apply_LKP_DDS_DMNS_FUND_NTR
        # Use First Value / Use Any Value: dedup by join keys
        df_LKP_DDS_DMNS_FUND_NTR = df_LKP_DDS_DMNS_FUND_NTR.dropDuplicates(subset=["FUND_NTR_CODE"])
        # Join condition: FUND_NTR_CODE=FUND_NTR_CODE
        # Rename right-side join keys ONLY when they share the same name as the
        # left-side key (e.g. TNCY_AGRMT_BK=TNCY_AGRMT_BK → _lkp_TNCY_AGRMT_BK).
        # Keys with different names on each side are kept as-is.
        _lkp_right = df_LKP_DDS_DMNS_FUND_NTR
        _lkp_right = _lkp_right.withColumnRenamed("FUND_NTR_CODE", "_lkp_FUND_NTR_CODE")
        # Drop lookup columns that would conflict with input columns (e.g. both
        # sides having EST_KEY but only one is a join key → ambiguity after join).
        __lkp_keep = [c for c in _lkp_right.columns if c.startswith("_lkp_") or c not in df_SQ_SOR_HOM_REF_FUND_NTR.columns]
        if len(__lkp_keep) < len(_lkp_right.columns):
            _lkp_right = _lkp_right.select(*__lkp_keep)
        df_lkp_merge_1 = df_SQ_SOR_HOM_REF_FUND_NTR.join(
            broadcast(_lkp_right),
            (df_SQ_SOR_HOM_REF_FUND_NTR["FUND_NTR_CODE"] == _lkp_right["_lkp_FUND_NTR_CODE"]),
            "left"
        ).drop("_lkp_FUND_NTR_CODE")

        ctx.register_df("df_lkp_merge_1", df_lkp_merge_1)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = df_lkp_merge_1
        df_EXPTRANS = df_EXPTRANS.withColumn("IN_DISP_SEQ_NUM", expr("DISP_SEQ_NUM"))
        df_EXPTRANS = df_EXPTRANS.withColumn("IN_FUND_NTR_DESP", expr("FUND_NTR_DESP"))
        df_EXPTRANS = df_EXPTRANS.withColumn("CHANGE_FLAG", expr("CASE WHEN (DMNS_FUND_NTR_KEY IS NULL) OR CASE WHEN FUND_NTR_DESP = FUND_NTR_DESP THEN false ELSE true END THEN 1 ELSE 0 END"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["FUND_NTR_DESP", "DMNS_FUND_NTR_KEY", "FUND_NTR_CODE", "FUND_NTR_DISP_SEQ_NUM", "IN_FUND_NTR_CODE"]:
            if _col not in df_EXPTRANS.columns:
                df_EXPTRANS = df_EXPTRANS.withColumn(_col, lit(None))
        # Keep all upstream columns + computed columns (no select filtering)
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_FIL_CHANGE")
        # Filter: apply_FIL_CHANGE
        __fil_input = df_EXPTRANS
        df_FIL_CHANGE = __fil_input.filter(expr("CHANGE_FLAG = 1 AND ( NOT (DMNS_FUND_NTR_KEY IS NULL))"))
        ctx.register_df("df_FIL_CHANGE", df_FIL_CHANGE)
        
        logger.info("Step: apply_FIL_NEW")
        # Filter: apply_FIL_NEW
        __fil_input = df_EXPTRANS
        df_FIL_NEW = __fil_input.filter(expr("CHANGE_FLAG = 1 AND (DMNS_FUND_NTR_KEY IS NULL)"))
        ctx.register_df("df_FIL_NEW", df_FIL_NEW)
        
        logger.info("Step: apply_Union_Transformation")
        # Union: apply_Union_Transformation
        # Select + rename upstream columns per input, then union
        df_Union_Transformation_change = df_FIL_CHANGE.select(
            col("DMNS_FUND_NTR_KEY").alias("DMNS_FUND_NTR_KEY"),
            col("IN_FUND_NTR_CODE").alias("IN_FUND_NTR_CODE"),
            col("IN_FUND_NTR_DESP").alias("IN_FUND_NTR_DESP"),
            col("IN_DISP_SEQ_NUM").alias("IN_DISP_SEQ_NUM")        )
        df_Union_Transformation_new = df_FIL_NEW.select(
            col("NEXTVAL").alias("DMNS_FUND_NTR_KEY"),
            col("IN_FUND_NTR_CODE").alias("IN_FUND_NTR_CODE"),
            col("IN_FUND_NTR_DESP").alias("IN_FUND_NTR_DESP"),
            col("IN_DISP_SEQ_NUM").alias("IN_DISP_SEQ_NUM")        )
        df_Union_Transformation = df_Union_Transformation_change
        df_Union_Transformation = df_Union_Transformation.unionByName(df_Union_Transformation_new, allowMissingColumns=True)
        # Select only union output columns
        df_Union_Transformation = df_Union_Transformation.select("DMNS_FUND_NTR_KEY", "IN_FUND_NTR_CODE", "IN_FUND_NTR_DESP", "IN_DISP_SEQ_NUM")
        ctx.register_df("df_Union_Transformation", df_Union_Transformation)
        
        logger.info("Step: write_DPA_DMNS_FUND_NTR")
        # Write to Target: write_DPA_DMNS_FUND_NTR
        df_write = df_Union_Transformation
        # Cast columns to match target schema data types
        if "fund_ntr_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "fund_ntr_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "fund_ntr_desp" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "fund_ntr_desp":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"DMNS_FUND_NTR_KEY": "DMNS_FUND_NTR_KEY", "FUND_NTR_CODE": "IN_FUND_NTR_CODE", "FUND_NTR_DESP": "IN_FUND_NTR_DESP", "FUND_NTR_DISP_SEQ_NUM": "IN_DISP_SEQ_NUM"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['DMNS_FUND_NTR_KEY', 'FUND_NTR_CODE', 'FUND_NTR_DESP', 'FUND_NTR_DISP_SEQ_NUM']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "DPA_DMNS_FUND_NTR", mode="append")

        logger.info("write_DPA_DMNS_FUND_NTR write completed")
        
        metrics.complete()
        logger.info("Mapping M_DPA_SUMMARIZE_DMNS_FUND_NTR completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DPA_SUMMARIZE_DMNS_FUND_NTR FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_FUND_NTR", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DPA_SUMMARIZE_DMNS_FUND_NTR")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()