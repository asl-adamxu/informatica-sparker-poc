'''
* Script - M_DDS_APLY_DMNS_PMC_DSTR.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_DDS_APLY_DMNS_PMC_DSTR mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DDS_APLY_DMNS_PMC_DSTR")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DDS_APLY_DMNS_PMC_DSTR", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "DPA")

    
    try:
        logger.info("Step: read_DPA_DMNS_PMS_DSTR")
        # Reading Data From Source - read_DPA_DMNS_PMS_DSTR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_DPA_DMNS_PMS_DSTR = lib.read_sql(spark, _conn, table="DPA_DMNS_PMS_DSTR")
        
        logger.info("Step: apply_SQ_DPA_DMNS_PMS_DSTR")
        # Source Qualifier: apply_SQ_DPA_DMNS_PMS_DSTR
        df_SQ_DPA_DMNS_PMS_DSTR = df_DPA_DMNS_PMS_DSTR
        df_SQ_DPA_DMNS_PMS_DSTR = lib.sq_output(
            input_df=df_SQ_DPA_DMNS_PMS_DSTR,
            port_cols={
                'DMNS_PMS_DSTR_KEY': 'double',
                'DSTR_CODE': 'string',
                'DSTR_NAME': 'string',
                'PMS_DSTR_CODE': 'string',
                'PMS_DSTR_NAME': 'string',
                'DSTR_DISP_SEQ_NUM': 'double',
                'PMS_DSTR_DISP_SEQ_NUM': 'double',
            },
        )
        ctx.register_df("df_SQ_DPA_DMNS_PMS_DSTR", df_SQ_DPA_DMNS_PMS_DSTR)
        
        logger.info("Step: write_DDS_DMNS_PMS_DSTR")
        # Write to Target: write_DDS_DMNS_PMS_DSTR
        lib.write_target(
            spark=spark,
            df=df_SQ_DPA_DMNS_PMS_DSTR,
            conn=conn_target,
            table='DDS_DMNS_PMS_DSTR',
            mode='append',
            source_columns=[
                'DMNS_PMS_DSTR_KEY',
                'DSTR_CODE',
                'DSTR_NAME',
                'PMS_DSTR_CODE',
                'PMS_DSTR_NAME',
                'DSTR_DISP_SEQ_NUM',
                'PMS_DSTR_DISP_SEQ_NUM',
            ],
            target_columns=[
                'DMNS_PMS_DSTR_KEY',
                'DSTR_CODE',
                'DSTR_NAME',
                'PMS_DSTR_CODE',
                'PMS_DSTR_NAME',
                'DSTR_DISP_SEQ_NUM',
                'PMS_DSTR_DISP_SEQ_NUM',
            ],
            config=config,
        )

        logger.info("write_DDS_DMNS_PMS_DSTR write completed")
        
        metrics.complete()
        logger.info("Mapping M_DDS_APLY_DMNS_PMC_DSTR completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DDS_APLY_DMNS_PMC_DSTR FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DDS_APLY_DMNS_PMC_DSTR", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DDS_APLY_DMNS_PMC_DSTR")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())