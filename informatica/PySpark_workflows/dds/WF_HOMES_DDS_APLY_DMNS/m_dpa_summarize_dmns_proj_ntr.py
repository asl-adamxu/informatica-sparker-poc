'''
* Script - M_DPA_SUMMARIZE_DMNS_PROJ_NTR.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_DPA_SUMMARIZE_DMNS_PROJ_NTR mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_DPA_SUMMARIZE_DMNS_PROJ_NTR")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_PROJ_NTR", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()


    v_snsh_date = ""
    # Load mapping variables from the UTL_JOB_PARAM file (shared helper)
    _vars = lib.load_mapping_variables(config, [ "$$v_snsh_date", ], logger)
    v_snsh_date = _vars.get("v_snsh_date", v_snsh_date)
    
    try:
        logger.info("Step: apply_SQ_SOR_HOM_REF_PROJ_NTR")
        # Source Qualifier: apply_SQ_SOR_HOM_REF_PROJ_NTR
        # SQL Pushdown - executes Informatica SQ SQL on source database
        _conn = lib.get_db_config(config, "SOR")
        # Parameterize schema from connection config
        _schema = _conn.get("schema", "") or "psor"
        query = f"""select n.proj_ntr_code, nn.proj_ntr_desp, nn.disp_seq_num proj_ntr_disp_seq_num
  from SOR_HOM_REF_PROJ_NTR n, SOR_HOM_REF_PROJ_NTR_STS nn
 where n.last_rec_txn_type_code is null
 and nn.last_rec_txn_type_code is null
 and n.proj_ntr_key = nn.proj_ntr_key
 and TO_DATE ('$$v_snsh_date', 'YYYYMMDD') BETWEEN nn.bgn_date AND nn.end_date
 union 
select 'OTHR' proj_ntr_code, 'Others' proj_ntr_desp, 10000
       proj_ntr_disp_seq_num
  from dual"""
        query = query.replace("$$v_snsh_date", v_snsh_date)
        df_SQ_SOR_HOM_REF_PROJ_NTR = lib.read_sql(spark, _conn, query=query)
        df_SQ_SOR_HOM_REF_PROJ_NTR = lib.sq_output(
            input_df=df_SQ_SOR_HOM_REF_PROJ_NTR,
            port_cols={
                'PROJ_NTR_CODE': 'string',
                'PROJ_NTR_DESP': 'string',
                'DISP_SEQ_NUM': 'decimal',
            },
        )
        ctx.register_df("df_SQ_SOR_HOM_REF_PROJ_NTR", df_SQ_SOR_HOM_REF_PROJ_NTR)
        
        logger.info("Step: read_LKP_DDS_DMNS_PROJ_NTR")
        # Reading Data From Source - read_LKP_DDS_DMNS_PROJ_NTR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_LKP_DDS_DMNS_PROJ_NTR = lib.read_sql(spark, _conn, table="DDS_DMNS_PROJ_NTR")
        
        logger.info("Step: apply_LKP_DDS_DMNS_PROJ_NTR")
        # Lookup: apply_LKP_DDS_DMNS_PROJ_NTR
        # Use First Value / Use Any Value: dedup by join keys
        df_LKP_DDS_DMNS_PROJ_NTR = df_LKP_DDS_DMNS_PROJ_NTR.dropDuplicates(subset=["PROJ_NTR_CODE"])
        # Rename upstream columns to match lookup input port names before join
        _lkp_input = df_SQ_SOR_HOM_REF_PROJ_NTR
        _lkp_input = _lkp_input.withColumn("IN_PROJ_NTR_CODE", col("PROJ_NTR_CODE"))
        # Join condition: IN_PROJ_NTR_CODE=PROJ_NTR_CODE
        # Alias-based join: _main.<source_col> == _lkp.<lookup_col>
        df_lkp_merge_SQ_SOR_HOM_REF_PROJ_NTR = _lkp_input.alias("_main").join(
            broadcast(df_LKP_DDS_DMNS_PROJ_NTR).alias("_lkp"),
            (col("_main.IN_PROJ_NTR_CODE") == col("_lkp.PROJ_NTR_CODE")),
            "left"
        ).select(
            *[_lkp_input[c] for c in _lkp_input.columns],
            *[df_LKP_DDS_DMNS_PROJ_NTR[c] for c in df_LKP_DDS_DMNS_PROJ_NTR.columns if c.lower() not in [x.lower() for x in _lkp_input.columns]]
        )
        ctx.register_df("df_lkp_merge_SQ_SOR_HOM_REF_PROJ_NTR", df_lkp_merge_SQ_SOR_HOM_REF_PROJ_NTR)        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = lib.expression(
            input_df=df_lkp_merge_SQ_SOR_HOM_REF_PROJ_NTR,
            computed_columns=[
                {'name': 'IN_DISP_SEQ_NUM', 'expr': 'DISP_SEQ_NUM'},
                {'name': 'CHANGE_FLAG', 'expr': 'CASE WHEN (DMNS_PROJ_NTR_KEY IS NULL) OR CASE WHEN PROJ_NTR_CODE = IN_PROJ_NTR_CODE THEN false ELSE true END OR CASE WHEN PROJ_NTR_DESP = PROJ_NTR_DESP THEN false ELSE true END THEN 1 ELSE 0 END'},
                {'name': 'IN_PROJ_NTR_DESP', 'expr': 'PROJ_NTR_DESP'}
            ],
        )
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: apply_FIL_CHANGE")
        # Filter: apply_FIL_CHANGE
        df_FIL_CHANGE = lib.filter(
            input_df=df_EXPTRANS,
            condition='CHANGE_FLAG = 1 AND ( NOT (DMNS_PROJ_NTR_KEY IS NULL))',
        )
        ctx.register_df("df_FIL_CHANGE", df_FIL_CHANGE)

        
        metrics.complete()
        logger.info("Mapping M_DPA_SUMMARIZE_DMNS_PROJ_NTR completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_DPA_SUMMARIZE_DMNS_PROJ_NTR FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_DPA_SUMMARIZE_DMNS_PROJ_NTR", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_DPA_SUMMARIZE_DMNS_PROJ_NTR")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())