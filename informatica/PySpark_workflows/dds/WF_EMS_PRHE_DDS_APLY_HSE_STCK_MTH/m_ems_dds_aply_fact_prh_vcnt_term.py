'''
* Script - M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "DPA")

    v_REC_RLS_IND = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    try:
        _param_obj = objects.get("UTL_JOB_PARAM", {})
        if isinstance(_param_obj, dict):
            _param_path = lib._resolve_path(_param_obj.get('path'))
        with open(_param_path, "r") as _f:
            for _line in _f:
                _line = _line.strip()
                for _var in [ "$$v_REC_RLS_IND", ]:
                    if _line.startswith(_var + "="):
                        _val = _line.split("=", 1)[1]
                        _clean = _var.replace("$", "")
                        if _clean == "v_REC_RLS_IND":
                            v_REC_RLS_IND = _val
                        logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
    except Exception:
        logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_DPA_FACT_EMS_PRH_VCNT_TERM")
        # Reading Data From Source - read_DPA_FACT_EMS_PRH_VCNT_TERM
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "DPA")
        df_DPA_FACT_EMS_PRH_VCNT_TERM = lib.read_sql(spark, _conn, table="DPA_FACT_EMS_PRH_VCNT_TERM")
        
        logger.info("Step: apply_SQ_DPA_FACT_EMS_PRH_VCNT_TERM")
        # Source Qualifier: apply_SQ_DPA_FACT_EMS_PRH_VCNT_TERM
        df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM = df_DPA_FACT_EMS_PRH_VCNT_TERM
        df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM = lib.sq_output(
            input_df=df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM,
            port_cols={
                'TIME_DMNS_KEY': 'decimal',
                'VCNT_FLAT_TYPE_DMNS_KEY': 'decimal',
                'BLK_SCD_KEY': 'decimal',
                'DSTR_BRD_DSTR_DMNS_KEY': 'decimal',
                'DSTR_CHC_DSTR_SCD_KEY': 'decimal',
                'FLAT_TYPE_DMNS_KEY': 'decimal',
                'UNIT_SIZE_DMNS_KEY': 'decimal',
                'MAX_UNIT_HEAD_CNT': 'decimal',
                'MIN_UNIT_HEAD_CNT': 'decimal',
                'UNIT_ADVS_ENV_IND': 'string',
                'STA_VCNT_UNIT_ALCT_CNT': 'decimal',
                'TOT_UNIT_MTH_RENT_AMT': 'decimal',
            },
        )
        ctx.register_df("df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM", df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM)
        
        logger.info("Step: apply_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1")
        # Source Qualifier: apply_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1
        df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1 = df_DPA_FACT_EMS_PRH_VCNT_TERM
        df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1 = lib.sq_output(
            input_df=df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1,
            port_cols={
                'TIME_DMNS_KEY': 'decimal',
                'VCNT_FLAT_TYPE_DMNS_KEY': 'decimal',
                'BLK_SCD_KEY': 'decimal',
                'DSTR_BRD_DSTR_DMNS_KEY': 'decimal',
                'DSTR_CHC_DSTR_SCD_KEY': 'decimal',
                'FLAT_TYPE_DMNS_KEY': 'decimal',
                'UNIT_SIZE_DMNS_KEY': 'decimal',
                'MAX_UNIT_HEAD_CNT': 'decimal',
                'MIN_UNIT_HEAD_CNT': 'decimal',
                'UNIT_ADVS_ENV_IND': 'string',
                'STA_VCNT_UNIT_ALCT_CNT': 'decimal',
                'TOT_UNIT_MTH_RENT_AMT': 'decimal',
                'REC_RLS_IND': 'string',
            },
        )
        ctx.register_df("df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1", df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1)
        
        logger.info("Step: apply_AGGTRANS")
        # Aggregator: apply_AGGTRANS
        # Select only mapped upstream columns with correct port names
        _agg_input = df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM.select(
            col("TIME_DMNS_KEY"),
            col("VCNT_FLAT_TYPE_DMNS_KEY"),
            col("BLK_SCD_KEY"),
            col("DSTR_BRD_DSTR_DMNS_KEY"),
            col("DSTR_CHC_DSTR_SCD_KEY"),
            col("FLAT_TYPE_DMNS_KEY"),
            col("UNIT_SIZE_DMNS_KEY"),
            col("MAX_UNIT_HEAD_CNT"),
            col("MIN_UNIT_HEAD_CNT"),
            col("UNIT_ADVS_ENV_IND"),
            col("STA_VCNT_UNIT_ALCT_CNT"),
            col("TOT_UNIT_MTH_RENT_AMT")        )
        df_AGGTRANS = _agg_input.groupBy("TIME_DMNS_KEY", "VCNT_FLAT_TYPE_DMNS_KEY", "BLK_SCD_KEY", "DSTR_BRD_DSTR_DMNS_KEY", "DSTR_CHC_DSTR_SCD_KEY", "FLAT_TYPE_DMNS_KEY", "UNIT_SIZE_DMNS_KEY", "MAX_UNIT_HEAD_CNT", "MIN_UNIT_HEAD_CNT", "UNIT_ADVS_ENV_IND")
        df_AGGTRANS = df_AGGTRANS.agg(
            sum("STA_VCNT_UNIT_ALCT_CNT").alias("STA_VCNT_UNIT_ALCT_CNT_out"),
            sum("TOT_UNIT_MTH_RENT_AMT").alias("TOT_UNIT_MTH_RENT_AMT_out")
        )
        ctx.register_df("df_AGGTRANS", df_AGGTRANS)
        
        logger.info("Step: apply_UPDTRANS")
        # Update Strategy: apply_UPDTRANS
        # Strategy: DD_DELETE
        df_UPDTRANS = lib.update_strategy(
            input_df=df_SQ_DPA_FACT_EMS_PRH_VCNT_TERM1,
        )
        ctx.register_df("df_UPDTRANS", df_UPDTRANS)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = lib.expression(
            input_df=df_AGGTRANS,
            computed_columns=[
                {'name': 'STA_VCNT_UNIT_ALCT_CNT', 'expr': 'STA_VCNT_UNIT_ALCT_CNT_out'},
                {'name': 'TOT_UNIT_MTH_RENT_AMT', 'expr': 'TOT_UNIT_MTH_RENT_AMT_out'},
                {'name': 'REC_RLS_IND', 'expr': "'$$v_REC_RLS_IND'"}
            ],
            substitutions={'$$v_REC_RLS_IND': v_REC_RLS_IND},
        )
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: write_DDS_FACT_EMS_PRH_VCNT_TERM1")
        # Write to Target: write_DDS_FACT_EMS_PRH_VCNT_TERM1
        lib.write_target(
            spark=spark,
            df=df_UPDTRANS,
            conn=conn_target,
            table='DDS_FACT_EMS_PRH_VCNT_TERM',
            mode='append',
            source_columns=[
                'TIME_DMNS_KEY',
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
                None,
            ],
            target_columns=[
                'TIME_DMNS_KEY',
                'VCNT_FLAT_TYPE_DMNS_KEY',
                'BLK_SCD_KEY',
                'DSTR_BRD_DSTR_DMNS_KEY',
                'DSTR_CHC_DSTR_SCD_KEY',
                'FLAT_TYPE_DMNS_KEY',
                'UNIT_SIZE_DMNS_KEY',
                'MAX_UNIT_HEAD_CNT',
                'MIN_UNIT_HEAD_CNT',
                'UNIT_ADVS_ENV_IND',
                'STA_VCNT_UNIT_ALCT_CNT',
                'TOT_UNIT_MTH_RENT_AMT',
                'REC_RLS_IND',
            ],
            is_delete=True,
            delete_keys=['TIME_DMNS_KEY', 'VCNT_FLAT_TYPE_DMNS_KEY', 'BLK_SCD_KEY', 'DSTR_BRD_DSTR_DMNS_KEY', 'DSTR_CHC_DSTR_SCD_KEY', 'FLAT_TYPE_DMNS_KEY', 'UNIT_SIZE_DMNS_KEY', 'MAX_UNIT_HEAD_CNT', 'MIN_UNIT_HEAD_CNT', 'UNIT_ADVS_ENV_IND'],
            static_dd='DD_DELETE',
            config=config,
        )

        logger.info("write_DDS_FACT_EMS_PRH_VCNT_TERM1 write completed")
        logger.info("Step: write_DDS_FACT_EMS_PRH_VCNT_TERM")
        # Write to Target: write_DDS_FACT_EMS_PRH_VCNT_TERM
        lib.write_target(
            spark=spark,
            df=df_EXPTRANS,
            conn=conn_target,
            table='DDS_FACT_EMS_PRH_VCNT_TERM',
            mode='append',
            source_columns=[
                'TIME_DMNS_KEY',
                'VCNT_FLAT_TYPE_DMNS_KEY',
                'BLK_SCD_KEY',
                'DSTR_BRD_DSTR_DMNS_KEY',
                'DSTR_CHC_DSTR_SCD_KEY',
                'FLAT_TYPE_DMNS_KEY',
                'UNIT_SIZE_DMNS_KEY',
                'MAX_UNIT_HEAD_CNT',
                'MIN_UNIT_HEAD_CNT',
                'UNIT_ADVS_ENV_IND',
                'STA_VCNT_UNIT_ALCT_CNT',
                'TOT_UNIT_MTH_RENT_AMT',
                'REC_RLS_IND',
            ],
            target_columns=[
                'TIME_DMNS_KEY',
                'VCNT_FLAT_TYPE_DMNS_KEY',
                'BLK_SCD_KEY',
                'DSTR_BRD_DSTR_DMNS_KEY',
                'DSTR_CHC_DSTR_SCD_KEY',
                'FLAT_TYPE_DMNS_KEY',
                'UNIT_SIZE_DMNS_KEY',
                'MAX_UNIT_HEAD_CNT',
                'MIN_UNIT_HEAD_CNT',
                'UNIT_ADVS_ENV_IND',
                'STA_VCNT_UNIT_ALCT_CNT',
                'TOT_UNIT_MTH_RENT_AMT',
                'REC_RLS_IND',
            ],
            config=config,
        )

        logger.info("write_DDS_FACT_EMS_PRH_VCNT_TERM write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_DDS_APLY_FACT_PRH_VCNT_TERM")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())