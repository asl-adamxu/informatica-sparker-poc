'''
* Script - M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "EMS")
    conn_target = lib.get_db_config(config, "EMS")

    v_load_start_ds = ""
    v_load_end_ds = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    if job_params:
        for _var, _val in job_params.items():
            _clean = _var.replace("$", "")
            if _clean in [ "v_load_start_ds",  "v_load_end_ds", ]:
                if _clean == "v_load_start_ds":
                    v_load_start_ds = _val
                if _clean == "v_load_end_ds":
                    v_load_end_ds = _val
                logger.info("Loaded %s=%s from job_params", _var, _val)
    else:
        try:
            _param_obj = objects.get("UTL_JOB_PARAM", {})
            if isinstance(_param_obj, dict):
                _param_path = lib._resolve_path(_param_obj.get('path', '/tmp/UTL_JOB_PARAM'))
            with open(_param_path, "r") as _f:
                for _line in _f:
                    _line = _line.strip()
                    for _var in [ "$$v_load_start_ds",  "$$v_load_end_ds", ]:
                        if _line.startswith(_var + "="):
                            _val = _line.split("=", 1)[1]
                            _clean = _var.replace("$", "")
                            if _clean == "v_load_start_ds":
                                v_load_start_ds = _val
                            if _clean == "v_load_end_ds":
                                v_load_end_ds = _val
                            logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
        except Exception:
            logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_HSP_TNT_INCM_DCLR")
        # Reading Data From Source - read_HSP_TNT_INCM_DCLR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "EMS")
        df_src_1 = lib.read_sql(spark, _conn, table="HSP_TNT_INCM_DCLR")
        
        logger.info("Step: apply_SQ_HSP_TNT_INCM_DCLR")
        # Source Qualifier: apply_SQ_HSP_TNT_INCM_DCLR
        df_sq_2 = df_src_1
        df_sq_2 = df_sq_2.filter(expr("HSP_TNT_INCM_DCLR.LAST_REC_TXN_DATE > to_date(''$$v_load_start_ds'','yyyy-mm-dd hh24:mi:ss') AND HSP_TNT_INCM_DCLR.LAST_REC_TXN_DATE <= to_date(''$$v_load_end_ds'','yyyy-mm-dd hh24:mi:ss')"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("HSP_REC_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY", "HSP_RVW_CYCL_YEAR_MTH", "HSP_STS_CODE", "HSP_IDF_STS_IND", "HSP_REF_PHRM_IND", "HSP_OPT_NOT_DCLR_IND", "HSP_FRST_PRN_IND", "FMLY_SIZE_NUM", "HSHLD_INCM_AMT", "HSP_RENT_FCTR_CODE", "HSP_RENT_BGN_DATE", "HSP_RENT_END_DATE", "HSP_RENT_CHNG_RSN_CODE", "HSP_RENT_CHNG_RSN_TEXT", "HSP_RMK_TEXT", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID", "RENT_RVW_CATG_CODE", "RENT_RVW_CATG_BGN_DATE", "HSP_CASE_TYPE_CODE", "HSP_EXTRC_DATE", "IMG_DOC_KEY", "IMG_MINS_KEY", "ORIG_HSE_SRVC_APLY_KEY", "EXTR_RENT_FRST_PRN_IND", "EXTR_RENT_DTL_FRST_PRN_IND")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_exp_3 = df_sq_2
        df_exp_3 = df_exp_3.withColumn("HSP_REC_KEY_OUT", expr("HSP_REC_KEY"))
        df_exp_3 = df_exp_3.withColumn("CUST_KEY_OUT", expr("ltrim(rtrim(CUST_KEY))"))
        df_exp_3 = df_exp_3.withColumn("HSE_SRVC_APLY_KEY_OUT", expr("ltrim(rtrim(HSE_SRVC_APLY_KEY))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_KEY_OUT", expr("ltrim(rtrim(HSE_UNIT_KEY))"))
        df_exp_3 = df_exp_3.withColumn("HSP_RVW_YEAR_OUT", expr("HSP_RVW_CYCL_YEAR_MTH"))
        df_exp_3 = df_exp_3.withColumn("HSP_STS_CODE_OUT", expr("ltrim(rtrim(HSP_STS_CODE))"))
        df_exp_3 = df_exp_3.withColumn("HSP_IDF_STS_IND_OUT", expr("ltrim(rtrim(HSP_IDF_STS_IND))"))
        df_exp_3 = df_exp_3.withColumn("HSP_REF_PHRM_IND_OUT", expr("ltrim(rtrim(HSP_REF_PHRM_IND))"))
        df_exp_3 = df_exp_3.withColumn("HSP_OPT_NOT_DCLR_IND_OUT", expr("ltrim(rtrim(HSP_OPT_NOT_DCLR_IND))"))
        df_exp_3 = df_exp_3.withColumn("HSP_FRST_PRN_IND_OUT", expr("ltrim(rtrim(HSP_FRST_PRN_IND))"))
        df_exp_3 = df_exp_3.withColumn("FMLY_SIZE_NUM_OUT", expr("FMLY_SIZE_NUM"))
        df_exp_3 = df_exp_3.withColumn("HSHLD_INCM_AMT_OUT", expr("HSHLD_INCM_AMT"))
        df_exp_3 = df_exp_3.withColumn("HSP_RENT_FCTR_CODE_OUT", expr("HSP_RENT_FCTR_CODE"))
        df_exp_3 = df_exp_3.withColumn("HSP_RENT_BGN_DATE_OUT", expr("HSP_RENT_BGN_DATE"))
        df_exp_3 = df_exp_3.withColumn("HSP_RENT_END_DATE_OUT", expr("HSP_RENT_END_DATE"))
        df_exp_3 = df_exp_3.withColumn("HSP_RENT_CHNG_RSN_CODE_OUT", expr("ltrim(rtrim(HSP_RENT_CHNG_RSN_CODE))"))
        df_exp_3 = df_exp_3.withColumn("HSP_RENT_CHNG_RSN_TEXT_OUT", expr("ltrim(rtrim(HSP_RENT_CHNG_RSN_TEXT))"))
        df_exp_3 = df_exp_3.withColumn("HSP_RMK_TEXT_OUT", expr("ltrim(rtrim(HSP_RMK_TEXT))"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_TYPE_CODE_OUT", expr("ltrim(rtrim(LAST_REC_TXN_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_DATE_OUT", expr("LAST_REC_TXN_DATE"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_USER_ID_OUT", expr("ltrim(rtrim(LAST_REC_TXN_USER_ID))"))
        df_exp_3 = df_exp_3.withColumn("RENT_RVW_CATG_CODE_OUT", expr("ltrim(rtrim(RENT_RVW_CATG_CODE))"))
        df_exp_3 = df_exp_3.withColumn("RENT_RVW_CATG_BGN_DATE_OUT", expr("RENT_RVW_CATG_BGN_DATE"))
        df_exp_3 = df_exp_3.withColumn("HSP_CASE_TYPE_CODE_OUT", expr("ltrim(rtrim(HSP_CASE_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("HSP_EXTRC_DATE_OUT", expr("HSP_EXTRC_DATE"))
        df_exp_3 = df_exp_3.withColumn("DUMMY", expr("'|'"))
        df_exp_3 = df_exp_3.withColumn("IMG_DOC_KEY_OUT", expr("ltrim(rtrim(IMG_DOC_KEY))"))
        df_exp_3 = df_exp_3.withColumn("IMG_MINS_KEY_OUT", expr("ltrim(rtrim(IMG_MINS_KEY))"))
        df_exp_3 = df_exp_3.withColumn("ORIG_HSE_SRVC_APLY_KEY_OUT", expr("ltrim(rtrim(ORIG_HSE_SRVC_APLY_KEY))"))
        df_exp_3 = df_exp_3.withColumn("EXTR_RENT_FRST_PRN_IND_OUT", expr("ltrim(rtrim(EXTR_RENT_FRST_PRN_IND))"))
        df_exp_3 = df_exp_3.withColumn("EXTR_RENT_DTL_FRST_PRN_IND_OUT", expr("ltrim(rtrim(EXTR_RENT_DTL_FRST_PRN_IND))"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        # Select only mapping output ports (prevents column leakage)
        df_exp_3 = df_exp_3.select("HSP_REC_KEY_OUT", "CUST_KEY_OUT", "HSE_SRVC_APLY_KEY_OUT", "HSE_UNIT_KEY_OUT", "HSP_RVW_YEAR_OUT", "HSP_STS_CODE_OUT", "HSP_IDF_STS_IND_OUT", "HSP_REF_PHRM_IND_OUT", "HSP_OPT_NOT_DCLR_IND_OUT", "HSP_FRST_PRN_IND_OUT", "FMLY_SIZE_NUM_OUT", "HSHLD_INCM_AMT_OUT", "HSP_RENT_FCTR_CODE_OUT", "HSP_RENT_BGN_DATE_OUT", "HSP_RENT_END_DATE_OUT", "HSP_RENT_CHNG_RSN_CODE_OUT", "HSP_RENT_CHNG_RSN_TEXT_OUT", "HSP_RMK_TEXT_OUT", "LAST_REC_TXN_TYPE_CODE_OUT", "LAST_REC_TXN_DATE_OUT", "LAST_REC_TXN_USER_ID_OUT", "RENT_RVW_CATG_CODE_OUT", "RENT_RVW_CATG_BGN_DATE_OUT", "HSP_CASE_TYPE_CODE_OUT", "HSP_EXTRC_DATE_OUT", "DUMMY", "IMG_DOC_KEY_OUT", "IMG_MINS_KEY_OUT", "ORIG_HSE_SRVC_APLY_KEY_OUT", "EXTR_RENT_FRST_PRN_IND_OUT", "EXTR_RENT_DTL_FRST_PRN_IND_OUT")
        ctx.register_df("df_exp_3", df_exp_3)
        
        logger.info("Step: write_EMS_HSP_TNT_INCM_DCLR1")
        # Write to Target: write_EMS_HSP_TNT_INCM_DCLR1
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_idf_sts_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_idf_sts_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_ref_phrm_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_ref_phrm_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_opt_not_dclr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_opt_not_dclr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_frst_prn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_frst_prn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_rent_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "hsp_rent_end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "hsp_rent_chng_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_chng_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_rent_chng_rsn_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_chng_rsn_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_rvw_catg_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_rvw_catg_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_rvw_catg_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_rvw_catg_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "hsp_case_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_case_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_extrc_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_extrc_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "img_doc_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "img_doc_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "img_mins_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "img_mins_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "orig_hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "orig_hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "extr_rent_dtl_frst_prn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "extr_rent_dtl_frst_prn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "extr_rent_frst_prn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "extr_rent_frst_prn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CUST_KEY": "CUST_KEY_OUT", "DUMMY": "DUMMY", "EXTR_RENT_DTL_FRST_PRN_IND": "EXTR_RENT_DTL_FRST_PRN_IND_OUT", "EXTR_RENT_FRST_PRN_IND": "EXTR_RENT_FRST_PRN_IND_OUT", "FMLY_SIZE_NUM": "FMLY_SIZE_NUM_OUT", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY_OUT", "HSE_UNIT_KEY": "HSE_UNIT_KEY_OUT", "HSHLD_INCM_AMT": "HSHLD_INCM_AMT_OUT", "HSP_CASE_TYPE_CODE": "HSP_CASE_TYPE_CODE_OUT", "HSP_EXTRC_DATE": "HSP_EXTRC_DATE_OUT", "HSP_FRST_PRN_IND": "HSP_FRST_PRN_IND_OUT", "HSP_IDF_STS_IND": "HSP_IDF_STS_IND_OUT", "HSP_OPT_NOT_DCLR_IND": "HSP_OPT_NOT_DCLR_IND_OUT", "HSP_REC_KEY": "HSP_REC_KEY_OUT", "HSP_REF_PHRM_IND": "HSP_REF_PHRM_IND_OUT", "HSP_RENT_BGN_DATE": "HSP_RENT_BGN_DATE_OUT", "HSP_RENT_CHNG_RSN_CODE": "HSP_RENT_CHNG_RSN_CODE_OUT", "HSP_RENT_CHNG_RSN_TEXT": "HSP_RENT_CHNG_RSN_TEXT_OUT", "HSP_RENT_END_DATE": "HSP_RENT_END_DATE_OUT", "HSP_RENT_FCTR_CODE": "HSP_RENT_FCTR_CODE_OUT", "HSP_RMK_TEXT": "HSP_RMK_TEXT_OUT", "HSP_RVW_YEAR": "HSP_RVW_YEAR_OUT", "HSP_STS_CODE": "HSP_STS_CODE_OUT", "IMG_DOC_KEY": "IMG_DOC_KEY_OUT", "IMG_MINS_KEY": "IMG_MINS_KEY_OUT", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE_OUT", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE_OUT", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID_OUT", "ORIG_HSE_SRVC_APLY_KEY": "ORIG_HSE_SRVC_APLY_KEY_OUT", "RENT_RVW_CATG_BGN_DATE": "RENT_RVW_CATG_BGN_DATE_OUT", "RENT_RVW_CATG_CODE": "RENT_RVW_CATG_CODE_OUT"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['HSP_REC_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'HSE_UNIT_KEY', 'HSP_RVW_YEAR', 'HSP_STS_CODE', 'HSP_IDF_STS_IND', 'HSP_REF_PHRM_IND', 'HSP_OPT_NOT_DCLR_IND', 'HSP_FRST_PRN_IND', 'FMLY_SIZE_NUM', 'HSHLD_INCM_AMT', 'HSP_RENT_FCTR_CODE', 'HSP_RENT_BGN_DATE', 'HSP_RENT_END_DATE', 'HSP_RENT_CHNG_RSN_CODE', 'HSP_RENT_CHNG_RSN_TEXT', 'HSP_RMK_TEXT', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID', 'RENT_RVW_CATG_CODE', 'RENT_RVW_CATG_BGN_DATE', 'HSP_CASE_TYPE_CODE', 'HSP_EXTRC_DATE', 'IMG_DOC_KEY', 'IMG_MINS_KEY', 'ORIG_HSE_SRVC_APLY_KEY', 'EXTR_RENT_DTL_FRST_PRN_IND', 'EXTR_RENT_FRST_PRN_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_HSP_TNT_INCM_DCLR", mode="append")

        logger.info("write_EMS_HSP_TNT_INCM_DCLR1 write completed")
        logger.info("Step: write_EMS_HSP_TNT_INCM_DCLR")
        # Write to Target: write_EMS_HSP_TNT_INCM_DCLR
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_idf_sts_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_idf_sts_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_ref_phrm_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_ref_phrm_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_opt_not_dclr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_opt_not_dclr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_frst_prn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_frst_prn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_rent_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "hsp_rent_end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "hsp_rent_chng_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_chng_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_rent_chng_rsn_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rent_chng_rsn_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_rvw_catg_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_rvw_catg_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_rvw_catg_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_rvw_catg_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "hsp_case_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_case_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hsp_extrc_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hsp_extrc_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "img_doc_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "img_doc_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "img_mins_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "img_mins_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "orig_hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "orig_hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "extr_rent_dtl_frst_prn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "extr_rent_dtl_frst_prn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "extr_rent_frst_prn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "extr_rent_frst_prn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CUST_KEY": "CUST_KEY_OUT", "EXTR_RENT_DTL_FRST_PRN_IND": "EXTR_RENT_DTL_FRST_PRN_IND_OUT", "EXTR_RENT_FRST_PRN_IND": "EXTR_RENT_FRST_PRN_IND_OUT", "FMLY_SIZE_NUM": "FMLY_SIZE_NUM_OUT", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY_OUT", "HSE_UNIT_KEY": "HSE_UNIT_KEY_OUT", "HSHLD_INCM_AMT": "HSHLD_INCM_AMT_OUT", "HSP_CASE_TYPE_CODE": "HSP_CASE_TYPE_CODE_OUT", "HSP_EXTRC_DATE": "HSP_EXTRC_DATE_OUT", "HSP_FRST_PRN_IND": "HSP_FRST_PRN_IND_OUT", "HSP_IDF_STS_IND": "HSP_IDF_STS_IND_OUT", "HSP_OPT_NOT_DCLR_IND": "HSP_OPT_NOT_DCLR_IND_OUT", "HSP_REC_KEY": "HSP_REC_KEY_OUT", "HSP_REF_PHRM_IND": "HSP_REF_PHRM_IND_OUT", "HSP_RENT_BGN_DATE": "HSP_RENT_BGN_DATE_OUT", "HSP_RENT_CHNG_RSN_CODE": "HSP_RENT_CHNG_RSN_CODE_OUT", "HSP_RENT_CHNG_RSN_TEXT": "HSP_RENT_CHNG_RSN_TEXT_OUT", "HSP_RENT_END_DATE": "HSP_RENT_END_DATE_OUT", "HSP_RENT_FCTR_CODE": "HSP_RENT_FCTR_CODE_OUT", "HSP_RMK_TEXT": "HSP_RMK_TEXT_OUT", "HSP_RVW_YEAR": "HSP_RVW_YEAR_OUT", "HSP_STS_CODE": "HSP_STS_CODE_OUT", "IMG_DOC_KEY": "IMG_DOC_KEY_OUT", "IMG_MINS_KEY": "IMG_MINS_KEY_OUT", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE_OUT", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE_OUT", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID_OUT", "ORIG_HSE_SRVC_APLY_KEY": "ORIG_HSE_SRVC_APLY_KEY_OUT", "RENT_RVW_CATG_BGN_DATE": "RENT_RVW_CATG_BGN_DATE_OUT", "RENT_RVW_CATG_CODE": "RENT_RVW_CATG_CODE_OUT"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['HSP_REC_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'HSE_UNIT_KEY', 'HSP_RVW_YEAR', 'HSP_STS_CODE', 'HSP_IDF_STS_IND', 'HSP_REF_PHRM_IND', 'HSP_OPT_NOT_DCLR_IND', 'HSP_FRST_PRN_IND', 'FMLY_SIZE_NUM', 'HSHLD_INCM_AMT', 'HSP_RENT_FCTR_CODE', 'HSP_RENT_BGN_DATE', 'HSP_RENT_END_DATE', 'HSP_RENT_CHNG_RSN_CODE', 'HSP_RENT_CHNG_RSN_TEXT', 'HSP_RMK_TEXT', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID', 'RENT_RVW_CATG_CODE', 'RENT_RVW_CATG_BGN_DATE', 'HSP_CASE_TYPE_CODE', 'HSP_EXTRC_DATE', 'IMG_DOC_KEY', 'IMG_MINS_KEY', 'ORIG_HSE_SRVC_APLY_KEY', 'EXTR_RENT_DTL_FRST_PRN_IND', 'EXTR_RENT_FRST_PRN_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_HSP_TNT_INCM_DCLR", mode="append")

        logger.info("write_EMS_HSP_TNT_INCM_DCLR write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SSAL1_EXTRACT_HSP_TNT_INCM_DCLR")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()