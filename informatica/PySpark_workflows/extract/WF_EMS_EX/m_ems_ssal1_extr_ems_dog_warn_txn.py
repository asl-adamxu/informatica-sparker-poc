'''
* Script - M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "EMS")
    conn_target = lib.get_db_config(config, "EMS")

    v_load_start_ds = ""
    v_load_end_ds = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    if job_params:
        for _var, _val in job_params.items():
            _clean = _var.replace("$", "")
            if _clean in [ "v_load_start_ds",  "v_load_end_ds", ]:
                if _clean == "v_load_start_ds":
                    v_load_start_ds = _val
                if _clean == "v_load_end_ds":
                    v_load_end_ds = _val
                logger.info("Loaded %s=%s from job_params", _var, _val)
    else:
        try:
            _param_obj = objects.get("UTL_JOB_PARAM", {})
            if isinstance(_param_obj, dict):
                _param_path = lib._resolve_path(_param_obj.get('path', '/tmp/UTL_JOB_PARAM'))
            with open(_param_path, "r") as _f:
                for _line in _f:
                    _line = _line.strip()
                    for _var in [ "$$v_load_start_ds",  "$$v_load_end_ds", ]:
                        if _line.startswith(_var + "="):
                            _val = _line.split("=", 1)[1]
                            _clean = _var.replace("$", "")
                            if _clean == "v_load_start_ds":
                                v_load_start_ds = _val
                            if _clean == "v_load_end_ds":
                                v_load_end_ds = _val
                            logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
        except Exception:
            logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_DOG_WARN_TXN")
        # Reading Data From Source - read_DOG_WARN_TXN
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "EMS")
        df_src_1 = lib.read_sql(spark, _conn, table="DOG_WARN_TXN")
        
        logger.info("Step: apply_SQ_DOG_WARN_TXN")
        # Source Qualifier: apply_SQ_DOG_WARN_TXN
        df_sq_2 = df_src_1
        df_sq_2 = df_sq_2.filter(expr("DOG_WARN_TXN.LAST_REC_TXN_DATE > to_date(''$$v_load_start_ds'','yyyy-mm-dd hh24:mi:ss') AND DOG_WARN_TXN.LAST_REC_TXN_DATE <= to_date(''$$v_load_end_ds'','yyyy-mm-dd hh24:mi:ss')"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("DOG_WARN_TXN_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "OFNDR_ID_TYPE_CODE", "OFNDR_ID_NUM", "INCDT_DATE", "WARN_LTR_LANG_CODE", "INCDT_LOC_OTHR_TEXT", "OFNDR_NAME", "WARN_CODE", "WARN_FILE_REF_TEXT", "WARN_ISS_OFFC_PHONE_NUM", "WARN_ISS_OFFC_FAX_NUM", "RESP_OFCR_NAME", "RESP_OFCR_PHONE_NUM", "WARN_APRV_OFCR_TYPE_CODE", "WARN_APRV_OFCR_POST_TEXT", "WARN_APRV_OFCR_NAME", "WARN_LTR_ATTN_NAME", "WARN_ISS_OFCR_TYPE_CODE", "WARN_ISS_OFCR_NAME", "WARN_DEL_RSN_CODE", "WARN_DEL_RSN_RMK_TEXT", "WARN_TXN_STS_CODE", "WARN_TXN_CRE_DATE", "WARN_TXN_CRE_USER_ID", "WARN_LTR_MAIL_ADDR_1", "WARN_LTR_MAIL_ADDR_2", "WARN_LTR_MAIL_ADDR_3", "WARN_LTR_MAIL_ADDR_4", "WARN_LTR_MAIL_ADDR_5", "FLUP_CMPLT_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: apply_EXP_L1")
        # Expression: apply_EXP_L1
        df_exp_3 = df_sq_2
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["DOG_WARN_TXN_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "OFNDR_ID_TYPE_CODE", "OFNDR_ID_NUM", "INCDT_DATE", "WARN_LTR_LANG_CODE", "INCDT_LOC_OTHR_TEXT", "OFNDR_NAME", "WARN_CODE", "WARN_FILE_REF_TEXT", "WARN_ISS_OFFC_PHONE_NUM", "WARN_ISS_OFFC_FAX_NUM", "RESP_OFCR_NAME", "RESP_OFCR_PHONE_NUM", "WARN_APRV_OFCR_TYPE_CODE", "WARN_APRV_OFCR_POST_TEXT", "WARN_APRV_OFCR_NAME", "WARN_LTR_ATTN_NAME", "WARN_ISS_OFCR_TYPE_CODE", "WARN_ISS_OFCR_NAME", "WARN_DEL_RSN_CODE", "WARN_DEL_RSN_RMK_TEXT", "WARN_TXN_STS_CODE", "WARN_TXN_CRE_DATE", "WARN_TXN_CRE_USER_ID", "WARN_LTR_MAIL_ADDR_1", "WARN_LTR_MAIL_ADDR_2", "WARN_LTR_MAIL_ADDR_3", "WARN_LTR_MAIL_ADDR_4", "WARN_LTR_MAIL_ADDR_5", "FLUP_CMPLT_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID"]:
            if _col not in df_exp_3.columns:
                df_exp_3 = df_exp_3.withColumn(_col, lit(None))
        # Select only mapping output ports (prevents column leakage)
        df_exp_3 = df_exp_3.select("DOG_WARN_TXN_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "OFNDR_ID_TYPE_CODE", "OFNDR_ID_NUM", "INCDT_DATE", "WARN_LTR_LANG_CODE", "INCDT_LOC_OTHR_TEXT", "OFNDR_NAME", "WARN_CODE", "WARN_FILE_REF_TEXT", "WARN_ISS_OFFC_PHONE_NUM", "WARN_ISS_OFFC_FAX_NUM", "RESP_OFCR_NAME", "RESP_OFCR_PHONE_NUM", "WARN_APRV_OFCR_TYPE_CODE", "WARN_APRV_OFCR_POST_TEXT", "WARN_APRV_OFCR_NAME", "WARN_LTR_ATTN_NAME", "WARN_ISS_OFCR_TYPE_CODE", "WARN_ISS_OFCR_NAME", "WARN_DEL_RSN_CODE", "WARN_DEL_RSN_RMK_TEXT", "WARN_TXN_STS_CODE", "WARN_TXN_CRE_DATE", "WARN_TXN_CRE_USER_ID", "WARN_LTR_MAIL_ADDR_1", "WARN_LTR_MAIL_ADDR_2", "WARN_LTR_MAIL_ADDR_3", "WARN_LTR_MAIL_ADDR_4", "WARN_LTR_MAIL_ADDR_5", "FLUP_CMPLT_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID")
        ctx.register_df("df_exp_3", df_exp_3)
        
        logger.info("Step: write_EMS_DOG_WARN_TXN")
        # Write to Target: write_EMS_DOG_WARN_TXN
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofndr_id_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofndr_id_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofndr_id_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofndr_id_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incdt_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incdt_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "warn_ltr_lang_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_lang_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incdt_loc_othr_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incdt_loc_othr_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofndr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofndr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_file_ref_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_file_ref_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_offc_phone_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_offc_phone_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_offc_fax_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_offc_fax_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "resp_ofcr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "resp_ofcr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "resp_ofcr_phone_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "resp_ofcr_phone_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_aprv_ofcr_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_aprv_ofcr_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_aprv_ofcr_post_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_aprv_ofcr_post_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_aprv_ofcr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_aprv_ofcr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_attn_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_attn_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_ofcr_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_ofcr_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_ofcr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_ofcr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_del_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_del_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_del_rsn_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_del_rsn_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_txn_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_txn_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_txn_cre_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_txn_cre_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "warn_txn_cre_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_txn_cre_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_3" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_3":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_4" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_4":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_5" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_5":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "flup_cmplt_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flup_cmplt_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(TimestampType()))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CUST_KEY": "CUST_KEY", "DOG_WARN_TXN_KEY": "DOG_WARN_TXN_KEY", "FLUP_CMPLT_DATE": "FLUP_CMPLT_DATE", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "INCDT_DATE": "INCDT_DATE", "INCDT_LOC_OTHR_TEXT": "INCDT_LOC_OTHR_TEXT", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID", "OFNDR_ID_NUM": "OFNDR_ID_NUM", "OFNDR_ID_TYPE_CODE": "OFNDR_ID_TYPE_CODE", "OFNDR_NAME": "OFNDR_NAME", "RESP_OFCR_NAME": "RESP_OFCR_NAME", "RESP_OFCR_PHONE_NUM": "RESP_OFCR_PHONE_NUM", "WARN_APRV_OFCR_NAME": "WARN_APRV_OFCR_NAME", "WARN_APRV_OFCR_POST_TEXT": "WARN_APRV_OFCR_POST_TEXT", "WARN_APRV_OFCR_TYPE_CODE": "WARN_APRV_OFCR_TYPE_CODE", "WARN_CODE": "WARN_CODE", "WARN_DEL_RSN_CODE": "WARN_DEL_RSN_CODE", "WARN_DEL_RSN_RMK_TEXT": "WARN_DEL_RSN_RMK_TEXT", "WARN_FILE_REF_TEXT": "WARN_FILE_REF_TEXT", "WARN_ISS_OFCR_NAME": "WARN_ISS_OFCR_NAME", "WARN_ISS_OFCR_TYPE_CODE": "WARN_ISS_OFCR_TYPE_CODE", "WARN_ISS_OFFC_FAX_NUM": "WARN_ISS_OFFC_FAX_NUM", "WARN_ISS_OFFC_PHONE_NUM": "WARN_ISS_OFFC_PHONE_NUM", "WARN_LTR_ATTN_NAME": "WARN_LTR_ATTN_NAME", "WARN_LTR_LANG_CODE": "WARN_LTR_LANG_CODE", "WARN_LTR_MAIL_ADDR_1": "WARN_LTR_MAIL_ADDR_1", "WARN_LTR_MAIL_ADDR_2": "WARN_LTR_MAIL_ADDR_2", "WARN_LTR_MAIL_ADDR_3": "WARN_LTR_MAIL_ADDR_3", "WARN_LTR_MAIL_ADDR_4": "WARN_LTR_MAIL_ADDR_4", "WARN_LTR_MAIL_ADDR_5": "WARN_LTR_MAIL_ADDR_5", "WARN_TXN_CRE_DATE": "WARN_TXN_CRE_DATE", "WARN_TXN_CRE_USER_ID": "WARN_TXN_CRE_USER_ID", "WARN_TXN_STS_CODE": "WARN_TXN_STS_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['DOG_WARN_TXN_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'OFNDR_ID_TYPE_CODE', 'OFNDR_ID_NUM', 'INCDT_DATE', 'WARN_LTR_LANG_CODE', 'INCDT_LOC_OTHR_TEXT', 'OFNDR_NAME', 'WARN_CODE', 'WARN_FILE_REF_TEXT', 'WARN_ISS_OFFC_PHONE_NUM', 'WARN_ISS_OFFC_FAX_NUM', 'RESP_OFCR_NAME', 'RESP_OFCR_PHONE_NUM', 'WARN_APRV_OFCR_TYPE_CODE', 'WARN_APRV_OFCR_POST_TEXT', 'WARN_APRV_OFCR_NAME', 'WARN_LTR_ATTN_NAME', 'WARN_ISS_OFCR_TYPE_CODE', 'WARN_ISS_OFCR_NAME', 'WARN_DEL_RSN_CODE', 'WARN_DEL_RSN_RMK_TEXT', 'WARN_TXN_STS_CODE', 'WARN_TXN_CRE_DATE', 'WARN_TXN_CRE_USER_ID', 'WARN_LTR_MAIL_ADDR_1', 'WARN_LTR_MAIL_ADDR_2', 'WARN_LTR_MAIL_ADDR_3', 'WARN_LTR_MAIL_ADDR_4', 'WARN_LTR_MAIL_ADDR_5', 'FLUP_CMPLT_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_DOG_WARN_TXN", mode="append")

        logger.info("write_EMS_DOG_WARN_TXN write completed")
        logger.info("Step: write_EMS_DOG_WARN_TXN1")
        # Write to Target: write_EMS_DOG_WARN_TXN1
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofndr_id_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofndr_id_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofndr_id_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofndr_id_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incdt_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incdt_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "warn_ltr_lang_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_lang_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incdt_loc_othr_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incdt_loc_othr_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofndr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofndr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_file_ref_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_file_ref_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_offc_phone_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_offc_phone_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_offc_fax_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_offc_fax_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "resp_ofcr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "resp_ofcr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "resp_ofcr_phone_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "resp_ofcr_phone_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_aprv_ofcr_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_aprv_ofcr_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_aprv_ofcr_post_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_aprv_ofcr_post_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_aprv_ofcr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_aprv_ofcr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_attn_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_attn_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_ofcr_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_ofcr_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_iss_ofcr_name" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_iss_ofcr_name":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_del_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_del_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_del_rsn_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_del_rsn_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_txn_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_txn_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_txn_cre_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_txn_cre_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "warn_txn_cre_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_txn_cre_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_1" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_1":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_2" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_2":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_3" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_3":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_4" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_4":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "warn_ltr_mail_addr_5" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "warn_ltr_mail_addr_5":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "flup_cmplt_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flup_cmplt_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(TimestampType()))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CUST_KEY": "CUST_KEY", "DOG_WARN_TXN_KEY": "DOG_WARN_TXN_KEY", "FLUP_CMPLT_DATE": "FLUP_CMPLT_DATE", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "INCDT_DATE": "INCDT_DATE", "INCDT_LOC_OTHR_TEXT": "INCDT_LOC_OTHR_TEXT", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID", "OFNDR_ID_NUM": "OFNDR_ID_NUM", "OFNDR_ID_TYPE_CODE": "OFNDR_ID_TYPE_CODE", "OFNDR_NAME": "OFNDR_NAME", "RESP_OFCR_NAME": "RESP_OFCR_NAME", "RESP_OFCR_PHONE_NUM": "RESP_OFCR_PHONE_NUM", "WARN_APRV_OFCR_NAME": "WARN_APRV_OFCR_NAME", "WARN_APRV_OFCR_POST_TEXT": "WARN_APRV_OFCR_POST_TEXT", "WARN_APRV_OFCR_TYPE_CODE": "WARN_APRV_OFCR_TYPE_CODE", "WARN_CODE": "WARN_CODE", "WARN_DEL_RSN_CODE": "WARN_DEL_RSN_CODE", "WARN_DEL_RSN_RMK_TEXT": "WARN_DEL_RSN_RMK_TEXT", "WARN_FILE_REF_TEXT": "WARN_FILE_REF_TEXT", "WARN_ISS_OFCR_NAME": "WARN_ISS_OFCR_NAME", "WARN_ISS_OFCR_TYPE_CODE": "WARN_ISS_OFCR_TYPE_CODE", "WARN_ISS_OFFC_FAX_NUM": "WARN_ISS_OFFC_FAX_NUM", "WARN_ISS_OFFC_PHONE_NUM": "WARN_ISS_OFFC_PHONE_NUM", "WARN_LTR_ATTN_NAME": "WARN_LTR_ATTN_NAME", "WARN_LTR_LANG_CODE": "WARN_LTR_LANG_CODE", "WARN_LTR_MAIL_ADDR_1": "WARN_LTR_MAIL_ADDR_1", "WARN_LTR_MAIL_ADDR_2": "WARN_LTR_MAIL_ADDR_2", "WARN_LTR_MAIL_ADDR_3": "WARN_LTR_MAIL_ADDR_3", "WARN_LTR_MAIL_ADDR_4": "WARN_LTR_MAIL_ADDR_4", "WARN_LTR_MAIL_ADDR_5": "WARN_LTR_MAIL_ADDR_5", "WARN_TXN_CRE_DATE": "WARN_TXN_CRE_DATE", "WARN_TXN_CRE_USER_ID": "WARN_TXN_CRE_USER_ID", "WARN_TXN_STS_CODE": "WARN_TXN_STS_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['DOG_WARN_TXN_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'OFNDR_ID_TYPE_CODE', 'OFNDR_ID_NUM', 'INCDT_DATE', 'WARN_LTR_LANG_CODE', 'INCDT_LOC_OTHR_TEXT', 'OFNDR_NAME', 'WARN_CODE', 'WARN_FILE_REF_TEXT', 'WARN_ISS_OFFC_PHONE_NUM', 'WARN_ISS_OFFC_FAX_NUM', 'RESP_OFCR_NAME', 'RESP_OFCR_PHONE_NUM', 'WARN_APRV_OFCR_TYPE_CODE', 'WARN_APRV_OFCR_POST_TEXT', 'WARN_APRV_OFCR_NAME', 'WARN_LTR_ATTN_NAME', 'WARN_ISS_OFCR_TYPE_CODE', 'WARN_ISS_OFCR_NAME', 'WARN_DEL_RSN_CODE', 'WARN_DEL_RSN_RMK_TEXT', 'WARN_TXN_STS_CODE', 'WARN_TXN_CRE_DATE', 'WARN_TXN_CRE_USER_ID', 'WARN_LTR_MAIL_ADDR_1', 'WARN_LTR_MAIL_ADDR_2', 'WARN_LTR_MAIL_ADDR_3', 'WARN_LTR_MAIL_ADDR_4', 'WARN_LTR_MAIL_ADDR_5', 'FLUP_CMPLT_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_DOG_WARN_TXN", mode="append")

        logger.info("write_EMS_DOG_WARN_TXN1 write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SSAL1_EXTR_EMS_DOG_WARN_TXN")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()