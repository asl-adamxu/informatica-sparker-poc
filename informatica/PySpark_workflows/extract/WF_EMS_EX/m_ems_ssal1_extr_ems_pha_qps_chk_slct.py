'''
* Script - M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "EMS")

    v_load_start_ds = ""
    v_load_end_ds = ""
    # Load mapping variables from the UTL_JOB_PARAM file (shared helper)
    _vars = lib.load_mapping_variables(config, [ "$$v_load_start_ds",  "$$v_load_end_ds", ], logger)
    v_load_start_ds = _vars.get("v_load_start_ds", v_load_start_ds)
    v_load_end_ds = _vars.get("v_load_end_ds", v_load_end_ds)
    
    try:
        logger.info("Step: read_PHA_QPS_CHK_SLCT")
        # Reading Data From Source - read_PHA_QPS_CHK_SLCT
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "EMS")
        df_PHA_QPS_CHK_SLCT = lib.read_sql(spark, _conn, table="PHA_QPS_CHK")
        
        logger.info("Step: apply_SQ_PHA_QPS_CHK_SLCT")
        # Source Qualifier: apply_SQ_PHA_QPS_CHK_SLCT
        df_SQ_PHA_QPS_CHK_SLCT = df_PHA_QPS_CHK_SLCT
        df_SQ_PHA_QPS_CHK_SLCT = lib.sq_output(
            input_df=df_SQ_PHA_QPS_CHK_SLCT,
            port_cols={
                'QPS_CHK_ID': 'string',
                'QPS_CHK_STG_CODE': 'string',
                'HSE_SRVC_APLY_KEY': 'string',
                'PRH_APLY_STG_CODE': 'string',
                'PRH_APLY_STS_CODE': 'string',
                'APLY_RGSTR_DATE': 'date/time',
                'APLY_EQVLN_DATE': 'date/time',
                'CUST_MBR_DOB_DATE': 'date/time',
                'CUST_MBR_DOB_IND': 'string',
                'QPS_DUP_REF_PNT_NUM': 'decimal',
                'QPS_RGSTR_AGE_PNT_NUM': 'decimal',
                'QPS_WAIT_MTH_PNT_NUM': 'decimal',
                'RTN_RCV_DATE': 'date/time',
                'RTN_RVW_RSLT_CODE': 'string',
                'RTN_HSHLD_INCM_AMT': 'decimal',
                'RTN_HSHLD_AST_AMT': 'decimal',
                'RTN_HSHLD_INCM_EXCD_IND': 'string',
                'RTN_HSHLD_AST_EXCD_IND': 'string',
                'RTN_MBL_PHONE_NUM': 'string',
                'RTN_PRPTY_OWNR_IND': 'string',
                'RTN_SELF_WTHDRW_IND': 'string',
                'RTN_ADD_MBR_IND': 'string',
                'RTN_INPT_DATE': 'date/time',
                'ACK_LTR_ISS_DATE': 'date/time',
                'ELGBL_LTR_ISS_DATE': 'date/time',
                'RMDR_LTR_ISS_DATE': 'date/time',
                'INTVW_APPT_DATE': 'date/time',
                'CUST_MBR_ID_TYPE_CODE': 'string',
                'CUST_MBR_ID_NUM': 'string',
                'CUST_MBR_ENG_NAME': 'string',
                'CUST_MBR_CHI_NAME': 'string',
                'CUST_MBR_GNDR_CODE': 'string',
                'QPS_AGE_PNT_DATE': 'date/time',
                'RTN_PPR_VET_IND': 'string',
                'REJ_LTR_ISS_DATE': 'date/time',
                'APLY_CNCL_DATE': 'date/time',
                'RTN_RNDM_CHK_IND': 'string',
                'RTN_RNDM_CHK_DATE': 'date/time',
                'QPS_CHK_BTCH_REJ_DATE': 'date/time',
                'QPS_CHK_BTCH_REJ_RPLY_DLN_DATE': 'date/time',
                'LAST_REC_TXN_TYPE_CODE': 'string',
                'LAST_REC_TXN_DATE': 'date/time',
                'LAST_REC_TXN_USER_ID': 'string',
            },
            filter_condition="LAST_REC_TXN_DATE > to_date('$$v_load_start_ds','yyyy-MM-dd HH:mm:ss') AND LAST_REC_TXN_DATE <= to_date('$$v_load_end_ds','yyyy-MM-dd HH:mm:ss')",
            substitutions={'$$v_load_start_ds': v_load_start_ds, '$$v_load_end_ds': v_load_end_ds},
        )
        ctx.register_df("df_SQ_PHA_QPS_CHK_SLCT", df_SQ_PHA_QPS_CHK_SLCT)
        
        logger.info("Step: apply_EXP_L1")
        # Expression: apply_EXP_L1
        df_EXP_L1 = lib.expression(
            input_df=df_SQ_PHA_QPS_CHK_SLCT,
        )
        ctx.register_df("df_EXP_L1", df_EXP_L1)
        
        logger.info("Step: write_EMS_PHA_QPS_CHK_SLCT")
        # Write to Target: write_EMS_PHA_QPS_CHK_SLCT
        lib.write_target(
            spark=spark,
            df=df_EXP_L1,
            conn=conn_target,
            table='EMS_PHA_QPS_CHK',
            mode='append',
            source_columns=[
                'QPS_CHK_ID',
                'QPS_CHK_STG_CODE',
                None,
                None,
                None,
                None,
                None,
                None,
                'QPS_CHK_BTCH_REJ_DATE',
                'QPS_CHK_BTCH_REJ_RPLY_DLN_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            target_columns=[
                'QPS_CHK_ID',
                'QPS_CHK_STG_CODE',
                'QPS_CHK_PNT_NUM',
                'QPS_CHK_PREV_RPLY_PCT',
                'QPS_CHK_PREV_NO_RPLY_PCT',
                'QPS_CHK_STG_BGN_DATE',
                'QPS_CHK_STG_END_DATE',
                'QPS_CHK_PPR_VET_CNT',
                'QPS_CHK_BTCH_REJ_DATE',
                'QPS_CHK_BTCH_REJ_RPLY_DLN_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            config=config,
        )

        logger.info("write_EMS_PHA_QPS_CHK_SLCT write completed")
        logger.info("Step: write_EMS_PHA_QPS_CHK_SLCT1")
        # Write to Target: write_EMS_PHA_QPS_CHK_SLCT1
        lib.write_target(
            spark=spark,
            df=df_EXP_L1,
            conn=conn_target,
            table='EMS_PHA_QPS_CHK',
            mode='append',
            source_columns=[
                'QPS_CHK_ID',
                'QPS_CHK_STG_CODE',
                None,
                None,
                None,
                None,
                None,
                None,
                'QPS_CHK_BTCH_REJ_DATE',
                'QPS_CHK_BTCH_REJ_RPLY_DLN_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            target_columns=[
                'QPS_CHK_ID',
                'QPS_CHK_STG_CODE',
                'QPS_CHK_PNT_NUM',
                'QPS_CHK_PREV_RPLY_PCT',
                'QPS_CHK_PREV_NO_RPLY_PCT',
                'QPS_CHK_STG_BGN_DATE',
                'QPS_CHK_STG_END_DATE',
                'QPS_CHK_PPR_VET_CNT',
                'QPS_CHK_BTCH_REJ_DATE',
                'QPS_CHK_BTCH_REJ_RPLY_DLN_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            config=config,
        )

        logger.info("write_EMS_PHA_QPS_CHK_SLCT1 write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SSAL1_EXTR_EMS_PHA_QPS_CHK_SLCT")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())