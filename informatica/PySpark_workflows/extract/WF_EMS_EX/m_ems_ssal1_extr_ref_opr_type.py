'''
* Script - M_EMS_SSAL1_EXTR_REF_OPR_TYPE.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_EMS_SSAL1_EXTR_REF_OPR_TYPE mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SSAL1_EXTR_REF_OPR_TYPE")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SSAL1_EXTR_REF_OPR_TYPE", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "EMS")
    conn_target = lib.get_db_config(config, "EMS")

    v_load_start_ds = ""
    v_load_end_ds = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    if job_params:
        for _var, _val in job_params.items():
            _clean = _var.replace("$", "")
            if _clean in [ "v_load_start_ds",  "v_load_end_ds", ]:
                if _clean == "v_load_start_ds":
                    v_load_start_ds = _val
                if _clean == "v_load_end_ds":
                    v_load_end_ds = _val
                logger.info("Loaded %s=%s from job_params", _var, _val)
    else:
        try:
            _param_obj = objects.get("UTL_JOB_PARAM", {})
            if isinstance(_param_obj, dict):
                _param_path = lib._resolve_path(_param_obj.get('path', '/tmp/UTL_JOB_PARAM'))
            with open(_param_path, "r") as _f:
                for _line in _f:
                    _line = _line.strip()
                    for _var in [ "$$v_load_start_ds",  "$$v_load_end_ds", ]:
                        if _line.startswith(_var + "="):
                            _val = _line.split("=", 1)[1]
                            _clean = _var.replace("$", "")
                            if _clean == "v_load_start_ds":
                                v_load_start_ds = _val
                            if _clean == "v_load_end_ds":
                                v_load_end_ds = _val
                            logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
        except Exception:
            logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_REF_OPR_TYPE")
        # Reading Data From Source - read_REF_OPR_TYPE
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "EMS")
        df_src_1 = lib.read_sql(spark, _conn, table="REF_OPR_TYPE")
        
        logger.info("Step: apply_SQ_REF_OPR_TYPE")
        # Source Qualifier: apply_SQ_REF_OPR_TYPE
        df_sq_2 = df_src_1
        df_sq_2 = df_sq_2.filter(expr("LAST_REC_TXN_DATE> to_date(''$$v_load_start_ds'','yyyy-mm-dd hh24:mi:ss') AND LAST_REC_TXN_DATE<= to_date(''$$v_load_end_ds'','yyyy-mm-dd hh24:mi:ss')"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("OPR_TYPE_CODE", "OPR_TYPE_DESP", "CHK_RCPT_IND", "CHK_TRGT_IND", "CHK_AFCT_IND", "CHK_FLAT_SLCT_IND", "CHK_ALWN_IND", "REF_CODE_BGN_DATE", "REF_CODE_END_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_exp_3 = df_sq_2
        df_exp_3 = df_exp_3.withColumn("OPR_TYPE_CODE", expr("rtrim(ltrim(OPR_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("OPR_TYPE_DESP", expr("rtrim(ltrim(OPR_TYPE_DESP))"))
        df_exp_3 = df_exp_3.withColumn("CHK_RCPT_IND", expr("rtrim(ltrim(CHK_RCPT_IND))"))
        df_exp_3 = df_exp_3.withColumn("CHK_TRGT_IND", expr("rtrim(ltrim(CHK_TRGT_IND))"))
        df_exp_3 = df_exp_3.withColumn("CHK_AFCT_IND", expr("rtrim(ltrim(CHK_AFCT_IND))"))
        df_exp_3 = df_exp_3.withColumn("CHK_FLAT_SLCT_IND", expr("rtrim(ltrim(CHK_FLAT_SLCT_IND))"))
        df_exp_3 = df_exp_3.withColumn("CHK_ALWN_IND", expr("rtrim(ltrim(CHK_ALWN_IND))"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_TYPE_CODE", expr("rtrim(ltrim(LAST_REC_TXN_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_USER_ID", expr("rtrim(ltrim(LAST_REC_TXN_USER_ID))"))
        df_exp_3 = df_exp_3.withColumn("DUMMY", expr("'|'"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["REF_CODE_BGN_DATE", "REF_CODE_END_DATE", "LAST_REC_TXN_DATE"]:
            if _col not in df_exp_3.columns:
                df_exp_3 = df_exp_3.withColumn(_col, lit(None))
        # Select only mapping output ports (prevents column leakage)
        df_exp_3 = df_exp_3.select("OPR_TYPE_CODE", "OPR_TYPE_DESP", "CHK_RCPT_IND", "CHK_TRGT_IND", "CHK_AFCT_IND", "CHK_FLAT_SLCT_IND", "CHK_ALWN_IND", "REF_CODE_BGN_DATE", "REF_CODE_END_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID", "DUMMY")
        ctx.register_df("df_exp_3", df_exp_3)
        
        logger.info("Step: apply_mplt_EXPTRANS3")
        # Expression: apply_mplt_EXPTRANS3
        df_mplt_expr_4 = df_exp_3
        df_mplt_expr_4 = df_mplt_expr_4.withColumn("LAST_REC_TXN_DATE1", expr("CASE WHEN NOT (LAST_REC_TXN_DATE IS NULL) THEN cast(LAST_REC_TXN_DATE as string) || '.000000 ' || cast(LAST_REC_TXN_DATE as string) ELSE null END"))
        ctx.register_df("df_mplt_expr_4", df_mplt_expr_4)
        
        logger.info("Step: apply_MPLT_TRANS_TIME_STAMP")
        # Expression: apply_MPLT_TRANS_TIME_STAMP
        df_mplt_5 = df_mplt_expr_4
        ctx.register_df("df_mplt_5", df_mplt_5)
        
        logger.info("Step: join_target_EMS_REF_OPR_TYPE1_0")
        # Lookup: join_target_EMS_REF_OPR_TYPE1_0
        # Merge parallel DataFrames on their common columns
        _common_cols = [c for c in df_mplt_5.columns if c in df_exp_3.columns]
        df_tgt_merge_6 = df_mplt_5.join(
            df_exp_3,
            on=_common_cols,
            how="left"
        )
        ctx.register_df("df_tgt_merge_6", df_tgt_merge_6)
        
        logger.info("Step: write_EMS_REF_OPR_TYPE1")
        # Write to Target: write_EMS_REF_OPR_TYPE1
        df_write = df_tgt_merge_6
        # Cast columns to match target schema data types
        if "opr_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "opr_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "opr_type_desp" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "opr_type_desp":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_rcpt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_rcpt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_trgt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_trgt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_afct_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_afct_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_flat_slct_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_flat_slct_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_alwn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_alwn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ref_code_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ref_code_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "ref_code_end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ref_code_end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CHK_AFCT_IND": "CHK_AFCT_IND", "CHK_ALWN_IND": "CHK_ALWN_IND", "CHK_FLAT_SLCT_IND": "CHK_FLAT_SLCT_IND", "CHK_RCPT_IND": "CHK_RCPT_IND", "CHK_TRGT_IND": "CHK_TRGT_IND", "DUMMY": "DUMMY", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE1", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID", "OPR_TYPE_CODE": "OPR_TYPE_CODE", "OPR_TYPE_DESP": "OPR_TYPE_DESP", "REF_CODE_BGN_DATE": "REF_CODE_BGN_DATE", "REF_CODE_END_DATE": "REF_CODE_END_DATE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['OPR_TYPE_CODE', 'OPR_TYPE_DESP', 'CHK_RCPT_IND', 'CHK_TRGT_IND', 'CHK_AFCT_IND', 'CHK_FLAT_SLCT_IND', 'CHK_ALWN_IND', 'REF_CODE_BGN_DATE', 'REF_CODE_END_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_REF_OPR_TYPE", mode="append")

        logger.info("write_EMS_REF_OPR_TYPE1 write completed")
        logger.info("Step: join_target_EMS_REF_OPR_TYPE_0")
        # Lookup: join_target_EMS_REF_OPR_TYPE_0
        # Merge parallel DataFrames on their common columns
        _common_cols = [c for c in df_mplt_5.columns if c in df_exp_3.columns]
        df_tgt_merge_7 = df_mplt_5.join(
            df_exp_3,
            on=_common_cols,
            how="left"
        )
        ctx.register_df("df_tgt_merge_7", df_tgt_merge_7)
        
        logger.info("Step: write_EMS_REF_OPR_TYPE")
        # Write to Target: write_EMS_REF_OPR_TYPE
        df_write = df_tgt_merge_7
        # Cast columns to match target schema data types
        if "opr_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "opr_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "opr_type_desp" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "opr_type_desp":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_rcpt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_rcpt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_trgt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_trgt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_afct_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_afct_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_flat_slct_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_flat_slct_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "chk_alwn_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "chk_alwn_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ref_code_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ref_code_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "ref_code_end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ref_code_end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CHK_AFCT_IND": "CHK_AFCT_IND", "CHK_ALWN_IND": "CHK_ALWN_IND", "CHK_FLAT_SLCT_IND": "CHK_FLAT_SLCT_IND", "CHK_RCPT_IND": "CHK_RCPT_IND", "CHK_TRGT_IND": "CHK_TRGT_IND", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE1", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID", "OPR_TYPE_CODE": "OPR_TYPE_CODE", "OPR_TYPE_DESP": "OPR_TYPE_DESP", "REF_CODE_BGN_DATE": "REF_CODE_BGN_DATE", "REF_CODE_END_DATE": "REF_CODE_END_DATE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['OPR_TYPE_CODE', 'OPR_TYPE_DESP', 'CHK_RCPT_IND', 'CHK_TRGT_IND', 'CHK_AFCT_IND', 'CHK_FLAT_SLCT_IND', 'CHK_ALWN_IND', 'REF_CODE_BGN_DATE', 'REF_CODE_END_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_REF_OPR_TYPE", mode="append")

        logger.info("write_EMS_REF_OPR_TYPE write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SSAL1_EXTR_REF_OPR_TYPE completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SSAL1_EXTR_REF_OPR_TYPE FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SSAL1_EXTR_REF_OPR_TYPE", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SSAL1_EXTR_REF_OPR_TYPE")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()