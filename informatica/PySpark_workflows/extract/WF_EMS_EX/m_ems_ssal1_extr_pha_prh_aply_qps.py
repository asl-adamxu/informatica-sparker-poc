'''
* Script - M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS.py
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None,
                session_sqls=None) -> bool:
    """
    Execute the M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
        session_sqls: The session's Target Pre/Post SQL dict 
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_target = lib.get_db_config(config, "EMS")

    v_load_start_ds = ""
    v_load_end_ds = ""
    # Load mapping variables from the UTL_JOB_PARAM file (shared helper)
    _vars = lib.load_mapping_variables(config, [ "$$v_load_start_ds",  "$$v_load_end_ds", ], logger)
    v_load_start_ds = _vars.get("v_load_start_ds", v_load_start_ds)
    v_load_end_ds = _vars.get("v_load_end_ds", v_load_end_ds)
    
    try:
        logger.info("Step: read_PHA_PRH_APLY_QPS")
        # Reading Data From Source - read_PHA_PRH_APLY_QPS
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "EMS")
        df_PHA_PRH_APLY_QPS = lib.read_sql(spark, _conn, table="PHA_PRH_APLY_QPS")
        
        logger.info("Step: apply_SQ_PHA_PRH_APLY_QPS")
        # Source Qualifier: apply_SQ_PHA_PRH_APLY_QPS
        df_SQ_PHA_PRH_APLY_QPS = df_PHA_PRH_APLY_QPS
        df_SQ_PHA_PRH_APLY_QPS = lib.sq_output(
            input_df=df_SQ_PHA_PRH_APLY_QPS,
            port_cols={
                'HSE_SRVC_APLY_KEY': 'string',
                'QPS_RGSTR_AGE_PNT_NUM': 'decimal',
                'QPS_WAIT_MTH_PNT_NUM': 'decimal',
                'QPS_DUP_REF_PNT_NUM': 'decimal',
                'QPS_TOT_PNT_NUM': 'decimal',
                'QPS_PNT_AMND_TYPE_CODE': 'decimal',
                'QPS_JOIN_DATE': 'date/time',
                'HSE_BNFT_CHK_DATE': 'date/time',
                'HSE_BNFT_TYPE_CODE': 'string',
                'HSE_BNFT_REF_NUM': 'string',
                'OCPY_STS_CODE': 'string',
                'APLY_RGSTR_DATE': 'date/time',
                'APLY_EQVLN_DATE': 'date/time',
                'CUST_MBR_DOB_DATE': 'date/time',
                'LAST_REC_TXN_TYPE_CODE': 'string',
                'LAST_REC_TXN_DATE': 'string',
                'LAST_REC_TXN_USER_ID': 'string',
            },
            filter_condition="LAST_REC_TXN_DATE > to_date('$$v_load_start_ds','yyyy-MM-dd HH:mm:ss') AND LAST_REC_TXN_DATE <= to_date('$$v_load_end_ds','yyyy-MM-dd HH:mm:ss')",
            substitutions={'$$v_load_start_ds': v_load_start_ds, '$$v_load_end_ds': v_load_end_ds},
        )
        ctx.register_df("df_SQ_PHA_PRH_APLY_QPS", df_SQ_PHA_PRH_APLY_QPS)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_EXPTRANS = lib.expression(
            input_df=df_SQ_PHA_PRH_APLY_QPS,
            computed_columns=[
                {'name': 'HSE_SRVC_APLY_KEY', 'expr': 'ltrim(rtrim(HSE_SRVC_APLY_KEY))'},
                {'name': 'HSE_BNFT_TYPE_CODE', 'expr': 'ltrim(rtrim(HSE_BNFT_TYPE_CODE))'},
                {'name': 'HSE_BNFT_REF_NUM', 'expr': 'ltrim(rtrim(HSE_BNFT_REF_NUM))'},
                {'name': 'OCPY_STS_CODE', 'expr': 'ltrim(rtrim(OCPY_STS_CODE))'},
                {'name': 'LAST_REC_TXN_TYPE_CODE', 'expr': 'ltrim(rtrim(LAST_REC_TXN_TYPE_CODE))'},
                {'name': 'LAST_REC_TXN_USER_ID', 'expr': 'ltrim(rtrim(LAST_REC_TXN_USER_ID))'},
                {'name': 'DUMMY', 'expr': "'|'"}
            ],
        )
        ctx.register_df("df_EXPTRANS", df_EXPTRANS)
        
        logger.info("Step: write_EMS_PHA_PRH_APLY_QPS1")
        # Write to Target: write_EMS_PHA_PRH_APLY_QPS1
        lib.write_target(
            spark=spark,
            df=df_EXPTRANS,
            conn=conn_target,
            table='EMS_PHA_PRH_APLY_QPS',
            mode='append',
            source_columns=[
                'HSE_SRVC_APLY_KEY',
                'QPS_RGSTR_AGE_PNT_NUM',
                'QPS_WAIT_MTH_PNT_NUM',
                'QPS_DUP_REF_PNT_NUM',
                'QPS_TOT_PNT_NUM',
                'QPS_PNT_AMND_TYPE_CODE',
                'QPS_JOIN_DATE',
                'HSE_BNFT_CHK_DATE',
                'HSE_BNFT_TYPE_CODE',
                'HSE_BNFT_REF_NUM',
                'OCPY_STS_CODE',
                'APLY_RGSTR_DATE',
                'APLY_EQVLN_DATE',
                'CUST_MBR_DOB_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            target_columns=[
                'HSE_SRVC_APLY_KEY',
                'QPS_RGSTR_AGE_PNT_NUM',
                'QPS_WAIT_MTH_PNT_NUM',
                'QPS_DUP_REF_PNT_NUM',
                'QPS_TOT_PNT_NUM',
                'QPS_PNT_AMND_TYPE_CODE',
                'QPS_JOIN_DATE',
                'HSE_BNFT_CHK_DATE',
                'HSE_BNFT_TYPE_CODE',
                'HSE_BNFT_REF_NUM',
                'OCPY_STS_CODE',
                'APLY_RGSTR_DATE',
                'APLY_EQVLN_DATE',
                'CUST_MBR_DOB_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            config=config,
        )

        logger.info("write_EMS_PHA_PRH_APLY_QPS1 write completed")
        logger.info("Step: write_EMS_PHA_PRH_APLY_QPS")
        # Write to Target: write_EMS_PHA_PRH_APLY_QPS
        lib.write_target(
            spark=spark,
            df=df_EXPTRANS,
            conn=conn_target,
            table='EMS_PHA_PRH_APLY_QPS',
            mode='append',
            source_columns=[
                'HSE_SRVC_APLY_KEY',
                'QPS_RGSTR_AGE_PNT_NUM',
                'QPS_WAIT_MTH_PNT_NUM',
                'QPS_DUP_REF_PNT_NUM',
                'QPS_TOT_PNT_NUM',
                'QPS_PNT_AMND_TYPE_CODE',
                'QPS_JOIN_DATE',
                'HSE_BNFT_CHK_DATE',
                'HSE_BNFT_TYPE_CODE',
                'HSE_BNFT_REF_NUM',
                'OCPY_STS_CODE',
                'APLY_RGSTR_DATE',
                'APLY_EQVLN_DATE',
                'CUST_MBR_DOB_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            target_columns=[
                'HSE_SRVC_APLY_KEY',
                'QPS_RGSTR_AGE_PNT_NUM',
                'QPS_WAIT_MTH_PNT_NUM',
                'QPS_DUP_REF_PNT_NUM',
                'QPS_TOT_PNT_NUM',
                'QPS_PNT_AMND_TYPE_CODE',
                'QPS_JOIN_DATE',
                'HSE_BNFT_CHK_DATE',
                'HSE_BNFT_TYPE_CODE',
                'HSE_BNFT_REF_NUM',
                'OCPY_STS_CODE',
                'APLY_RGSTR_DATE',
                'APLY_EQVLN_DATE',
                'CUST_MBR_DOB_DATE',
                'LAST_REC_TXN_TYPE_CODE',
                'LAST_REC_TXN_DATE',
                'LAST_REC_TXN_USER_ID',
            ],
            config=config,
        )

        logger.info("write_EMS_PHA_PRH_APLY_QPS write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SSAL1_EXTR_PHA_PRH_APLY_QPS")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        if not success:
            # Exit the JVM non-zero so YARN marks the application FAILED.
            # In client mode the AM lives in this JVM: a normal spark.stop() +
            # python exit code still reports SUCCEEDED (AM exits cleanly).
            spark.sparkContext._jvm.System.exit(1)
        return 0
    finally:
        spark.stop()


if __name__ == "__main__":
    # sys.exit propagates the failure exit code — without it the process exits 0
    # and YARN reports SUCCEEDED even when the mapping failed.
    import sys as _sys
    _sys.exit(main())