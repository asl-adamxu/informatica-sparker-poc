'''
* Script - M_EMS_SSAL1_EXTR_EMS_WTP_DCLR.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_EMS_SSAL1_EXTR_EMS_WTP_DCLR mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_EMS_SSAL1_EXTR_EMS_WTP_DCLR")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_EMS_SSAL1_EXTR_EMS_WTP_DCLR", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "EMS")
    conn_target = lib.get_db_config(config, "EMS")

    v_load_start_ds = ""
    v_load_end_ds = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    if job_params:
        for _var, _val in job_params.items():
            _clean = _var.replace("$", "")
            if _clean in [ "v_load_start_ds",  "v_load_end_ds", ]:
                if _clean == "v_load_start_ds":
                    v_load_start_ds = _val
                if _clean == "v_load_end_ds":
                    v_load_end_ds = _val
                logger.info("Loaded %s=%s from job_params", _var, _val)
    else:
        try:
            _param_obj = objects.get("UTL_JOB_PARAM", {})
            if isinstance(_param_obj, dict):
                _param_path = lib._resolve_path(_param_obj.get('path', '/tmp/UTL_JOB_PARAM'))
            with open(_param_path, "r") as _f:
                for _line in _f:
                    _line = _line.strip()
                    for _var in [ "$$v_load_start_ds",  "$$v_load_end_ds", ]:
                        if _line.startswith(_var + "="):
                            _val = _line.split("=", 1)[1]
                            _clean = _var.replace("$", "")
                            if _clean == "v_load_start_ds":
                                v_load_start_ds = _val
                            if _clean == "v_load_end_ds":
                                v_load_end_ds = _val
                            logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
        except Exception:
            logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_WTP_DCLR")
        # Reading Data From Source - read_WTP_DCLR
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "EMS")
        df_src_1 = lib.read_sql(spark, _conn, table="WTP_DCLR")
        
        logger.info("Step: apply_SQ_WTP_DCLR")
        # Source Qualifier: apply_SQ_WTP_DCLR
        df_sq_2 = df_src_1
        df_sq_2 = df_sq_2.filter(expr("WTP_DCLR.LAST_REC_TXN_DATE > to_date(''$$v_load_start_ds'','yyyy-mm-dd hh24:mi:ss') AND WTP_DCLR.LAST_REC_TXN_DATE <= to_date(''$$v_load_end_ds'','yyyy-mm-dd hh24:mi:ss')"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("WTP_REC_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY", "WTP_CYCL_YEAR_MTH", "WTP_STS_CODE", "WTP_DCLR_FORM_RCV_IND", "WTP_REF_PHRM_IND", "WTP_OPT_NOT_DCLR_IND", "CMT_SBTYP_CODE", "WTP_DSBL_ALWN_RCPT_IND", "WTP_HK_DPO_IND", "WTP_HSHLD_INCM_OVER_IND", "WTP_HSHLD_AST_OVER_IND", "FMLY_SIZE_NUM", "HSHLD_INCM_AMT", "HSHLD_AST_AMT", "RENT_FCTR_CODE", "RENT_BGN_DATE", "RENT_END_DATE", "RENT_CHNG_RSN_CODE", "RENT_CHNG_RSN_TEXT", "WTP_RMK_TEXT", "INT37_PRN_DATE", "WTP25_PRN_DATE", "WTP26_PRN_DATE", "WTP28_PRN_DATE", "WTP29_PRN_DATE", "WTP30_PRN_DATE", "WTP31_PRN_DATE", "WTP32_PRN_DATE", "WTP33_PRN_DATE", "WTP34_PRN_DATE", "WTP35_PRN_DATE", "WTP36_PRN_DATE", "WTP37_PRN_DATE", "WTP38_PRN_DATE", "ORIG_HSE_SRVC_APLY_KEY", "WTP_EXTRC_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: apply_EXP_L1")
        # Expression: apply_EXP_L1
        df_exp_3 = df_sq_2
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["WTP_REC_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY", "WTP_CYCL_YEAR_MTH", "WTP_STS_CODE", "WTP_DCLR_FORM_RCV_IND", "WTP_REF_PHRM_IND", "WTP_OPT_NOT_DCLR_IND", "CMT_SBTYP_CODE", "WTP_DSBL_ALWN_RCPT_IND", "WTP_HK_DPO_IND", "WTP_HSHLD_INCM_OVER_IND", "WTP_HSHLD_AST_OVER_IND", "FMLY_SIZE_NUM", "HSHLD_INCM_AMT", "HSHLD_AST_AMT", "RENT_FCTR_CODE", "RENT_BGN_DATE", "RENT_END_DATE", "RENT_CHNG_RSN_CODE", "RENT_CHNG_RSN_TEXT", "WTP_RMK_TEXT", "INT37_PRN_DATE", "WTP25_PRN_DATE", "WTP26_PRN_DATE", "WTP28_PRN_DATE", "WTP29_PRN_DATE", "WTP30_PRN_DATE", "WTP31_PRN_DATE", "WTP32_PRN_DATE", "WTP33_PRN_DATE", "WTP34_PRN_DATE", "WTP35_PRN_DATE", "WTP36_PRN_DATE", "WTP37_PRN_DATE", "WTP38_PRN_DATE", "WTP_EXTRC_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID", "ORIG_HSE_SRVC_APLY_KEY"]:
            if _col not in df_exp_3.columns:
                df_exp_3 = df_exp_3.withColumn(_col, lit(None))
        # Select only mapping output ports (prevents column leakage)
        df_exp_3 = df_exp_3.select("WTP_REC_KEY", "CUST_KEY", "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY", "WTP_CYCL_YEAR_MTH", "WTP_STS_CODE", "WTP_DCLR_FORM_RCV_IND", "WTP_REF_PHRM_IND", "WTP_OPT_NOT_DCLR_IND", "CMT_SBTYP_CODE", "WTP_DSBL_ALWN_RCPT_IND", "WTP_HK_DPO_IND", "WTP_HSHLD_INCM_OVER_IND", "WTP_HSHLD_AST_OVER_IND", "FMLY_SIZE_NUM", "HSHLD_INCM_AMT", "HSHLD_AST_AMT", "RENT_FCTR_CODE", "RENT_BGN_DATE", "RENT_END_DATE", "RENT_CHNG_RSN_CODE", "RENT_CHNG_RSN_TEXT", "WTP_RMK_TEXT", "INT37_PRN_DATE", "WTP25_PRN_DATE", "WTP26_PRN_DATE", "WTP28_PRN_DATE", "WTP29_PRN_DATE", "WTP30_PRN_DATE", "WTP31_PRN_DATE", "WTP32_PRN_DATE", "WTP33_PRN_DATE", "WTP34_PRN_DATE", "WTP35_PRN_DATE", "WTP36_PRN_DATE", "WTP37_PRN_DATE", "WTP38_PRN_DATE", "ORIG_HSE_SRVC_APLY_KEY", "WTP_EXTRC_DATE", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID")
        ctx.register_df("df_exp_3", df_exp_3)
        
        logger.info("Step: write_EMS_WTP_DCLR")
        # Write to Target: write_EMS_WTP_DCLR
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_dclr_form_rcv_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_dclr_form_rcv_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_ref_phrm_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_ref_phrm_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_opt_not_dclr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_opt_not_dclr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "cmt_sbtyp_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cmt_sbtyp_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_dsbl_alwn_rcpt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_dsbl_alwn_rcpt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_hk_dpo_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_hk_dpo_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_hshld_incm_over_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_hshld_incm_over_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_hshld_ast_over_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_hshld_ast_over_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "rent_end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "rent_chng_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_chng_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_chng_rsn_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_chng_rsn_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "int37_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "int37_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp25_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp25_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp26_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp26_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp28_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp28_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp29_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp29_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp30_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp30_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp31_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp31_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp32_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp32_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp33_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp33_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp34_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp34_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp35_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp35_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp36_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp36_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp37_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp37_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp38_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp38_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "orig_hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "orig_hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_extrc_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_extrc_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(TimestampType()))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CMT_SBTYP_CODE": "CMT_SBTYP_CODE", "CUST_KEY": "CUST_KEY", "FMLY_SIZE_NUM": "FMLY_SIZE_NUM", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY": "HSE_UNIT_KEY", "HSHLD_AST_AMT": "HSHLD_AST_AMT", "HSHLD_INCM_AMT": "HSHLD_INCM_AMT", "INT37_PRN_DATE": "INT37_PRN_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID", "ORIG_HSE_SRVC_APLY_KEY": "ORIG_HSE_SRVC_APLY_KEY", "RENT_BGN_DATE": "RENT_BGN_DATE", "RENT_CHNG_RSN_CODE": "RENT_CHNG_RSN_CODE", "RENT_CHNG_RSN_TEXT": "RENT_CHNG_RSN_TEXT", "RENT_END_DATE": "RENT_END_DATE", "RENT_FCTR_CODE": "RENT_FCTR_CODE", "WTP25_PRN_DATE": "WTP25_PRN_DATE", "WTP26_PRN_DATE": "WTP26_PRN_DATE", "WTP28_PRN_DATE": "WTP28_PRN_DATE", "WTP29_PRN_DATE": "WTP29_PRN_DATE", "WTP30_PRN_DATE": "WTP30_PRN_DATE", "WTP31_PRN_DATE": "WTP31_PRN_DATE", "WTP32_PRN_DATE": "WTP32_PRN_DATE", "WTP33_PRN_DATE": "WTP33_PRN_DATE", "WTP34_PRN_DATE": "WTP34_PRN_DATE", "WTP35_PRN_DATE": "WTP35_PRN_DATE", "WTP36_PRN_DATE": "WTP36_PRN_DATE", "WTP37_PRN_DATE": "WTP37_PRN_DATE", "WTP38_PRN_DATE": "WTP38_PRN_DATE", "WTP_CYCL_YEAR_MTH": "WTP_CYCL_YEAR_MTH", "WTP_DCLR_FORM_RCV_IND": "WTP_DCLR_FORM_RCV_IND", "WTP_DSBL_ALWN_RCPT_IND": "WTP_DSBL_ALWN_RCPT_IND", "WTP_EXTRC_DATE": "WTP_EXTRC_DATE", "WTP_HK_DPO_IND": "WTP_HK_DPO_IND", "WTP_HSHLD_AST_OVER_IND": "WTP_HSHLD_AST_OVER_IND", "WTP_HSHLD_INCM_OVER_IND": "WTP_HSHLD_INCM_OVER_IND", "WTP_OPT_NOT_DCLR_IND": "WTP_OPT_NOT_DCLR_IND", "WTP_REC_KEY": "WTP_REC_KEY", "WTP_REF_PHRM_IND": "WTP_REF_PHRM_IND", "WTP_RMK_TEXT": "WTP_RMK_TEXT", "WTP_STS_CODE": "WTP_STS_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['WTP_REC_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'HSE_UNIT_KEY', 'WTP_CYCL_YEAR_MTH', 'WTP_STS_CODE', 'WTP_DCLR_FORM_RCV_IND', 'WTP_REF_PHRM_IND', 'WTP_OPT_NOT_DCLR_IND', 'CMT_SBTYP_CODE', 'WTP_DSBL_ALWN_RCPT_IND', 'WTP_HK_DPO_IND', 'WTP_HSHLD_INCM_OVER_IND', 'WTP_HSHLD_AST_OVER_IND', 'FMLY_SIZE_NUM', 'HSHLD_INCM_AMT', 'HSHLD_AST_AMT', 'RENT_FCTR_CODE', 'RENT_BGN_DATE', 'RENT_END_DATE', 'RENT_CHNG_RSN_CODE', 'RENT_CHNG_RSN_TEXT', 'WTP_RMK_TEXT', 'INT37_PRN_DATE', 'WTP25_PRN_DATE', 'WTP26_PRN_DATE', 'WTP28_PRN_DATE', 'WTP29_PRN_DATE', 'WTP30_PRN_DATE', 'WTP31_PRN_DATE', 'WTP32_PRN_DATE', 'WTP33_PRN_DATE', 'WTP34_PRN_DATE', 'WTP35_PRN_DATE', 'WTP36_PRN_DATE', 'WTP37_PRN_DATE', 'WTP38_PRN_DATE', 'ORIG_HSE_SRVC_APLY_KEY', 'WTP_EXTRC_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_WTP_DCLR", mode="append")

        logger.info("write_EMS_WTP_DCLR write completed")
        logger.info("Step: write_EMS_WTP_DCLR1")
        # Write to Target: write_EMS_WTP_DCLR1
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "cust_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cust_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_sts_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_sts_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_dclr_form_rcv_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_dclr_form_rcv_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_ref_phrm_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_ref_phrm_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_opt_not_dclr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_opt_not_dclr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "cmt_sbtyp_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "cmt_sbtyp_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_dsbl_alwn_rcpt_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_dsbl_alwn_rcpt_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_hk_dpo_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_hk_dpo_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_hshld_incm_over_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_hshld_incm_over_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_hshld_ast_over_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_hshld_ast_over_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_bgn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_bgn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "rent_end_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_end_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "rent_chng_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_chng_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "rent_chng_rsn_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "rent_chng_rsn_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_rmk_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_rmk_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "int37_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "int37_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp25_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp25_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp26_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp26_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp28_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp28_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp29_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp29_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp30_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp30_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp31_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp31_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp32_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp32_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp33_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp33_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp34_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp34_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp35_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp35_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp36_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp36_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp37_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp37_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "wtp38_prn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp38_prn_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "orig_hse_srvc_aply_key" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "orig_hse_srvc_aply_key":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "wtp_extrc_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "wtp_extrc_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c, col(c).cast(TimestampType()))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"CMT_SBTYP_CODE": "CMT_SBTYP_CODE", "CUST_KEY": "CUST_KEY", "FMLY_SIZE_NUM": "FMLY_SIZE_NUM", "HSE_SRVC_APLY_KEY": "HSE_SRVC_APLY_KEY", "HSE_UNIT_KEY": "HSE_UNIT_KEY", "HSHLD_AST_AMT": "HSHLD_AST_AMT", "HSHLD_INCM_AMT": "HSHLD_INCM_AMT", "INT37_PRN_DATE": "INT37_PRN_DATE", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID", "ORIG_HSE_SRVC_APLY_KEY": "ORIG_HSE_SRVC_APLY_KEY", "RENT_BGN_DATE": "RENT_BGN_DATE", "RENT_CHNG_RSN_CODE": "RENT_CHNG_RSN_CODE", "RENT_CHNG_RSN_TEXT": "RENT_CHNG_RSN_TEXT", "RENT_END_DATE": "RENT_END_DATE", "RENT_FCTR_CODE": "RENT_FCTR_CODE", "WTP25_PRN_DATE": "WTP25_PRN_DATE", "WTP26_PRN_DATE": "WTP26_PRN_DATE", "WTP28_PRN_DATE": "WTP28_PRN_DATE", "WTP29_PRN_DATE": "WTP29_PRN_DATE", "WTP30_PRN_DATE": "WTP30_PRN_DATE", "WTP31_PRN_DATE": "WTP31_PRN_DATE", "WTP32_PRN_DATE": "WTP32_PRN_DATE", "WTP33_PRN_DATE": "WTP33_PRN_DATE", "WTP34_PRN_DATE": "WTP34_PRN_DATE", "WTP35_PRN_DATE": "WTP35_PRN_DATE", "WTP36_PRN_DATE": "WTP36_PRN_DATE", "WTP37_PRN_DATE": "WTP37_PRN_DATE", "WTP38_PRN_DATE": "WTP38_PRN_DATE", "WTP_CYCL_YEAR_MTH": "WTP_CYCL_YEAR_MTH", "WTP_DCLR_FORM_RCV_IND": "WTP_DCLR_FORM_RCV_IND", "WTP_DSBL_ALWN_RCPT_IND": "WTP_DSBL_ALWN_RCPT_IND", "WTP_EXTRC_DATE": "WTP_EXTRC_DATE", "WTP_HK_DPO_IND": "WTP_HK_DPO_IND", "WTP_HSHLD_AST_OVER_IND": "WTP_HSHLD_AST_OVER_IND", "WTP_HSHLD_INCM_OVER_IND": "WTP_HSHLD_INCM_OVER_IND", "WTP_OPT_NOT_DCLR_IND": "WTP_OPT_NOT_DCLR_IND", "WTP_REC_KEY": "WTP_REC_KEY", "WTP_REF_PHRM_IND": "WTP_REF_PHRM_IND", "WTP_RMK_TEXT": "WTP_RMK_TEXT", "WTP_STS_CODE": "WTP_STS_CODE"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['WTP_REC_KEY', 'CUST_KEY', 'HSE_SRVC_APLY_KEY', 'HSE_UNIT_KEY', 'WTP_CYCL_YEAR_MTH', 'WTP_STS_CODE', 'WTP_DCLR_FORM_RCV_IND', 'WTP_REF_PHRM_IND', 'WTP_OPT_NOT_DCLR_IND', 'CMT_SBTYP_CODE', 'WTP_DSBL_ALWN_RCPT_IND', 'WTP_HK_DPO_IND', 'WTP_HSHLD_INCM_OVER_IND', 'WTP_HSHLD_AST_OVER_IND', 'FMLY_SIZE_NUM', 'HSHLD_INCM_AMT', 'HSHLD_AST_AMT', 'RENT_FCTR_CODE', 'RENT_BGN_DATE', 'RENT_END_DATE', 'RENT_CHNG_RSN_CODE', 'RENT_CHNG_RSN_TEXT', 'WTP_RMK_TEXT', 'INT37_PRN_DATE', 'WTP25_PRN_DATE', 'WTP26_PRN_DATE', 'WTP28_PRN_DATE', 'WTP29_PRN_DATE', 'WTP30_PRN_DATE', 'WTP31_PRN_DATE', 'WTP32_PRN_DATE', 'WTP33_PRN_DATE', 'WTP34_PRN_DATE', 'WTP35_PRN_DATE', 'WTP36_PRN_DATE', 'WTP37_PRN_DATE', 'WTP38_PRN_DATE', 'ORIG_HSE_SRVC_APLY_KEY', 'WTP_EXTRC_DATE', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_WTP_DCLR", mode="append")

        logger.info("write_EMS_WTP_DCLR1 write completed")
        
        metrics.complete()
        logger.info("Mapping M_EMS_SSAL1_EXTR_EMS_WTP_DCLR completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_EMS_SSAL1_EXTR_EMS_WTP_DCLR FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_EMS_SSAL1_EXTR_EMS_WTP_DCLR", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_EMS_SSAL1_EXTR_EMS_WTP_DCLR")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()