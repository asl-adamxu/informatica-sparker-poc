'''
* Script - M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND.py
* @date    2026-07-02
* @author Auto-generated by ASL Informatica XML to PySpark Converter
* @version 1.0
* @par Remarks
*   This code is auto-generated. Please review the business rules before productionizing.
'''

import env.runtime_lib as lib
from pyspark.sql import DataFrame
# Save builtins before pyspark.sql.functions shadows max/min with column versions
_builtin_max = max
_builtin_min = min
from pyspark.sql.functions import *
from pyspark.sql.types import *

# =============================================================================
# MAPPING LOGIC
# =============================================================================

def run_mapping(ctx: lib.SparkContext = None, metrics=None, job_params=None) -> bool:
    """
    Execute the M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND mapping transformations.

    All initialization (logging, config, spark session, connections) happens
    lazily inside this function, so importing the module has no side effects.
    
    Args:
        ctx: Optional SparkContext for session and DataFrame registry
        metrics: Optional metrics tracker (or NullMetrics if not provided)
        job_params: Optional dict of job parameters loaded by workflow
    
    Returns:
        bool: True if successful
    """
    lib.init_logger()
    logger = lib.get_logger()
    logger.propagate = False  # mapping detail stays in mapping log only
    logger.info("Start to run mapping M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND")

    config = lib.load_config('env/config.yml')
    objects = config.get('objects', {})

    spark = ctx.spark if ctx else lib.get_spark_session("M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND", config)
    metrics = metrics or lib.NullMetrics()
    metrics.start()

    conn_oracle = lib.get_db_config(config, "oracle-defaults")
    conn_source = lib.get_db_config(config, "EMS")
    conn_target = lib.get_db_config(config, "EMS")

    v_load_start_ds = ""
    v_load_end_ds = ""
    # Load mapping variables from job_params or UTL_JOB_PARAM file
    if job_params:
        for _var, _val in job_params.items():
            _clean = _var.replace("$", "")
            if _clean in [ "v_load_start_ds",  "v_load_end_ds", ]:
                if _clean == "v_load_start_ds":
                    v_load_start_ds = _val
                if _clean == "v_load_end_ds":
                    v_load_end_ds = _val
                logger.info("Loaded %s=%s from job_params", _var, _val)
    else:
        try:
            _param_obj = objects.get("UTL_JOB_PARAM", {})
            if isinstance(_param_obj, dict):
                _param_path = lib._resolve_path(_param_obj.get('path', '/tmp/UTL_JOB_PARAM'))
            with open(_param_path, "r") as _f:
                for _line in _f:
                    _line = _line.strip()
                    for _var in [ "$$v_load_start_ds",  "$$v_load_end_ds", ]:
                        if _line.startswith(_var + "="):
                            _val = _line.split("=", 1)[1]
                            _clean = _var.replace("$", "")
                            if _clean == "v_load_start_ds":
                                v_load_start_ds = _val
                            if _clean == "v_load_end_ds":
                                v_load_end_ds = _val
                            logger.info("Loaded %s=%s from %s", _var, _val, _param_path)
        except Exception:
            logger.warning("UTL_JOB_PARAM not found, using default values")
    
    try:
        logger.info("Step: read_PHA_PRH_APLY_OFR_SPCL_GND")
        # Reading Data From Source - read_PHA_PRH_APLY_OFR_SPCL_GND
        # Resolve connection by alias (supports lookup/source connections dynamically)
        _conn = lib.get_db_config(config, "EMS")
        df_src_1 = lib.read_sql(spark, _conn, table="PHA_PRH_APLY_OFR_SPCL_GND")
        
        logger.info("Step: apply_SQ_PHA_PRH_APLY_OFR_SPCL_GND")
        # Source Qualifier: apply_SQ_PHA_PRH_APLY_OFR_SPCL_GND
        df_sq_2 = df_src_1
        df_sq_2 = df_sq_2.filter(expr("PHA_PRH_APLY_OFR_SPCL_GND.LAST_REC_TXN_DATE > to_date(''$$v_load_start_ds'','yyyy-mm-dd hh24:mi:ss') AND PHA_PRH_APLY_OFR_SPCL_GND.LAST_REC_TXN_DATE <= to_date(''$$v_load_end_ds'','yyyy-mm-dd hh24:mi:ss')"))
        # Select only SQ output ports (matches Informatica behavior)
        df_sq_2 = df_sq_2.select("BTCH_ALCT_KEY", "OFR_DATE", "OFR_SPCL_GND_RSN_CODE", "OFR_SPCL_GND_RSN_TEXT", "HSE_DSTR_CODE_LIST", "HSE_SBDSTR_CODE_LIST", "EXCLD_EST_CODE", "DSGN_EST_CODE", "HSE_BLK_CODE", "HSE_UNIT_TYPE_CODE", "PRH_UNIT_HSC_TYPE_CODE", "HSE_UNIT_FROM_FLR_NUM", "HSE_UNIT_TO_FLR_NUM", "HSE_UNIT_FROM_IFA_AREA", "HSE_UNIT_TO_IFA_AREA", "PRH_UNIT_FROM_RFSL_NUM", "PRH_UNIT_TO_RFSL_NUM", "HSE_UNIT_VOID_PRD_DAY_CNT", "PRH_UNIT_TAKE_OVER_IND", "HSE_UNIT_CSL_VCNCY_IND", "HSE_UNIT_LIFT_STOP_IND", "HSE_UNIT_TLT_TYPE_CODE", "INCLD_ALL_ENV_CODE_LIST", "INCLD_ANY_ENV_CODE_LIST", "EXCLD_ENV_CODE_LIST", "PAST_THREE_MTH_NO_RFSL_IND", "VLT_ALCT_STD_IND", "ALCT_NEW_FLAT_TYPE_IND", "ALCT_ALL_FLAT_TYPE_IND", "APLY_ALCT_ORD_NUM", "FLAT_SLCT_ORD_NUM", "SAME_EST_AS_PREV_OFR_IND", "LAST_REC_TXN_TYPE_CODE", "LAST_REC_TXN_DATE", "LAST_REC_TXN_USER_ID", "OFR_SPCL_GND_TYPE_CODE", "VLT_LOC_RSTR_IND")
        ctx.register_df("df_sq_2", df_sq_2)
        
        logger.info("Step: apply_EXPTRANS")
        # Expression: apply_EXPTRANS
        df_exp_3 = df_sq_2
        df_exp_3 = df_exp_3.withColumn("BTCH_ALCT_KEY1", expr("BTCH_ALCT_KEY"))
        df_exp_3 = df_exp_3.withColumn("OFR_DATE1", expr("OFR_DATE"))
        df_exp_3 = df_exp_3.withColumn("OFR_SPCL_GND_RSN_CODE_OUT", expr("ltrim(rtrim(OFR_SPCL_GND_RSN_CODE))"))
        df_exp_3 = df_exp_3.withColumn("OFR_SPCL_GND_RSN_TEXT_OUT", expr("ltrim(rtrim(OFR_SPCL_GND_RSN_TEXT))"))
        df_exp_3 = df_exp_3.withColumn("HSE_DSTR_CODE_LIST_OUT", expr("ltrim(rtrim(HSE_DSTR_CODE_LIST))"))
        df_exp_3 = df_exp_3.withColumn("HSE_SBDSTR_CODE_LIST_OUT", expr("ltrim(rtrim(HSE_SBDSTR_CODE_LIST))"))
        df_exp_3 = df_exp_3.withColumn("EXCLD_EST_CODE_OUT", expr("ltrim(rtrim(EXCLD_EST_CODE))"))
        df_exp_3 = df_exp_3.withColumn("DSGN_EST_CODE_OUT", expr("ltrim(rtrim(DSGN_EST_CODE))"))
        df_exp_3 = df_exp_3.withColumn("HSE_BLK_CODE_OUT", expr("ltrim(rtrim(HSE_BLK_CODE))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_TYPE_CODE_OUT", expr("ltrim(rtrim(HSE_UNIT_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("PRH_UNIT_HSC_TYPE_CODE_OUT", expr("ltrim(rtrim(PRH_UNIT_HSC_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_FROM_FLR_NUM_OUT", expr("ltrim(rtrim(HSE_UNIT_FROM_FLR_NUM))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_TO_FLR_NUM_OUT", expr("ltrim(rtrim(HSE_UNIT_TO_FLR_NUM))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_FROM_IFA_AREA_OUT", expr("HSE_UNIT_FROM_IFA_AREA"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_TO_IFA_AREA_OUT", expr("HSE_UNIT_TO_IFA_AREA"))
        df_exp_3 = df_exp_3.withColumn("PRH_UNIT_FROM_RFSL_NUM_OUT", expr("PRH_UNIT_FROM_RFSL_NUM"))
        df_exp_3 = df_exp_3.withColumn("PRH_UNIT_TO_RFSL_NUM_OUT", expr("PRH_UNIT_TO_RFSL_NUM"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_VOID_PRD_DAY_CNT_OUT", expr("HSE_UNIT_VOID_PRD_DAY_CNT"))
        df_exp_3 = df_exp_3.withColumn("PRH_UNIT_TAKE_OVER_IND_OUT", expr("ltrim(rtrim(PRH_UNIT_TAKE_OVER_IND))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_CSL_VCNCY_IND_OUT", expr("ltrim(rtrim(HSE_UNIT_CSL_VCNCY_IND))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_LIFT_STOP_IND_OUT", expr("ltrim(rtrim(HSE_UNIT_LIFT_STOP_IND))"))
        df_exp_3 = df_exp_3.withColumn("HSE_UNIT_TLT_TYPE_CODE_OUT", expr("ltrim(rtrim(HSE_UNIT_TLT_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("INCLD_ALL_ENV_CODE_LIST_OUT", expr("ltrim(rtrim(INCLD_ALL_ENV_CODE_LIST))"))
        df_exp_3 = df_exp_3.withColumn("INCLD_ANY_ENV_CODE_LIST_OUT", expr("ltrim(rtrim(INCLD_ANY_ENV_CODE_LIST))"))
        df_exp_3 = df_exp_3.withColumn("EXCLD_ENV_CODE_LIST_OUT", expr("ltrim(rtrim(EXCLD_ENV_CODE_LIST))"))
        df_exp_3 = df_exp_3.withColumn("PAST_THREE_MTH_NO_RFSL_IND_OUT", expr("ltrim(rtrim(PAST_THREE_MTH_NO_RFSL_IND))"))
        df_exp_3 = df_exp_3.withColumn("VLT_ALCT_STD_IND_OUT", expr("ltrim(rtrim(VLT_ALCT_STD_IND))"))
        df_exp_3 = df_exp_3.withColumn("ALCT_NEW_FLAT_TYPE_IND_OUT", expr("ltrim(rtrim(ALCT_NEW_FLAT_TYPE_IND))"))
        df_exp_3 = df_exp_3.withColumn("ALCT_ALL_FLAT_TYPE_IND_OUT", expr("ltrim(rtrim(ALCT_ALL_FLAT_TYPE_IND))"))
        df_exp_3 = df_exp_3.withColumn("APLY_ALCT_ORD_NUM_OUT", expr("ltrim(rtrim(APLY_ALCT_ORD_NUM))"))
        df_exp_3 = df_exp_3.withColumn("FLAT_SLCT_ORD_NUM_OUT", expr("ltrim(rtrim(FLAT_SLCT_ORD_NUM))"))
        df_exp_3 = df_exp_3.withColumn("SAME_EST_AS_PREV_OFR_IND_OUT", expr("ltrim(rtrim(SAME_EST_AS_PREV_OFR_IND))"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_TYPE_CODE_OUT", expr("ltrim(rtrim(LAST_REC_TXN_TYPE_CODE))"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_DATE_OUT", expr("LAST_REC_TXN_DATE"))
        df_exp_3 = df_exp_3.withColumn("LAST_REC_TXN_USER_ID_OUT", expr("ltrim(rtrim(LAST_REC_TXN_USER_ID))"))
        df_exp_3 = df_exp_3.withColumn("DUMMY", expr("'|'"))
        # Ensure any missing pass-through columns exist (no connector feeding them)
        for _col in ["OFR_SPCL_GND_TYPE_CODE", "VLT_LOC_RSTR_IND"]:
            if _col not in df_exp_3.columns:
                df_exp_3 = df_exp_3.withColumn(_col, lit(None))
        # Select only mapping output ports (prevents column leakage)
        df_exp_3 = df_exp_3.select("BTCH_ALCT_KEY1", "OFR_DATE1", "OFR_SPCL_GND_RSN_CODE_OUT", "OFR_SPCL_GND_RSN_TEXT_OUT", "HSE_DSTR_CODE_LIST_OUT", "HSE_SBDSTR_CODE_LIST_OUT", "EXCLD_EST_CODE_OUT", "DSGN_EST_CODE_OUT", "HSE_BLK_CODE_OUT", "HSE_UNIT_TYPE_CODE_OUT", "PRH_UNIT_HSC_TYPE_CODE_OUT", "HSE_UNIT_FROM_FLR_NUM_OUT", "HSE_UNIT_TO_FLR_NUM_OUT", "HSE_UNIT_FROM_IFA_AREA_OUT", "HSE_UNIT_TO_IFA_AREA_OUT", "PRH_UNIT_FROM_RFSL_NUM_OUT", "PRH_UNIT_TO_RFSL_NUM_OUT", "HSE_UNIT_VOID_PRD_DAY_CNT_OUT", "PRH_UNIT_TAKE_OVER_IND_OUT", "HSE_UNIT_CSL_VCNCY_IND_OUT", "HSE_UNIT_LIFT_STOP_IND_OUT", "HSE_UNIT_TLT_TYPE_CODE_OUT", "INCLD_ALL_ENV_CODE_LIST_OUT", "INCLD_ANY_ENV_CODE_LIST_OUT", "EXCLD_ENV_CODE_LIST_OUT", "PAST_THREE_MTH_NO_RFSL_IND_OUT", "VLT_ALCT_STD_IND_OUT", "ALCT_NEW_FLAT_TYPE_IND_OUT", "ALCT_ALL_FLAT_TYPE_IND_OUT", "APLY_ALCT_ORD_NUM_OUT", "FLAT_SLCT_ORD_NUM_OUT", "SAME_EST_AS_PREV_OFR_IND_OUT", "LAST_REC_TXN_TYPE_CODE_OUT", "LAST_REC_TXN_DATE_OUT", "LAST_REC_TXN_USER_ID_OUT", "DUMMY", "OFR_SPCL_GND_TYPE_CODE", "VLT_LOC_RSTR_IND")
        ctx.register_df("df_exp_3", df_exp_3)
        
        logger.info("Step: write_EMS_PHA_PRH_APLY_OFR_SPCL_GND")
        # Write to Target: write_EMS_PHA_PRH_APLY_OFR_SPCL_GND
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "ofr_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "ofr_spcl_gnd_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofr_spcl_gnd_rsn_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_rsn_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_dstr_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_dstr_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_sbdstr_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_sbdstr_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "excld_est_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excld_est_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "dsgn_est_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dsgn_est_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_blk_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_blk_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "prh_unit_hsc_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "prh_unit_hsc_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_from_flr_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_from_flr_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_to_flr_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_to_flr_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "prh_unit_take_over_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "prh_unit_take_over_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_csl_vcncy_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_csl_vcncy_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_lift_stop_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_lift_stop_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_tlt_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_tlt_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incld_all_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incld_all_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incld_any_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incld_any_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "excld_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excld_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "past_three_mth_no_rfsl_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "past_three_mth_no_rfsl_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "vlt_alct_std_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "vlt_alct_std_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "alct_new_flat_type_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "alct_new_flat_type_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "alct_all_flat_type_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "alct_all_flat_type_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "aply_alct_ord_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "aply_alct_ord_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "flat_slct_ord_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flat_slct_ord_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "same_est_as_prev_ofr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "same_est_as_prev_ofr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofr_spcl_gnd_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "vlt_loc_rstr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "vlt_loc_rstr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"ALCT_ALL_FLAT_TYPE_IND": "ALCT_ALL_FLAT_TYPE_IND_OUT", "ALCT_NEW_FLAT_TYPE_IND": "ALCT_NEW_FLAT_TYPE_IND_OUT", "APLY_ALCT_ORD_NUM": "APLY_ALCT_ORD_NUM_OUT", "BTCH_ALCT_KEY": "BTCH_ALCT_KEY1", "DSGN_EST_CODE": "DSGN_EST_CODE_OUT", "EXCLD_ENV_CODE_LIST": "EXCLD_ENV_CODE_LIST_OUT", "EXCLD_EST_CODE": "EXCLD_EST_CODE_OUT", "FLAT_SLCT_ORD_NUM": "FLAT_SLCT_ORD_NUM_OUT", "HSE_BLK_CODE": "HSE_BLK_CODE_OUT", "HSE_DSTR_CODE_LIST": "HSE_DSTR_CODE_LIST_OUT", "HSE_SBDSTR_CODE_LIST": "HSE_SBDSTR_CODE_LIST_OUT", "HSE_UNIT_CSL_VCNCY_IND": "HSE_UNIT_CSL_VCNCY_IND_OUT", "HSE_UNIT_FROM_FLR_NUM": "HSE_UNIT_FROM_FLR_NUM_OUT", "HSE_UNIT_FROM_IFA_AREA": "HSE_UNIT_FROM_IFA_AREA_OUT", "HSE_UNIT_LIFT_STOP_IND": "HSE_UNIT_LIFT_STOP_IND_OUT", "HSE_UNIT_TLT_TYPE_CODE": "HSE_UNIT_TLT_TYPE_CODE_OUT", "HSE_UNIT_TO_FLR_NUM": "HSE_UNIT_TO_FLR_NUM_OUT", "HSE_UNIT_TO_IFA_AREA": "HSE_UNIT_TO_IFA_AREA_OUT", "HSE_UNIT_TYPE_CODE": "HSE_UNIT_TYPE_CODE_OUT", "HSE_UNIT_VOID_PRD_DAY_CNT": "HSE_UNIT_VOID_PRD_DAY_CNT_OUT", "INCLD_ALL_ENV_CODE_LIST": "INCLD_ALL_ENV_CODE_LIST_OUT", "INCLD_ANY_ENV_CODE_LIST": "INCLD_ANY_ENV_CODE_LIST_OUT", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE_OUT", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE_OUT", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID_OUT", "OFR_DATE": "OFR_DATE1", "OFR_SPCL_GND_RSN_CODE": "OFR_SPCL_GND_RSN_CODE_OUT", "OFR_SPCL_GND_RSN_TEXT": "OFR_SPCL_GND_RSN_TEXT_OUT", "OFR_SPCL_GND_TYPE_CODE": "OFR_SPCL_GND_TYPE_CODE", "PAST_THREE_MTH_NO_RFSL_IND": "PAST_THREE_MTH_NO_RFSL_IND_OUT", "PRH_UNIT_FROM_RFSL_NUM": "PRH_UNIT_FROM_RFSL_NUM_OUT", "PRH_UNIT_HSC_TYPE_CODE": "PRH_UNIT_HSC_TYPE_CODE_OUT", "PRH_UNIT_TAKE_OVER_IND": "PRH_UNIT_TAKE_OVER_IND_OUT", "PRH_UNIT_TO_RFSL_NUM": "PRH_UNIT_TO_RFSL_NUM_OUT", "SAME_EST_AS_PREV_OFR_IND": "SAME_EST_AS_PREV_OFR_IND_OUT", "VLT_ALCT_STD_IND": "VLT_ALCT_STD_IND_OUT", "VLT_LOC_RSTR_IND": "VLT_LOC_RSTR_IND"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['BTCH_ALCT_KEY', 'OFR_DATE', 'OFR_SPCL_GND_RSN_CODE', 'OFR_SPCL_GND_RSN_TEXT', 'HSE_DSTR_CODE_LIST', 'HSE_SBDSTR_CODE_LIST', 'EXCLD_EST_CODE', 'DSGN_EST_CODE', 'HSE_BLK_CODE', 'HSE_UNIT_TYPE_CODE', 'PRH_UNIT_HSC_TYPE_CODE', 'HSE_UNIT_FROM_FLR_NUM', 'HSE_UNIT_TO_FLR_NUM', 'HSE_UNIT_FROM_IFA_AREA', 'HSE_UNIT_TO_IFA_AREA', 'PRH_UNIT_FROM_RFSL_NUM', 'PRH_UNIT_TO_RFSL_NUM', 'HSE_UNIT_VOID_PRD_DAY_CNT', 'PRH_UNIT_TAKE_OVER_IND', 'HSE_UNIT_CSL_VCNCY_IND', 'HSE_UNIT_LIFT_STOP_IND', 'HSE_UNIT_TLT_TYPE_CODE', 'INCLD_ALL_ENV_CODE_LIST', 'INCLD_ANY_ENV_CODE_LIST', 'EXCLD_ENV_CODE_LIST', 'PAST_THREE_MTH_NO_RFSL_IND', 'VLT_ALCT_STD_IND', 'ALCT_NEW_FLAT_TYPE_IND', 'ALCT_ALL_FLAT_TYPE_IND', 'APLY_ALCT_ORD_NUM', 'FLAT_SLCT_ORD_NUM', 'SAME_EST_AS_PREV_OFR_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID', 'OFR_SPCL_GND_TYPE_CODE', 'VLT_LOC_RSTR_IND']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_PHA_PRH_APLY_OFR_SPCL_GND", mode="append")

        logger.info("write_EMS_PHA_PRH_APLY_OFR_SPCL_GND write completed")
        logger.info("Step: write_EMS_PHA_PRH_APLY_OFR_SPCL_GND1")
        # Write to Target: write_EMS_PHA_PRH_APLY_OFR_SPCL_GND1
        df_write = df_exp_3
        # Cast columns to match target schema data types
        if "ofr_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_date":
                    df_write = df_write.withColumn(c, col(c).cast(DateType()))
        if "ofr_spcl_gnd_rsn_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_rsn_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofr_spcl_gnd_rsn_text" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_rsn_text":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_dstr_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_dstr_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_sbdstr_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_sbdstr_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "excld_est_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excld_est_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "dsgn_est_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dsgn_est_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_blk_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_blk_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "prh_unit_hsc_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "prh_unit_hsc_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_from_flr_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_from_flr_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_to_flr_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_to_flr_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "prh_unit_take_over_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "prh_unit_take_over_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_csl_vcncy_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_csl_vcncy_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_lift_stop_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_lift_stop_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "hse_unit_tlt_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "hse_unit_tlt_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incld_all_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incld_all_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "incld_any_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "incld_any_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "excld_env_code_list" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "excld_env_code_list":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "past_three_mth_no_rfsl_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "past_three_mth_no_rfsl_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "vlt_alct_std_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "vlt_alct_std_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "alct_new_flat_type_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "alct_new_flat_type_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "alct_all_flat_type_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "alct_all_flat_type_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "aply_alct_ord_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "aply_alct_ord_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "flat_slct_ord_num" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "flat_slct_ord_num":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "same_est_as_prev_ofr_ind" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "same_est_as_prev_ofr_ind":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_date" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_date":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "last_rec_txn_user_id" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "last_rec_txn_user_id":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "ofr_spcl_gnd_type_code" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "ofr_spcl_gnd_type_code":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        if "dummy" in [c.lower() for c in df_write.columns]:
            for c in df_write.columns:
                if c.lower() == "dummy":
                    df_write = df_write.withColumn(c,
                        when(col(c).cast(DecimalType(38,0)).isNotNull(),
                             col(c).cast(DecimalType(38,0)).cast(StringType()))
                        .otherwise(col(c).cast(StringType())))
        # Map source columns to target columns using connector field map (handles name mismatches)
        _field_map = {"ALCT_ALL_FLAT_TYPE_IND": "ALCT_ALL_FLAT_TYPE_IND_OUT", "ALCT_NEW_FLAT_TYPE_IND": "ALCT_NEW_FLAT_TYPE_IND_OUT", "APLY_ALCT_ORD_NUM": "APLY_ALCT_ORD_NUM_OUT", "BTCH_ALCT_KEY": "BTCH_ALCT_KEY1", "DSGN_EST_CODE": "DSGN_EST_CODE_OUT", "DUMMY": "DUMMY", "EXCLD_ENV_CODE_LIST": "EXCLD_ENV_CODE_LIST_OUT", "EXCLD_EST_CODE": "EXCLD_EST_CODE_OUT", "FLAT_SLCT_ORD_NUM": "FLAT_SLCT_ORD_NUM_OUT", "HSE_BLK_CODE": "HSE_BLK_CODE_OUT", "HSE_DSTR_CODE_LIST": "HSE_DSTR_CODE_LIST_OUT", "HSE_SBDSTR_CODE_LIST": "HSE_SBDSTR_CODE_LIST_OUT", "HSE_UNIT_CSL_VCNCY_IND": "HSE_UNIT_CSL_VCNCY_IND_OUT", "HSE_UNIT_FROM_FLR_NUM": "HSE_UNIT_FROM_FLR_NUM_OUT", "HSE_UNIT_FROM_IFA_AREA": "HSE_UNIT_FROM_IFA_AREA_OUT", "HSE_UNIT_LIFT_STOP_IND": "HSE_UNIT_LIFT_STOP_IND_OUT", "HSE_UNIT_TLT_TYPE_CODE": "HSE_UNIT_TLT_TYPE_CODE_OUT", "HSE_UNIT_TO_FLR_NUM": "HSE_UNIT_TO_FLR_NUM_OUT", "HSE_UNIT_TO_IFA_AREA": "HSE_UNIT_TO_IFA_AREA_OUT", "HSE_UNIT_TYPE_CODE": "HSE_UNIT_TYPE_CODE_OUT", "HSE_UNIT_VOID_PRD_DAY_CNT": "HSE_UNIT_VOID_PRD_DAY_CNT_OUT", "INCLD_ALL_ENV_CODE_LIST": "INCLD_ALL_ENV_CODE_LIST_OUT", "INCLD_ANY_ENV_CODE_LIST": "INCLD_ANY_ENV_CODE_LIST_OUT", "LAST_REC_TXN_DATE": "LAST_REC_TXN_DATE_OUT", "LAST_REC_TXN_TYPE_CODE": "LAST_REC_TXN_TYPE_CODE_OUT", "LAST_REC_TXN_USER_ID": "LAST_REC_TXN_USER_ID_OUT", "OFR_DATE": "OFR_DATE1", "OFR_SPCL_GND_RSN_CODE": "OFR_SPCL_GND_RSN_CODE_OUT", "OFR_SPCL_GND_RSN_TEXT": "OFR_SPCL_GND_RSN_TEXT_OUT", "OFR_SPCL_GND_TYPE_CODE": "OFR_SPCL_GND_TYPE_CODE", "PAST_THREE_MTH_NO_RFSL_IND": "PAST_THREE_MTH_NO_RFSL_IND_OUT", "PRH_UNIT_FROM_RFSL_NUM": "PRH_UNIT_FROM_RFSL_NUM_OUT", "PRH_UNIT_HSC_TYPE_CODE": "PRH_UNIT_HSC_TYPE_CODE_OUT", "PRH_UNIT_TAKE_OVER_IND": "PRH_UNIT_TAKE_OVER_IND_OUT", "PRH_UNIT_TO_RFSL_NUM": "PRH_UNIT_TO_RFSL_NUM_OUT", "SAME_EST_AS_PREV_OFR_IND": "SAME_EST_AS_PREV_OFR_IND_OUT", "VLT_ALCT_STD_IND": "VLT_ALCT_STD_IND_OUT"}
        for _tgt_col, _src_col in _field_map.items():
            if _tgt_col not in df_write.columns and _src_col in df_write.columns:
                df_write = df_write.withColumnRenamed(_src_col, _tgt_col)
        # Select only target-defined columns (field_map already handled name alignment)
        _target_cols = ['BTCH_ALCT_KEY', 'OFR_DATE', 'OFR_SPCL_GND_RSN_CODE', 'OFR_SPCL_GND_RSN_TEXT', 'HSE_DSTR_CODE_LIST', 'HSE_SBDSTR_CODE_LIST', 'EXCLD_EST_CODE', 'DSGN_EST_CODE', 'HSE_BLK_CODE', 'HSE_UNIT_TYPE_CODE', 'PRH_UNIT_HSC_TYPE_CODE', 'HSE_UNIT_FROM_FLR_NUM', 'HSE_UNIT_TO_FLR_NUM', 'HSE_UNIT_FROM_IFA_AREA', 'HSE_UNIT_TO_IFA_AREA', 'PRH_UNIT_FROM_RFSL_NUM', 'PRH_UNIT_TO_RFSL_NUM', 'HSE_UNIT_VOID_PRD_DAY_CNT', 'PRH_UNIT_TAKE_OVER_IND', 'HSE_UNIT_CSL_VCNCY_IND', 'HSE_UNIT_LIFT_STOP_IND', 'HSE_UNIT_TLT_TYPE_CODE', 'INCLD_ALL_ENV_CODE_LIST', 'INCLD_ANY_ENV_CODE_LIST', 'EXCLD_ENV_CODE_LIST', 'PAST_THREE_MTH_NO_RFSL_IND', 'VLT_ALCT_STD_IND', 'ALCT_NEW_FLAT_TYPE_IND', 'ALCT_ALL_FLAT_TYPE_IND', 'APLY_ALCT_ORD_NUM', 'FLAT_SLCT_ORD_NUM', 'SAME_EST_AS_PREV_OFR_IND', 'LAST_REC_TXN_TYPE_CODE', 'LAST_REC_TXN_DATE', 'LAST_REC_TXN_USER_ID', 'OFR_SPCL_GND_TYPE_CODE', 'DUMMY']
        df_write = df_write.select(*[col for col in _target_cols if col in df_write.columns])
        # Write to database table (Oracle, etc.) using write_table (supports smart repartition, batch size, empty-df skip)
        lib.write_table(df_write, conn_target, "EMS_PHA_PRH_APLY_OFR_SPCL_GND1", mode="append")

        logger.info("write_EMS_PHA_PRH_APLY_OFR_SPCL_GND1 write completed")
        
        metrics.complete()
        logger.info("Mapping M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND completed: SUCCESS")
        return True
        
    except Exception as e:
        metrics.fail(e)
        logger.error(f"Mapping M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND FAILED: {e}", exc_info=True)
        return False


def main():
    """Standalone execution entry point."""
    lib.init_logger()
    logger = lib.get_logger()
    config = lib.load_config('env/config.yml')
    spark = lib.get_spark_session("M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND", config)
    ctx = lib.SparkContext(spark)
    metrics = lib.MappingMetrics("M_S5_SSAL1_EXTRACT_PHA_PRH_APLY_OFR_SPCL_GND")
    
    try:
        success = run_mapping(ctx, metrics)
        if success:
            lib._flush_pending_passwords()
        return 0 if success else 1
    finally:
        spark.stop()


if __name__ == "__main__":
    main()