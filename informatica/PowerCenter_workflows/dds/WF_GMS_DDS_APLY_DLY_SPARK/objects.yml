# Minimal object definitions for runtime
# Generated from: PCIS_GMS

objects:
    DDS_DMNS_GMS_MSD_CODE:
        connection: "DDS"

    DDS_DMNS_GMS_SCORE_GRP:
        connection: "DDS"

    DDS_FACT_GMS_DLY_MSD_SMRY:
        connection: "DDS"

    DPA_FACT_GMS_DLY_MSD_SMRY:
        connection: "DPA"

    DDS_HRCHY_GMS_EST:
        connection: "DDS"

    DDS_FACT_GMS_DLY_DOG_RGSTR:
        connection: "DDS"

    DPA_FACT_GMS_DLY_DOG_RGSTR:
        connection: "DPA"

    DPA_FACT_GMS_DLY_MSD_INCDT:
        connection: "DPA"

    UTL_SESSION_LIST:
        file:
            format: "csv"
            path: "/tmp/UTL_SESSION_LIST"
            delimiter: ","
            header: "true"

    UTL_SSA_TBL_LIST:
        file:
            format: "csv"
            path: "/tmp/UTL_SSA_TBL_LIST"
            delimiter: ","
            header: "true"

    DDS_FACT_GMS_DLY_MSD_INCDT:
        connection: "${TARGET_CONNECTION:target}"

    DDS_DMNS_TIME:
        connection: "${TARGET_CONNECTION:target}"

    DDS_DMNS_EMS_AGE_GRP:
        connection: "${TARGET_CONNECTION:target}"

    DDS_DMNS_GNDR:
        connection: "${TARGET_CONNECTION:target}"

    DDS_DMNS_MSSEM_OFCR_TYPE:
        connection: "${TARGET_CONNECTION:target}"

    UTL_JOB_PARAM:
        file:
            format: "csv"
            path: "/tmp/UTL_JOB_PARAM"
            delimiter: ","
            header: "true"

    UTL_DEV_NULL:
        file:
            format: "csv"
            path: "/tmp/UTL_DEV_NULL.csv"
            delimiter: ","
            header: "true"
