# Configuration for PySpark Job
# Auto-generated by Informatica XML to PySpark Converter
# Generated from: PCIS_GMS

# =============================================================================
# ENVIRONMENT SETTINGS
# =============================================================================
environment:
  name: ${ENV_NAME:development}
  log_level: ${LOG_LEVEL:INFO}
  
# =============================================================================
# SPARK CONFIGURATION
# =============================================================================
spark:
  app_name: "PCIS_GMS"
  master: ${SPARK_MASTER:local[*]}
  config:
    spark.ui.port: "4040"
    spark.driver.memory: "16g"
    spark.executor.memory: "16g"
    spark.sql.shuffle.partitions: "50"
    spark.default.parallelism: "50"
    spark.sql.debug.maxToStringFields: "100"
    spark.driver.maxResultSize: "8g"
    spark.sql.autoBroadcastJoinThreshold: "-1"
    spark.memory.fraction: "0.8"
    spark.memory.storageFraction: "0.3"
    spark.dynamicAllocation.enabled: "true"
    spark.shuffle.service.enabled: "true"
    spark.sql.adaptive.enabled: "true"

# =============================================================================
# DATABASE CONNECTIONS (Auto-detected from XML)
# =============================================================================
connections:
  DDS:
    type: "oracle"
    host: "${DB_HOST}"
    port: 1521
    database: "DDS"
    username: "${DB_USER}"
    password: "${DB_PASSWORD}"
    schema: "PDDS"
    driver: "oracle.jdbc.driver.OracleDriver"
  DPA:
    type: "oracle"
    host: "${DB_HOST}"
    port: 1521
    database: "DPA"
    username: "${DB_USER}"
    password: "${DB_PASSWORD}"
    schema: "PDPA"
    driver: "oracle.jdbc.driver.OracleDriver"
  UTL:
    type: "flatfile"
    host: "${DB_HOST}"
    port: 1433
    database: "UTL"
    username: "${DB_USER}"
    password: "${DB_PASSWORD}"
    schema: "dbo"
    driver: "com.microsoft.sqlserver.jdbc.SQLServerDriver"

# =============================================================================
# FILE PATHS
# =============================================================================
paths:
  input_base: "${INPUT_PATH:/data/input}"
  output_base: "${OUTPUT_PATH:/data/output}"
  staging: "${STAGING_PATH:/data/staging}"
  archive: "${ARCHIVE_PATH:/data/archive}"

# =============================================================================
# SOURCE/TARGET TABLE DEFINITIONS
# =============================================================================
tables:
  DDS_DMNS_GMS_MSD_CODE:
    connection: "DDS"
    database: "DDS"
    schema: "PDDS"
    type: "source"    DDS_DMNS_GMS_SCORE_GRP:
    connection: "DDS"
    database: "DDS"
    schema: "PDDS"
    type: "source"    DDS_FACT_GMS_DLY_MSD_SMRY:
    connection: "DDS"
    database: "DDS"
    schema: "PDDS"
    type: "source"    DPA_FACT_GMS_DLY_MSD_SMRY:
    connection: "DPA"
    database: "DPA"
    schema: "PDPA"
    type: "source"    DDS_HRCHY_GMS_EST:
    connection: "DDS"
    database: "DDS"
    schema: "PDDS"
    type: "source"    DDS_FACT_GMS_DLY_DOG_RGSTR:
    connection: "DDS"
    database: "DDS"
    schema: "PDDS"
    type: "source"    DPA_FACT_GMS_DLY_DOG_RGSTR:
    connection: "DPA"
    database: "DPA"
    schema: "PDPA"
    type: "source"    DPA_FACT_GMS_DLY_MSD_INCDT:
    connection: "DPA"
    database: "DPA"
    schema: "PDPA"
    type: "source"    UTL_SESSION_LIST:
    connection: "UTL"
    database: "UTL"
    schema: "dbo"
    type: "source"    UTL_SSA_TBL_LIST:
    connection: "UTL"
    database: "UTL"
    schema: "dbo"
    type: "source"    DDS_FACT_GMS_DLY_MSD_INCDT:
    connection: "target"
    database: ""
    schema: "dbo"
    type: "target"    DDS_DMNS_TIME:
    connection: "target"
    database: ""
    schema: "dbo"
    type: "target"    DDS_DMNS_EMS_AGE_GRP:
    connection: "target"
    database: ""
    schema: "dbo"
    type: "target"    DDS_DMNS_GNDR:
    connection: "target"
    database: ""
    schema: "dbo"
    type: "target"    DDS_DMNS_MSSEM_OFCR_TYPE:
    connection: "target"
    database: ""
    schema: "dbo"
    type: "target"    UTL_JOB_PARAM:
    connection: "target"
    database: ""
    schema: "dbo"
    type: "target"    UTL_DEV_NULL:
    connection: "target"
    database: ""
    schema: "dbo"
    type: "target"  
# =============================================================================
# MAPPING VARIABLES
# =============================================================================
variables:
  v_snsh_date:
    datatype: "string"
    default_value: ""

# =============================================================================
# CONVERSION OPTIONS
# =============================================================================
options:
  use_delta_format: true
  broadcast_lookups: true
  enable_schema_evolution: true
  enable_metrics: true

# =============================================================================
# MAPPING-SPECIFIC RULES
# =============================================================================
rules:
  date_format: "yyyy-MM-dd"
  timestamp_format: "yyyy-MM-dd HH:mm:ss"
  decimal_precision: 38
  decimal_scale: 10
  string_max_length: 4000
  batch_size: 10000